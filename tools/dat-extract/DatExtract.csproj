<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Lod.DatExtract</RootNamespace>
    <AssemblyName>dat-extract</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- The reader that already ships with the server repo. Assets come from our own archives, never from
         a restored copy made elsewhere. -->
    <ProjectReference Include="..\..\sources\wren11\Dark-Ages-Private-Server\src\Hades.Client.Base\Hades.Client.Base.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="SixLabors.ImageSharp" Version="3.1.5" />
  </ItemGroup>

</Project>
