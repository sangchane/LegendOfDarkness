# 포테의숲 — 구현할 수 있나 (조사, 2026-09-17)

사용자: "포테의숲 구현 가능한지 찾아두라" → "포테의숲 진행". 맵·괴물·그림은 이미 서버에 있었고, 막힌 것은 **들어가는 길(워프)** 하나였다.

**2026-09-17 구현함 — 1~6존.** 수오미마을 동쪽 끝(99,24~27)을 밟으면 1존 33,47 에 도착하고 괴물이 보인다
(`tests/hades-characterization/PoteForestTests.cs`). 워프 37장은 `python3 tools/pack-import/import.py --kind potewarps --write`.
모바일 맵 재료는 1~6존·보스존과, 입구까지 걸어갈 수오미마을 바닥(`scripts/build-client-maps.py`). **남은 것**: 보스존(아래 4) ·
입장 레벨 21~51 · 수오미마을 안 건물(수오미 작업 때, 사용자).

## 1. 이미 있는 것

| 맵 (서버 번호) | 크기 | 괴물 템플릿(마릿수) | 괴물 그림 |
|---|---|---|---|
| 포테의숲1존 (20263) | 50x50 | 옐로우·그린·레드·퍼플팜팻 10씩 · 실버팜팻 4 · 사슴 1 | 있음 |
| 포테의숲2존 (20264) | 50x50 | 퍼플 15 · 그린·레드 12 · 옐로우 10 · 트랜트 10 · 실버 4 · 사슴 1 | 있음 |
| 포테의숲3존 (20265) | 70x70 | 레드 20 · 트랜트 15 · 그린 13 · 엔트라이온 7 · 실버 5 · 사슴 1 | 있음 |
| 포테의숲4존 (20266) | 41x47 | 놀 19 · 엔트라이온 10 · 실버 5 · 엔트자이언트 4 · 사슴 1 | 있음 |
| 포테의숲5존 (20267) | 30x30 | 놀 12 · 은빛늑대 7 · 엔트자이언트 6 · 사슴 1 | 있음 |
| 포테의숲6존 (20268) | 48x31 | 은빛늑대 7 · 엔트자이언트 7 · 사슴 1 | 있음 |
| 포테의숲보스존 (20269) | 20x20 | 없음 | — |

5.99 `mob/Fota/Fota_Spawn.txt` 와 같은 수다.

## 2. 없는 것 — 들어가는 길

- 5.99 원본 `db/warp/Suomi_Warp.txt` 에 **수오미마을 (99,24~27) → 1존 (33,47)**, 1존 ↔ 2존 ↔ 3존 ↔ 4존 ↔ 5존·6존 워프 37줄이 있다
  (레벨 21~51, 나오는 길은 0~99).
- 그런데 5.99 의 워프 목록 `db/warp/warp_db.txt` 가 이 파일을 **싣지 않는다** — 5.99 서버에서도 포테의숲에 갈 수 없었다.
  같은 이유로 `Baeqnatop`·`Casmanum`·`Christmas`·`DraculaCastle`·`Mechanic`·`Roues` 워프 파일도 빠져 있다
  (`data/server-packs/extracted/5.99-server/매니페스트에없는파일.json`). 추출 자료 `warps.json` 에도 없어서 서버에 워프가 안 들어왔다.
- **Novaonline 팩은 같은 맵 파일(`lod6613`…`lod6620`)을 같은 연결로 싣는다** — 수오미마을 → 1존 → … → 5존·6존. 혼든 팩은 다른 판이다
  (피에트마을 → 대기실 → L·C·R 갈래 4층, 맵 파일 `lod4600~4611`).
- 5.99 `script/script.txt` 에 주석으로 남은 말: "레벨 21 … 노비스를 졸업 … 이제부터는 수오미마을과 포테의숲에서 활동" — 노비스 사냥터
  (레벨 1~22) 다음 단계로 만들어 둔 곳이다.

## 3. 구현하려면

1. **(했다)** `Suomi_Warp.txt` 의 포테의숲 줄을 워프 템플릿으로 들인다 — 가져오기 도구의 `potewarps` 갈래(한쪽 끝이 포테의숲인 줄만).
   레벨은 다른 워프와 같이 1 로 들어갔다 — **레벨 상한 51 은 하데스 워프에 칸이 없다**(`WarpTemplate.LevelRequired` — 하한만).
2. **(했다)** 모바일 맵 재료: `python3 scripts/build-client-maps.py` 기본 목록에 1~6존·보스존·수오미마을.
3. 입구 수오미마을(20355)은 월드맵 점(수오미 → 40,11)으로 이미 갈 수 있다. 마을 안 건물 워프도 같은 `Suomi_Warp.txt` 에 있다 — 수오미 작업 때 함께.
4. (선택) **5존 위 오솔길 던전** — `포테의숲오솔길입장` 스크립트(5존 26·27,0 에서, 레벨 52 미만)가 캐릭터마다 맵을 새로 만들어
   (`map_create` ×3 · `warp_create` · `group_warp`) 사나운은빛늑대와 보스 자이언트맨티스를 세운다(`mob_spawn3`).
   이 명령들은 `Pack599.cs` 에 아직 없고, 두 괴물은 팩 정의(`mobs.json`)에는 있지만 서버 템플릿에는 없다. 1~6존과 따로 해도 된다.
