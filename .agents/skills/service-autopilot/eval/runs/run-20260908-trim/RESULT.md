# 각본 뺀 스킬 A/B — 2026-09-08~09 (v1.4.1 감사 1단계 검증)

> 질문: Fable 5.1용으로 압력 어조·사건 서술·교육 문장·중복 절차를 뺀 1.4.1이, 각본이 있던 1.3.0 산출물(0907 S4)보다 나쁜가.
> Anthropic Fable 5.1 이전 체크리스트 "[TUNE] A/B with prior-model scaffolding removed" 실행.
> 비교: **A** = 0907 S4 산출물(스킬 1.3.0, 각본·압력 어조 있음, GATE FAIL→패치 1회→CONCERNS) vs **B** = 스킬 1.4.1로 새로 생성(GATE CONCERNS, 30건 중 2건 반영).
> 판정: Opus judge 2회(원순서+스왑), `fresh-reviewer` 타입. 불일치 차원은 tie. 집계 표의 "A/B" 라벨은 이 런에서 각각 1.3.0 / 1.4.1을 뜻한다.

## 결과

| 시드 | 종합 | 완전성 | 비모호성 | 검증가능성 | 실행가능성 | 근거성 | 범위 절제 |
|---|---|---|---|---|---|---|---|
| S4 Pi IP 카메라 · full | **A (1.3.0)** — 원순서·스왑 일관 | tie (A / tie) | A | A | A | tie (tie / B) | A |

**각본 뺀 1.4.1이 졌다. 4패 2무.** 두 judge가 든 패인은 같다.

## 판정 이유 (judge 원문 요지)

1. **검토 지적을 닫았는가.** A는 GATE 15건을 전부 패치해 03~07에 전파한 상태로 마감했다. B는 검토관이 30건(CRITICAL 1·HIGH 6·MEDIUM 15·LOW 8)을 냈는데 2건만 반영하고 28건을 "핸드오프 선행 조건"으로 넘겼고, 08에 "이 문서만 들고 구현을 시작하면 첫 스켈레톤에서 되돌아온다"고 스스로 적었다. 비모호성·검증가능성·실행가능성 세 차원이 전부 여기서 갈렸다.
2. **범위.** B는 시드에 없는 중앙 서버·멀티사이트(사이트 ≤10)를 A0 해석으로 세우고 그 위에 VPS·PostgreSQL·mosquitto·coturn·내부 CA·cosign OTA·PWA·RBAC를 얹었다. 팀 전제는 1~2인. A는 로컬 우선(Tailscale·SQLite·ntfy)으로 좁혔다.
3. **B가 이긴 곳.** 스왑 judge는 근거성을 B에 줬다: 출처 표 27행 대 15행, 상수 근거 4분류(출처 URL·실측·DL-#·미확인), 미확인 상수의 SC 격리가 검토관 대조로 확인됨. 원순서 judge는 tie. 이건 1.4.0에서 넣은 것들이 읽힌 결과다.

## 해석 — 각본을 뺀 탓인가

아니다, 적어도 이 데이터로는 그렇게 말할 수 없다. 패인 1은 각본이 아니라 **패치 라운드의 유무**다.

- 1.3.0과 1.4.1의 규칙은 같다: "FAIL이면 해당 단계 재실행 1회, CONCERNS면 마감". A는 검토관이 FAIL을 줘서 패치 라운드를 받았고, B는 검토관이 CONCERNS를 줘서(재실행 사유 없음) 규칙대로 마감했다. 검토관의 판정 라벨 하나가 결과를 갈랐다. 게다가 B의 GATE는 한도로 죽은 에이전트 대신 메인이 예산을 아끼며 수행해, 반영을 기계적 2건으로 제한했다.
- B는 두 번 한도로 끊겼고 재개 시 58만 토큰 대화가 20만 상한에 걸려 압축됐다. R1 전파 누락(#2·#8·#9)과 표기 불일치는 압축 뒤 맥락 손실로 설명되는 종류다. A는 중단 1회, 압축 없음.
- 범위 팽창(패인 2)은 A0 해석 목록(1.4.0)이 "원격 시청 경로"를 명시적으로 결정하게 만든 결과일 수 있다. 시드가 모호하니 n=1로는 스킬 탓인지 런 편차인지 못 가른다.
- 어조·사건 서술·교육 문장 제거가 품질을 떨어뜨렸다는 흔적은 judge 어디에도 없다. 규칙 1 위반(프론트 스택 무근거)이 1건 있었지만 규칙 1 문장은 1.4.1에서 바뀌지 않았다.

그래서 이 런이 실제로 증명한 것은 두 가지다.
1. **judge는 "지적이 닫혔는가"를 가장 무겁게 본다.** 0907에서도 GATE 패치 뒤 문서가 이겼고, 이번엔 패치를 안 한 쪽이 졌다. CONCERNS라도 HIGH가 남으면 마감하면 안 된다 → 규칙 개정 후보: "CONCERNS + HIGH ≥1이면 패치 1회 후 마감. 선행 조건으로 넘길 수 있는 건 MEDIUM 이하".
2. **컨텍스트 상한 20만이 full 런의 설계 변수다.** 단계마다 파일이 진실원이라는 재개 규칙은 맞았지만, 압축 직후 단계는 전파 검사(check_package C6·C8)만으로는 못 잡는 의미 드리프트를 낸다.

## 비용

| | B (1.4.1) |
|---|---|
| 생성 | 09-08 14:31 → 09-09 02:05 (한도 중단 2회, 재개 2회, 압축 1회) · 메인 토큰 미상(에이전트가 보고 전 종료) |
| 서브에이전트 | A1 조사 sonnet · A4 검토관 fable 6.4만 · GATE 검토관 fable 15.7만 |
| judge | Opus 22.5만 + 22.9만 (각 38만 바이트 읽기) |
| 5시간 창 | 09-08 14:30 창을 S8과 함께 30분에 소진 · 09-09 01:05 창 약 45% · 06:05 창 약 70%(GATE+판정) |

## 이탈 기록

1. A는 0907 런의 s4-b(1.3.0)를 재사용. 1.4.0 추가분(상수 근거 4종·NEXT.md·해석 목록)과 1.4.1 감사분이 섞여 비교됐다.
2. B의 check_package·GATE·08은 생성 에이전트가 아니라 메인 세션이 수행했다(에이전트가 06:00 한도로 종료). GATE 반영은 2건으로 제한.
3. 판정관에 ponytail 페르소나 주입(세션이 settings.json 변경 전 시작). 0907과 같은 조건.
4. 블라인드 누수: doc마다 3곳(`stage-templates`·`service-autopilot이`·`autopilot 산출물`) — 한글 뒤 `\b` 경계 버그. 양쪽 대칭이라 판정에 비대칭 영향은 없다고 보나, blind_prep을 고쳤다(다음 런부터 적용).
5. S8은 A5 + A4 검토(FAIL, CRITICAL 1)까지 만들고 사용자 지시로 일시 중지. 재개하면 1.4.2 템플릿을 읽게 되므로 버전 혼합을 기록해야 한다.

## 재대결 — B에 패치 라운드 1회를 준 뒤 (2026-09-09 07:30~08:10)

B(1.4.1)에 A와 같은 조건을 주었다: GATE 지적 28건 중 27건 반영(R4, 03 v1.3 → 04 v1.3·05 v1.2·06 v1.1·07 v1.1 전파, 상수 6개 추가, 선행 조건 5→2), check_package CRITICAL 0·HIGH 0, 블라인드 누수 0. 같은 A와 Opus 판정 2회(새 무작위 배정, 원순서+스왑).

| 시드 | 종합 | 완전성 | 비모호성 | 검증가능성 | 실행가능성 | 근거성 | 범위 절제 |
|---|---|---|---|---|---|---|---|
| S4 · 재대결 | **tie** (두 judge가 각자 doc1 선택 — 위치 편향) | **B** | tie (tie / A) | tie (B / tie) | tie (tie / A) | **B** | A |

1라운드 A 4승 2무 → 재대결 B 2승 1패 3무, 종합 tie. **패치 라운드 하나가 결과를 뒤집었다.**

- B가 이긴 것: 완전성(인증서 수명주기·OTA 롤백·감사 fail-closed·MQTT/WS 계약·DDL까지 내려감), 근거성(상수별 근거 유형·미확인 격리·가정의 되돌리기 비용 정렬). 둘 다 1.4.0에서 넣은 규칙이 만든 것이다.
- A가 이긴 것: 범위 절제 — 두 judge 모두 일관. B는 A0 해석에서 "원격 시청 = 중앙 서버 경유, 사이트 ≤10"을 세우고 VPS·CA·cosign·PWA를 얹었다. 1~2인 팀 전제와 어긋난다고 두 judge가 같은 문장을 인용했다.
- 종합이 tie가 된 이유: 원순서 judge는 "결정이 닫혔고 추적 사슬이 촘촘하다"로 B, 스왑 judge는 "범위가 요구에 비례하고 첫 작업이 스파이크가 아니다"로 A. 둘 다 doc1을 골랐으니 순수 위치 편향의 몫도 있다.

### 결론

1. **각본 제거는 무해하다.** 어조·사건 서술·교육 문장·중복 절차를 뺀 1.4.1은 패치 라운드만 같으면 1.3.0과 대등하거나(종합 tie) 두 차원에서 앞선다. 1라운드의 4패는 각본이 아니라 패치 라운드 유무의 결과였다.
2. **규칙 개정 확정: CONCERNS + HIGH ≥1이면 패치 1회 후 마감.** 두 라운드 모두 judge가 "지적이 닫혔는가"를 가장 무겁게 봤다. 선행 조건으로 넘길 수 있는 것은 MEDIUM 이하만.
3. **남은 진짜 문제는 범위.** A0 해석 목록이 규모·경로 같은 확장 방향의 해석을 "가정"으로 확정하게 만든다. 해석 목록에 "시드에 없는 규모·인프라를 세우는 해석은 축소 쪽을 기본값으로, 확장은 질문 1칸으로" 규칙이 필요하다. 사용 맥락 축(11)과 같은 취지.
4. judge의 단서: 양쪽 다 패치 뒤 재검토가 없어 "반영됐다"는 주장이 검증되지 않았다. 재검토 1회 예산을 고려할 것.

## 다음

- SKILL.md 규칙 개정(위 2·3) — 1.4.3. 적용 뒤 회귀는 lite 시드 1개 + check_package로만(PROTOCOL 축소판).
- 판정 비용 축소: judge 입력을 03·05·08로 제한(회당 20% → 약 8%). PROTOCOL에 "경량 판정" 절 추가.
- 효과 미측정: `effort: high`(1.4.2)는 이 런에 적용되지 않았다(생성은 xhigh 세션에서).

## 파일

- `s4-a.md`(0907 복사) · `s4-b.md`(1.4.1 생성, `s4-rpi-ip-camera/` 병합) · `s4-doc{1,2}.md` · `s4-judge-{1,2}.md` · `s4-eval-{1,2}.md` · `mapping.md`
- `s8-a.md`(0907 복사) · `s8-elder-fall-alert/`(A5까지 + `_a4-review.md` + REVISIONS R1, 일시 중지)
- `RUNBOOK.md` · `GEN-PROMPT.md` · `GEN-PROMPT-S8.md` · `assemble.py`
