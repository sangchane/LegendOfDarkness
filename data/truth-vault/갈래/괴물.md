---
이름: "괴물"
갈래: 자료출처
---

# 괴물

## 하데스에 있나

- `templates/monsters/insight_1` · `minions` — **3마리**(bees · spider · minion). 그게 전부다
- **메타파일에 없다.** 원본 클라이언트는 괴물 수치를 받지 않는다
- **원작 아카이브에도 없다.** `Legend.dat`의 `MobTile.tbl` 은 프레임 수, `mns###.pal` 은 색표다
- `DADataViewer/MonstersForm` 도 그림만 읽는다 (`mpf` · `mns###.pal`)
- **수치는 식이 만든다** — `scripts/Creations/monsters.cs` 가 `Level` 에서 체력·방어·능력치를, `Formulas/damage.cs` 가 공격력을, `Formulas/monsterexp.cs` 가 경험치를 만든다

## 원작에서 뽑아 둔 것

- 없다. 원작 쪽에 괴물 수치 자료가 존재하지 않는다

## 지금 서버에 실린 것

- `templates/monsters/` **165장** (하데스 3 + 우리가 넣은 164)
- **수치는 비어 있다** — `MaximumHP: 0` 이라 하데스 식이 레벨에서 만든다. 5.99 수치를 넣었다가 되돌렸다(`444b7f5`)

## 팩 (규칙 3번 — 둘이 일치할 때만 후보)

- 쪽수 {'5.99-server': 242, 'honden-community': 821} · 이름 겹침 35 · 검사한 칸이 **전부 일치 0**
- 칸별: 경험치 1 · 경험치 ✗ 30 · 방어력 4 · 방어력 ✗ 31 · 이미지 30 · 이미지 ✗ 5 · 체력 ✗ 35 · 최대공격력 2 · 최대공격력 ✗ 29 · 최소공격력 ✗ 31
- 
- **사냥터별로 본 것** (쓸 수 있는 수치가 있는지):
- **우드랜드** — 괴물 25종 · **뿌리가 둘 다에 있는 것(실재) 14종**: 늑대1, 늑대2, 늑대3, 늑대4, 늑대인간1, 늑대인간2, 맨티스1, 맨티스2, 맨티스3, 맨티스4, 맨티스5, 뱀1, 뱀2, 브라운맨티스1
- **포테의숲** — 괴물 11종 · **뿌리가 둘 다에 있는 것(실재) 8종**: 그린팜팻, 놀, 레드팜팻, 실버팜팻, 엔트라이온, 엔트자이언트, 은빛늑대, 퍼플팜팻
- **노비스** — 괴물 13종 · **뿌리가 둘 다에 있는 것(실재) 2종**: 독거미, 브라운맨티스
- 
- **2026-09-13, 사람의 판정** (팩이 어긋나므로 이것이 정한다):
- - **둘 다에 있어야 실재다.** 뿌리가 둘 다에 있는 것 **56뿌리**만 쓴다. 혼든에만 있는 734종과 5.99 에만 있는 147종은 **창작**이다 — 체력이 150만을 넘는 괴물이 혼든에 230종 있고 최대가 99,999,999,999,999 이다
- - **5.99 가 뻥튀기됐다.** 경험치는 혼든이 원작에 가깝다 — 겹치는 것의 비율 중간값 5.0배, 최대 31.0배(`그린팜팻` 56,000 대 2,220). 1레벨의 다음 레벨까지가 600 이니 5.99 는 한 마리에 여섯 레벨이 오른다
- - 방어력·공격력은 유저가 알 수 없는 값이라 사람이 판정할 수 없다. 그림 번호는 겹치는 것에서 대부분 일치하므로 **신원은 믿을 수 있고 수치만 못 믿는다**

## 판정

하데스에 데이터가 없다(규칙 1·2번 해당 없음). 팩은 **값이 하나도 일치하지 않는다** — 이름이 그대로 겹치는 35마리조차 검사한 칸 전부 일치가 0개다. 그래서 규칙 3번만으로는 쓸 값이 없고, **규칙 0번(사람의 판정)이 정한다**: 뿌리가 둘 다에 있는 것만 실재로 보고, 어긋나는 값은 혼든 쪽을 쓴다(5.99 가 뻥튀기됐다). 수치를 아직 넣지 않았으므로 지금 서버는 하데스 식이 레벨에서 만든 값으로 돈다

## 어디를 보나

- `data/formula-vault/` — 식이 레벨에서 무엇을 만드는지
