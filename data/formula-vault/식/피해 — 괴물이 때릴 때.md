---
이름: "피해 — 괴물이 때릴 때"
갈래: 식
근거수: 11
출처: "database/server/scripts/Formulas/damage.cs"
---

# 피해 — 괴물이 때릴 때

`database/server/scripts/Formulas/damage.cs` 에서 11줄.

```csharp
database/server/scripts/Formulas/damage.cs:29  return Math.Max(1, Generator.Random.Next(Math.Min(least, most), Math.Max(least, most) + 1));
database/server/scripts/Formulas/damage.cs:34  var mod = 0.0;
database/server/scripts/Formulas/damage.cs:35  var diff = 0;
database/server/scripts/Formulas/damage.cs:38  diff = obj.Level + 1 - aisling.ExpLevel;
database/server/scripts/Formulas/damage.cs:41  diff = obj.Level + 1 - monster.Template.Level;
database/server/scripts/Formulas/damage.cs:43  if (diff <= 0)
database/server/scripts/Formulas/damage.cs:44  mod = obj.Level * (type == MonsterDamageType.Physical ? 0.1 : 2) * ServerContext.Config.BaseDamageMod;
database/server/scripts/Formulas/damage.cs:46  mod = obj.Level * (type == MonsterDamageType.Physical ? 0.1 : 2) * (ServerContext.Config.BaseDamageMod * diff);
database/server/scripts/Formulas/damage.cs:48  var dmg = Math.Abs((int)(mod + 1));
database/server/scripts/Formulas/damage.cs:53  return dmg;
database/server/scripts/Formulas/damage.cs:56  return 1;
```

## 읽는 설정

이 식이 쓰는 값은 [[설정/LoruleConfig|LoruleConfig]] 에 있다.
