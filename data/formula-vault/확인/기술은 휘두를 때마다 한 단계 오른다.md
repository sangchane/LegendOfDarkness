---
이름: "기술은 휘두를 때마다 한 단계 오른다"
갈래: 확인
근거수: 7
지키는시험: "tests/hades-characterization/CombatSmokeTests.cs (LevelOnUse)"
---

# 기술은 휘두를 때마다 한 단계 오른다

`toImprove = (int)(0.10 / LevelRate)` 가 **0** 이 되어 `Uses++ >= 0` 이 언제나 참이다. 백 번 써야 오르도록 만든 값이 한 번마다 오르게 만든다.

0.10 / 0.5 = 0.2 이고 `(int)` 가 0 으로 깎는다. 그래서 Assail 은 첫 휘두름에 2수준이 되고, k번째 휘두름의 수준은 1+k 다(MaxLevel 100 에서 멎는다).

수준은 피해에 들어간다 — `imp = 10 + 수준`. 그래서 **연속으로 휘두르면 매번 더 아프다**. 예측값을 맞춰 보려면 몇 번째 휘두름인지를 알아야 한다.

서버는 오를 때마다 `"Assail has improved. (Lv. N)"` 이라고 말한다. 그 말이 유일한 표시다.

## 근거

`src/Hades.Server.Base/Network/Game/GameClient.cs`

```csharp
src/Hades.Server.Base/Network/Game/GameClient.cs:1232  var toImprove = (int) (0.10 / skill.Template.LevelRate);
src/Hades.Server.Base/Network/Game/GameClient.cs:1233  if (skill.Uses++ >= toImprove)
src/Hades.Server.Base/Network/Game/GameClient.cs:1235  skill.Level++;
src/Hades.Server.Base/Network/Game/GameClient.cs:1254  var toImprove = (int) (0.10 / spell.Template.LevelRate);
src/Hades.Server.Base/Network/Game/GameClient.cs:1255  if (spell.Casts++ >= toImprove)
```

`database/server/templates/skills/Assail.json`

```csharp
database/server/templates/skills/Assail.json:4  "LevelRate": 0.5,
database/server/templates/skills/Assail.json:6  "MaxLevel": 100,
```

## 이걸 지키는 시험

`tests/hades-characterization/CombatSmokeTests.cs (LevelOnUse)`

식대로인지는 눈으로 못 본다. 그래서 값을 먼저 계산하고 서버에 실제로 때려 본다 —
"체력이 좀 깎였다" 로는 한 대에 1% 가 깎이든 90% 가 깎이든 똑같이 통과한다.
