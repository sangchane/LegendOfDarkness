---
이름: "체력·마력은 21초마다 능력치로 찬다"
갈래: 확인
근거수: 8
지키는시험: "tests/hades-characterization/Pack599RecoveryTests.cs"
---

# 체력·마력은 21초마다 능력치로 찬다

5.99 식이다(`0x46d1a5`, 타이머 `0x4766aa` 의 21000ms). 한 번에 최대 ÷ 100 × 지구력(마력은 지혜) ÷ 4.3 을 채우고, 최대의 15% 아래면 15%, 25% 위거나 능력치 108 이상이면 25%.

**전에는 5초마다 최대의 10~20%** 를 채웠다(분당 120~240%). 25레벨·지구력 65·체력 3083 이면 5초에 622 — 괴물 하나가 치는 것보다 빨리 차서 죽지 않았다. 5.99 는 21초에 466 이다.

능력치는 장비를 뺀 것(캐릭터 칸 +163 지구력 · +162 지혜)이고, 장비의 회복 칸 합(+152)을 그대로 더한다 — 하데스에서 그 자리는 `Regen` 이다(지금 그 칸을 가진 아이템은 없다).

5.99 가 더 보는 것 셋은 옮기지 않았다: 배고픔 0 이면 안 참(하데스에 배고픔이 없다), 혼수 모습이면 안 참, 캐릭터 칸 +98 이 켜져 있으면 1.5배(무엇인지 모른다).

## 근거

`src/Hades.Server.Base/Network/Game/GameClient.cs`

```csharp
src/Hades.Server.Base/Network/Game/GameClient.cs:769  public static int NaturalRecovery(int maximum, int attribute)
src/Hades.Server.Base/Network/Game/GameClient.cs:771  var hundredth = (int) (maximum / 100.0);
src/Hades.Server.Base/Network/Game/GameClient.cs:774  if (amount < hundredth * 15)
src/Hades.Server.Base/Network/Game/GameClient.cs:775  amount = hundredth * 15;
src/Hades.Server.Base/Network/Game/GameClient.cs:777  if (amount > hundredth * 25 || attribute >= 108)
src/Hades.Server.Base/Network/Game/GameClient.cs:778  amount = hundredth * 25;
src/Hades.Server.Base/Network/Game/GameClient.cs:805  Aisling.CurrentHp = (Aisling.CurrentHp + NaturalRecovery(Aisling.MaximumHp, Aisling._Con) + Aisling.Regen)
src/Hades.Server.Base/Network/Game/GameClient.cs:809  Aisling.CurrentMp = (Aisling.CurrentMp + NaturalRecovery(Aisling.MaximumMp, Aisling._Wis) + Aisling.Regen)
```

## 이걸 지키는 시험

`tests/hades-characterization/Pack599RecoveryTests.cs`

식대로인지는 눈으로 못 본다. 그래서 값을 먼저 계산하고 서버에 실제로 때려 본다 —
"체력이 좀 깎였다" 로는 한 대에 1% 가 깎이든 90% 가 깎이든 똑같이 통과한다.
