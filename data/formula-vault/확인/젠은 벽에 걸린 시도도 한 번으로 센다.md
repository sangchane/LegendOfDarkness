---
이름: "젠은 벽에 걸린 시도도 한 번으로 센다"
갈래: 확인
근거수: 11
지키는시험: "tests/hades-characterization/CombatSmokeTests.cs (MonsterRoom · AnyMonster)"
---

# 젠은 벽에 걸린 시도도 한 번으로 센다

자리를 무작위로 골라 벽이면 아무것도 안 세우는데, 그 정의는 이미 `SpawnRate` 만큼 쉬러 들어간다.

`ReadyToSpawn()` 이 **먼저** 다음 시각을 20초 뒤로 밀고 참을 돌려준다. 그 뒤 `FindBestMonsterMapSlot` 이 벽을 골라 `Create` 가 null 을 내면 그 20초는 그냥 날아간다.

그래서 정의가 둘뿐인 방은 1분에 여섯 번만 굴리고, 집 안은 대부분 벽이라 몇 분을 기다려도 빈 방일 수 있다. 시험이 `지하수로D-2`(정의 7개, 20×20)를 쓰는 이유가 이것이다.

그리고 훑는 고리가 `continue` 대신 **`return`** 이다 — 크기가 0 인 맵 하나를 만나면 그 뒤 모든 맵의 젠이 그 회차에 멎는다.

## 근거

`src/Hades.Server.Base/Templates/MonsterTemplate.cs`

```csharp
src/Hades.Server.Base/Templates/MonsterTemplate.cs:85  [JsonIgnore] public DateTime NextAvailableSpawn { get; set; }
src/Hades.Server.Base/Templates/MonsterTemplate.cs:91  [JsonIgnore] public bool Ready => DateTime.UtcNow > NextAvailableSpawn;
src/Hades.Server.Base/Templates/MonsterTemplate.cs:135  public bool ReadyToSpawn(double seconds)
src/Hades.Server.Base/Templates/MonsterTemplate.cs:137  if (!Ready)
src/Hades.Server.Base/Templates/MonsterTemplate.cs:140  NextAvailableSpawn = DateTime.UtcNow.AddSeconds(seconds);
```

`src/Hades.Server.Base/Network/Game/Components/MonolithComponent.cs`

```csharp
src/Hades.Server.Base/Network/Game/Components/MonolithComponent.cs:45  return;
src/Hades.Server.Base/Network/Game/Components/MonolithComponent.cs:51  if (map == null || map.Rows == 0 || map.Cols == 0)
src/Hades.Server.Base/Network/Game/Components/MonolithComponent.cs:69  if (!template.ReadyToSpawn(template.SpawnRate / (double) spread))
src/Hades.Server.Base/Network/Game/Components/MonolithComponent.cs:75  var most = (int) Math.Round(template.SpawnMax * Math.Sqrt(spread));
```

`database/server/scripts/Creations/monsters.cs`

```csharp
database/server/scripts/Creations/monsters.cs:50  case SpawnQualifer.Random:
database/server/scripts/Creations/monsters.cs:67  if (map.IsWall(x, y))
```

## 이걸 지키는 시험

`tests/hades-characterization/CombatSmokeTests.cs (MonsterRoom · AnyMonster)`

식대로인지는 눈으로 못 본다. 그래서 값을 먼저 계산하고 서버에 실제로 때려 본다 —
"체력이 좀 깎였다" 로는 한 대에 1% 가 깎이든 90% 가 깎이든 똑같이 통과한다.
