---
이름: "스크립트 하나가 어긋나면 전부 사라지고, 서버는 아무 일 없는 듯 뜬다"
갈래: 확인
근거수: 8
지키는시험: "tests/hades-characterization/IsolatedHadesServer.cs (RequireCompiledScripts)"
---

# 스크립트 하나가 어긋나면 전부 사라지고, 서버는 아무 일 없는 듯 뜬다

`scripts/` 아래 C# 은 **한 덩어리로** 컴파일된다. 한 곳이 안 되면 115장이 다 사라지는데 서버는 그대로 켜지고 포트를 연다 — 괴물도 안 서고 기술도 안 돌지만 로그 한 줄만 남는다.

실제로 겪었다. `damage.cs` 에 쓴 변수 이름이 아래쪽 `target is Monster monster` 와 겹쳐(CS0136) 스크립트가 0개가 됐고, 서버는 정상으로 보였다. 시험은 '괴물이 안 선다' 며 2분을 기다린 뒤 엉뚱한 것을 신고했다.

찍히는 줄은 `Scripts Loaded and Compiled: 0` 하나다. 그래서 격리 서버 하네스가 그 줄을 보고 **바로 멈추고 컴파일러가 한 말을 보여준다** — 같은 실수가 2분이 아니라 14초에 드러났다.

`tools/` 나 원본 게임 파일이 옆에 있으면 관리 어셈블리가 아닌 DLL 도 후보로 들어간다. `AssemblyName.GetAssemblyName` 으로 걸러 내는 코드가 이미 있다 — 같은 함정이 전에도 있었다.

## 근거

`src/Hades.Server.Base/Scripting/ScriptManager.cs`

```csharp
src/Hades.Server.Base/Scripting/ScriptManager.cs:83  ServerContext.Logger("Compiling all scripts...");
src/Hades.Server.Base/Scripting/ScriptManager.cs:89  if (!result.Success)
src/Hades.Server.Base/Scripting/ScriptManager.cs:91  IEnumerable<Diagnostic> failures = result.Diagnostics.Where(diagnostic =>
src/Hades.Server.Base/Scripting/ScriptManager.cs:93  diagnostic.Severity == DiagnosticSeverity.Error);
src/Hades.Server.Base/Scripting/ScriptManager.cs:95  foreach (Diagnostic diagnostic in failures)
src/Hades.Server.Base/Scripting/ScriptManager.cs:105  LoadFromAssembly(compiled_assembly);
src/Hades.Server.Base/Scripting/ScriptManager.cs:109  ServerContext.Logger("Compiling all scripts... completed.");
src/Hades.Server.Base/Scripting/ScriptManager.cs:112  public static void LoadFromAssembly(Assembly assembly)
```

## 이걸 지키는 시험

`tests/hades-characterization/IsolatedHadesServer.cs (RequireCompiledScripts)`

식대로인지는 눈으로 못 본다. 그래서 값을 먼저 계산하고 서버에 실제로 때려 본다 —
"체력이 좀 깎였다" 로는 한 대에 1% 가 깎이든 90% 가 깎이든 똑같이 통과한다.
