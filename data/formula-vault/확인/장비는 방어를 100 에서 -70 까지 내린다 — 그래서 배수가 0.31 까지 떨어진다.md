---
이름: "장비는 방어를 100 에서 -70 까지 내린다 — 그래서 배수가 0.31 까지 떨어진다"
갈래: 확인
근거수: 9
지키는시험: "(아직 지키는 시험이 없다 — 장비를 갖춘 캐릭터로 재는 시험이 없다)"
---

# 장비는 방어를 100 에서 -70 까지 내린다 — 그래서 배수가 0.31 까지 떨어진다

맨몸은 `100 - 수준/3` 이라 1수준이 +100 이고 피해를 2.03배로 받는다. 장비가 그것을 내리는 유일한 수단이고, 실을 것은 이미 실려 있다.

아이템의 `AcModifer` 가 `Operator.Remove` 면 걸칠 때 `BonusAc` 를 그만큼 **내린다**. 자리별 최고를 다 갖추면 -245 가 되고 `Sprite.Ac` 의 바닥 -70 에 걸린다 → 배수 `(Ac+101)/99` 가 **0.31**. 맨몸 2.03배와 6.5배 차이다.

**착용은 조건을 검사한다.** `GameClient.CheckReqs` 가 요구 레벨·직업·성별·내구도를 본다(GM 과 Developer 는 통과). 그래서 상위 장비를 끼려면 수준과 직업을 맞춰야 한다 — 1수준 무직이 낄 수 있는 것은 착용 가능한 757개 중 293개이고, 그것만으로도 방어가 100 → **7** 까지 내려간다.

적용 순서가 맞게 되어 있다. 로그인 때 `SetAislingStartupVariables` 가 `BonusAc` 를 먼저 정하고 그 **뒤에** `LoadEquipment` 가 장비 보정을 얹는다 — 순서가 반대면 장비가 지워질 것이다.

## 근거

`src/Hades.Server.Base/Types/Item.cs`

```csharp
src/Hades.Server.Base/Types/Item.cs:111  obj.Template.AcModifer.Value -= -obj.Upgrades;
src/Hades.Server.Base/Types/Item.cs:258  client.Aisling.BonusAc += Template.AcModifer.Value;
src/Hades.Server.Base/Types/Item.cs:260  client.Aisling.BonusAc -= Template.AcModifer.Value;
src/Hades.Server.Base/Types/Item.cs:624  client.Aisling.BonusAc -= Template.AcModifer.Value;
src/Hades.Server.Base/Types/Item.cs:628  client.Aisling.BonusAc += Template.AcModifer.Value;
```

`src/Hades.Server.Base/Types/Sprite.cs`

```csharp
src/Hades.Server.Base/Types/Sprite.cs:137  public int Ac
src/Hades.Server.Base/Types/Sprite.cs:141  if (BonusAc < -70)
```

`src/Hades.Server.Base/Network/Game/GameClient.cs`

```csharp
src/Hades.Server.Base/Network/Game/GameClient.cs:137  if (client.Aisling.ExpLevel < item.Template.LevelRequired)
src/Hades.Server.Base/Network/Game/GameClient.cs:1011  Aisling.BonusAc = (100 - Aisling.Level / 3);
```

## 이걸 지키는 시험

`(아직 지키는 시험이 없다 — 장비를 갖춘 캐릭터로 재는 시험이 없다)`

식대로인지는 눈으로 못 본다. 그래서 값을 먼저 계산하고 서버에 실제로 때려 본다 —
"체력이 좀 깎였다" 로는 한 대에 1% 가 깎이든 90% 가 깎이든 똑같이 통과한다.
