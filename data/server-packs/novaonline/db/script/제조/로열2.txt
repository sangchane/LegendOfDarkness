0,0,0,0,0,0,0	로열2	{
		
		mes 1, "리치의피로 로열복을 만들어드립니다.";

		set @select, menu("제작할것을 선택하세요", "로열복(남전사)", "로열복(여전사)", "로열복(남도적)", "로열복(여도적)", "로열복(남법사)", "로열복(여법사)", "로열복(남직자)", "로열복(여직자)", "로열복(남도가)", "로열복(여도가)");

		if(@select == 1)
		{
		mes 1, "로열복(남전사)를 제작합니다";
		mes 1, "{=c리치의피,금20개,철20개";
		mes 1, "재료를 확인중입니다";
		set @myid, get_myid();
if(item_exist(@myid, "리치의피") >= 1 && item_exist(@myid, "금") >= 20 && item_exist(@myid, "철") >=20){

			item_del "리치의피", 1;
			item_del "금", 20;
			item_del "철", 20;
			mes 1, "제작중";
			mes 1, "제작을 성공하셨습니다!!";
			item_add "로열가드복(남전사)", 1;
 		legend_add 1, 255, "리치 1년, 봄 로열복(남전사)를 만들다!!";
		}
		else
		{
			mes 1, "{=q 재료가 부족하네요";
		}

	}		if(@select == 2)
		{
		mes 1, "로열복(여전사)를 제작합니다";
		mes 1, "{=c리치의피,금20개,철20개";
		mes 1, "재료를 확인중입니다";
		set @myid, get_myid();
if(item_exist(@myid, "리치의피") >= 1 && item_exist(@myid, "금") >= 20 && item_exist(@myid, "철") >=20){

			item_del "리치의피", 1;
			item_del "금", 20;
			item_del "철", 20;
			mes 1, "제작중";
			mes 1, "제작을 성공하셨습니다!!";
			item_add "로열가드복(여전사)", 1;
 		legend_add 1, 255, "리치 1년, 봄 로열복(여전사)를 만들다!!";
		}
		else
		{
			mes 1, "{=q 재료가 부족하네요";
		}

	}		if(@select == 3)
		{
		mes 1, "로열복(남도적)를 제작합니다";
		mes 1, "{=c리치의피,금20개,철20개";
		mes 1, "재료를 확인중입니다";
		set @myid, get_myid();
if(item_exist(@myid, "리치의피") >= 1 && item_exist(@myid, "금") >= 20 && item_exist(@myid, "철") >=20){

			item_del "리치의피", 1;
			item_del "금", 20;
			item_del "철", 20;
			mes 1, "제작중";
			mes 1, "제작을 성공하셨습니다!!";
			item_add "로열가드복(남도적)", 1;
 		legend_add 1, 255, "리치 1년, 봄 로열복(남도적)을 만들다!!";
		}
		else
		{
			mes 1, "{=q 재료가 부족하네요";
		}

	}		if(@select == 4)
		{
		mes 1, "로열복(여도적)를 제작합니다";
		mes 1, "{=c리치의피,금20개,철20개";
		mes 1, "재료를 확인중입니다";
		set @myid, get_myid();
if(item_exist(@myid, "리치의피") >= 1 && item_exist(@myid, "금") >= 20 && item_exist(@myid, "철") >=20){

			item_del "리치의피", 1;
			item_del "금", 20;
			item_del "철", 20;
			mes 1, "제작중";
			mes 1, "제작을 성공하셨습니다!!";
			item_add "로열가드복(여도적)", 1;
 		legend_add 1, 255, "리치 1년, 봄 로열복(여도적) 만들다!!";
		}
		else
		{
			mes 1, "{=q 재료가 부족하네요";
		}

	}		if(@select == 5)
		{
		mes 1, "로열복(남법사)를 제작합니다";
		mes 1, "{=c리치의피,금20개,철20개";
		mes 1, "재료를 확인중입니다";
		set @myid, get_myid();
if(item_exist(@myid, "리치의피") >= 1 && item_exist(@myid, "금") >= 20 && item_exist(@myid, "철") >=20){

			item_del "리치의피", 1;
			item_del "금", 20;
			item_del "철", 20;
			mes 1, "제작중";
			mes 1, "제작을 성공하셨습니다!!";
			item_add "로열가드복(남법사)", 1;
 		legend_add 1, 255, "리치 1년, 봄 로열복(남법사)를 만들다!!";
		}
		else
		{
			mes 1, "{=q 재료가 부족하네요";
		}

	}		if(@select == 6)
		{
		mes 1, "로열복(남전사)를 제작합니다";
		mes 1, "{=c리치의피,금20개,철20개";
		mes 1, "재료를 확인중입니다";
		set @myid, get_myid();
if(item_exist(@myid, "리치의피") >= 1 && item_exist(@myid, "금") >= 20 && item_exist(@myid, "철") >=20){

			item_del "리치의피", 1;
			item_del "금", 20;
			item_del "철", 20;
			mes 1, "제작중";
			mes 1, "제작을 성공하셨습니다!!";
			item_add "로열가드복(여법사)", 1;
 		legend_add 1, 255, "리치 1년, 봄 로열복(여법사)를 만들다!!";
		}
		else
		{
			mes 1, "{=q 재료가 부족하네요";
		}

	}		if(@select == 7)
		{
		mes 1, "로열복(남직자)를 제작합니다";
		mes 1, "{=c리치의피,금20개,철20개";
		mes 1, "재료를 확인중입니다";
		set @myid, get_myid();
if(item_exist(@myid, "리치의피") >= 1 && item_exist(@myid, "금") >= 20 && item_exist(@myid, "철") >=20){

			item_del "리치의피", 1;
			item_del "금", 20;
			item_del "철", 20;
			mes 1, "제작중";
			mes 1, "제작을 성공하셨습니다!!";
			item_add "로열가드복(남직자)", 1;
 		legend_add 1, 255, "리치 1년, 봄 로열복(남직자)를 만들다!!";
		}
		else
		{
			mes 1, "{=q 재료가 부족하네요";
		}

	}		if(@select == 8)
		{
		mes 1, "로열복(여직자)를 제작합니다";
		mes 1, "{=c리치의피,금20개,철20개";
		mes 1, "재료를 확인중입니다";
		set @myid, get_myid();
if(item_exist(@myid, "리치의피") >= 1 && item_exist(@myid, "금") >= 20 && item_exist(@myid, "철") >=20){

			item_del "리치의피", 1;
			item_del "금", 20;
			item_del "철", 20;
			mes 1, "제작중";
			mes 1, "제작을 성공하셨습니다!!";
			item_add "로열가드복(여직자)", 1;
 		legend_add 1, 255, "리치 1년, 봄 로열복(여직자)를 만들다!!";
		}
		else
		{
			mes 1, "{=q 재료가 부족하네요";
		}

	}		if(@select == 9)
		{
		mes 1, "로열복(남도가)를 제작합니다";
		mes 1, "{=c리치의피,금20개,철20개";
		mes 1, "재료를 확인중입니다";
		set @myid, get_myid();
if(item_exist(@myid, "리치의피") >= 1 && item_exist(@myid, "금") >= 20 && item_exist(@myid, "철") >=20){

			item_del "리치의피", 1;
			item_del "금", 20;
			item_del "철", 20;
			mes 1, "제작중";
			mes 1, "제작을 성공하셨습니다!!";
			item_add "로열가드복(남도가)", 1;
 		legend_add 1, 255, "리치 1년, 봄 로열복(남도가)를 만들다!!";
		}
		else
		{
			mes 1, "{=q 재료가 부족하네요";
		}

	}		if(@select == 10)
		{
		mes 1, "로열복(여도가)를 제작합니다";
		mes 1, "{=c리치의피,금20개,철20개";
		mes 1, "재료를 확인중입니다";
		set @myid, get_myid();
if(item_exist(@myid, "리치의피") >= 1 && item_exist(@myid, "금") >= 20 && item_exist(@myid, "철") >=20){

			item_del "리치의피", 1;
			item_del "금", 20;
			item_del "철", 20;
			mes 1, "제작중";
			mes 1, "제작을 성공하셨습니다!!";
			item_add "로열가드복(여도가)", 1;
 		legend_add 1, 255, "리치 1년, 봄 로열복(여도가)를 만들다!!";
		}
		else
		{
			mes 1, "{=q 재료가 부족하네요";
		}

	}
}
	del @select;
}
