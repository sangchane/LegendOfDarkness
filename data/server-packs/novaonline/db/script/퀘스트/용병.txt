0,0,0,0,0,0,0	용병가입	{
	set @myid, get_myid();
 	mes 1, "안녕하십니까. 용병 길드의 인사부마스터 레일입니다.";
 	mes 1, "용병이 되고싶습니까? 그렇다면, 실력을 증명해보이십시오.";
 	mes 1, "기본적인 용병등급은 C,B,A,S로 구별됩니다.";
 	mes 1, "자신의 용병전용 퀘스트와 일정 실력이된다면, 등급을 올릴수 있습니다.";
 	mes 1, "많은 보상과, 많은 경험을 원하신다면, 용병이 되십시오.";
	set @select, menu("무엇을 하겠습니까?", "용병가입", "B등급업", "A등급업", "S등급업");
	if(@select == 1)
	{
	if(pc_itemcheck("C등급용병")){
	mes 1, "당신은 이미 용병이십니다.";
	}
	else
	{
	if(pc_itemcheck("B등급용병")){
	mes 1, "당신은 이미 용병이십니다.";
	}
	else
	{
	if(pc_itemcheck("A등급용병")){
	mes 1, "당신은 이미 용병이십니다.";
	}
	else
	{
	if(pc_itemcheck("S등급용병")){
	mes 1, "당신은 이미 용병이십니다.";
	}
	else
	{
	mes 1, "C등급용병이 되셧습니다.";
	item_add "C등급용병", 1;
	}
	}
}
}
	}if(@select == 2)
	{
	mes 1, "B등급용병이 되실려면 체력과 마력이 100만이상이셔야 합니다.";
	if(pc_itemcheck("C등급용병") == 0x01 && get_basevita() >= 1000000 && get_basemana() >= 1000000){
	mes 1, "B등급용병이 되셧습니다.";
	item_add "B등급용병", 1;
	item_del "C등급용병", 1;
	}
	else
	{
	mes 1, "당신은 C등급용병이 아니거나, 체력과마력이 아직 낮습니다.";
	}
	}if(@select == 3)
	{
	mes 1, "A등급용병이 되실려면 체력과 마력이 300만이상이셔야 합니다.";
	if(pc_itemcheck("B등급용병") == 0x01 && get_basevita() >= 3000000 && get_basemana() >= 3000000){
	mes 1, "A등급용병이 되셧습니다.";
	item_add "A등급용병", 1;
	item_del "B등급용병", 1;
	}
	else
	{
	mes 1, "당신은 B등급용병이 아니거나, 체력과마력이 아직 낮습니다.";
	}
	}if(@select == 4)
	{
	mes 1, "S등급용병이 되실려면 체력과 마력이 500만이상이셔야 합니다.";
	if(pc_itemcheck("A등급용병") == 0x01 && get_basevita() >= 5000000 && get_basemana() >= 5000000){
	mes 1, "S등급용병이 되셧습니다.";
	item_add "S등급용병", 1;
	item_del "A등급용병", 1;
	}
	else
	{
	mes 1, "당신은 A등급용병이 아니거나, 체력과마력이 아직 낮습니다.";
	}
	}
	}
	}
	}
	}
 del @val;
 }
 del @select;

0,0,0,0,0,0,0	용병퀘스트	{
	set @myid, get_myid();
 	mes 1, "안녕하십니까. 용병퀘스트를 지급해드립니다.";
 	mes 1, "용병퀘스트는 자기등급에맞는 퀘스트만 실행가능합니다.";
	set @select, menu("어떤퀘스트를 받겠습니까?", "[C급]도둑잡기", "[C급]고대의조각");
	if(@select == 1){
	if(pc_itemcheck("도둑의물품") == 0x01 && pc_itemcheck("C등급용병") == 0x01){
	mes 1, "도둑의물품을 가져오셧군요, 의뢰자에게 고대의조각을 전달하겠습니다.";
	item_del "도둑의물품", 1;
   	money_add "4000000";
	}else{
	if(pc_itemcheck("C등급용병") == 0x01){
	mes 1, "C등급용병용 퀘스트입니다. 이퀘스트는 무한반복이가능합니다.";
	mes 1, "의뢰내용[요즘 늘어난 도둑때문에 마이소시아 주민들이 고민이다. 도둑들의 은신처는 루어스 마을이라 알려져있으니, 루어스마을에가 도둑을잡고, 도둑의물품을 가져오자!]";
	mes 1, "보상은 골드 4백만골드이라네. 등급이 올를수록 보상은 무지좋아진다는것을 잊지말게나!";
	}else{
	mes 1, "당신은 용병이아니거나C등급이 아닙니다.";
	end;
	}
	}
	}
	if(@select == 2){
	if(pc_itemcheck("고대의조각") == 0x01 && pc_itemcheck("C등급용병") == 0x01 && get_ac(@myid) == 100){
	mes 1, "고대의 조각을 가져오셧군요, 의뢰자에게 고대의조각을 전달하겠습니다.";
	mes 1, "의뢰자가 건 보상은, 체력과 마력 1백씩 상승이였습니다. 그리고 골드 2백골드입니다.";
	mes 1, "보상을 지급하겠습니다.";
	item_del "고대의조각", 1;
	set_basevita get_basevita(@myid) + 100;
	set_basemana get_basemana(@myid) + 100;
   	money_add "2000000";
	mes 1, "보상지급이 완료됬습니다.";
	}else{
	if(pc_itemcheck("C등급용병") == 0x01){
	mes 1, "C등급용병용 퀘스트입니다. 이퀘스트는 무한반복이가능합니다.";
	mes 1, "의뢰내용[마이소시아의 과거의 흔적이 나무에서 나왔다!, 나무를 잘 뒤져보면, 고대의 조각이 나올수도 있다!, 채집을 통해 고대의조각을 얻어오자.]";
	mes 1, "보상은 체력과마력 100씩상승, 2백만골드라네, 등급이 올를수록 보상은 무지좋아진다는것을 잊지말게나!";
	mes 1, "(이퀘스트를 완료할려면 무장해체를 하셔야합니다.)";
	}else{
	mes 1, "당신은 용병이아니거나c등급이 아닙니다.";
	end;
	}
	}
	}
	}
 del @val;
 del @myid;
 }
 del @select;