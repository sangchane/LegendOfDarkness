<!-- NEXT-ACTION:START -->
## ▶ 지금 할 일 (새 세션은 이 블록부터 — SessionStart 훅이 자동 주입)

- **[참고]** 격리 시험을 돌리기 전에 `database/server/aislings/` 를 비운다. 시험이 그 폴더가 비어 있기를 요구하는데, 손으로 만든 계정과 서버가 만드는 `.backup` 이 거기 쌓인다
- **[보류/자산]** **눈 맵은 지금 확인할 방법이 없다.** `tiles … 눈|snow` 로 눈 타일셋은 뽑힌다. 그런데 ① 참고 저장소 16개 어디에도 눈 맵을 그리는 구현이 없고(`dark-ages-ts` 는 `MapFlags.SnowTileset=128` 을 **선언만** 하고 쓰지 않으며, `da-lib` 에는 눈 관련 코드가 없다), ② 이 서버의 맵 넷은 **모두 눈 비트가 꺼져 있다**(Flags 106240 동일). 즉 맞춰 볼 정답도, 시험할 맵도 없다. 원본 클라이언트를 뜯거나 눈 맵을 하나 만들어야 진전된다
- **[다음/자산]** **벽은 그림이 아니라 속성이다 — `sotp.dat` 를 읽어야 한다.** 별도 벽 타일셋을 찾을 일이 아니었다. `da-lib/Definitions/Flags.cs` 의 `TileFlags`(`sotp.dat` 용): `None=0` · **`Wall=15`** · `Transparent=128`. 이 저장소의 `database/server/static/sotp.dat` 는 20,423바이트이고 값이 정확히 그 넷뿐이다(15가 15,782 · 0이 4,265 · 128이 322 · 143(벽+투명)이 54). 서버는 이미 이 파일을 읽는다(`Types/Area.cs`). **남은 일**: 도구가 이 플래그를 읽어 벽을 가려 그리거나 표시하게 하는 것, 그리고 20,423이 `TILEA`(19,243)와도 `TILEA+TILEAS`(22,048)와도 안 맞는 이유 확인

<!-- NEXT-ACTION:END -->

<!--
규칙:
- 이 마커 사이는 "지금/다음 할 일" 1~3건만. 짧게(화면 한 판).
- 완료된 항목은 여기 두지 말고 WORKLOG.md 의 ## History 로 옮긴다 (단일 출처·비대 방지).
- 훅(print_next_action.py)은 이 마커 사이만 세션에 주입한다.
-->
