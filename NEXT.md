<!-- NEXT-ACTION:START -->
## ▶ 지금 할 일 (새 세션은 이 블록부터 — SessionStart 훅이 자동 주입)

- **[지금/이식]** **`plans/server-pack-content-port.md` 7단계 — 괴물 배치 565.** 0·1·3·4·5·6 은 끝났다(맵 801 · 워프 890 · 아이템 992, 오류 0). 조심할 것 셋: 젠 자료에 **좌표가 없다**(`SpawnType` 을 Defined 로 두면 전부 (0,0)에 뭉친다 — Random 계열로 두고 `SpawnMax` 에 마리수), `레아로4` 는 **정의가 둘**(이미지 383 vs 815), `LootType` 에 Random 켜고 `Drops` 비우면 **잡을 때마다 예외**(`Formulas/monsterexp.cs:112`)

- **[주의/시험]** **격리 시험이 불안정해지기 시작했다.** 한 번은 6분 7초에 1개 실패, 다음은 2분 27초에 64개 통과. 시험마다 `database/server`(맵 801 + 14MB 바이너리 + 아이템 992)를 통째로 복사하고 적재한다. 콘텐츠가 더 늘면 대기 한도에 걸린다 — 복사를 줄이거나 한도를 올려야 한다

- **[보류/그림]** 지도 그림은 **바닥만** 그린다. 건물·상점은 맵 칸의 벽 두 칸과 `seo.dat` 안 맵별 `.hea`(34개, `000500.hea`=밀레스)에 있고 읽는 코드가 없다. 벽 번호(최대 3,996)가 `.hea` 를 가리키는지 별도 타일셋을 가리키는지부터 확인하면 통째로 뜯지 않아도 될 수 있다

- **[참고]** 그래프·볼트에 **먼저 물어라.** 한국어 문장 말고 **기호**로 — `graphify explain "shop1"`. 팩 자료는 `data/server-packs/vault/<팩>/`(Obsidian)

<!-- NEXT-ACTION:END -->

<!--
규칙:
- 이 마커 사이는 "지금/다음 할 일" 1~3건만. 짧게(화면 한 판).
- 완료된 항목은 여기 두지 말고 WORKLOG.md 의 ## History 로 옮긴다 (단일 출처·비대 방지).
- 훅(print_next_action.py)은 이 마커 사이만 세션에 주입한다.
-->
