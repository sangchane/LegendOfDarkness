<!-- NEXT-ACTION:START -->
## ▶ 지금 할 일 (새 세션은 이 블록부터 — SessionStart 훅이 자동 주입)

- **[지금/서버]** **macOS 에서 인증 전 연결이 정리되지 않는다.** 붙었다 끊는 것을 50번 하면 **50개 모두 `CLOSE_WAIT` 으로 남는다**(`ConnectionLifetimeTests`). 계획서는 Windows 에서 0개로 측정했다고 적혀 있으니 플랫폼 차이다 — 서버가 소켓을 닫지 않는 것이므로 지인 테스트 전에 봐야 한다. 잠금 수정 전후로 똑같이 실패하므로 그것과는 무관하다
- **[참고]** 격리 시험을 돌리기 전에 `database/server/aislings/` 를 비운다. 시험이 그 폴더가 비어 있기를 요구하는데, 손으로 만든 계정과 서버가 만드는 `.backup` 이 거기 쌓인다
- **[다음/클라이언트]** **서버 주소를 어떻게 줄지 정해야 한다.** 지금은 커밋되지 않는 `mobile/client/server.cfg`(우선순위: `--server` → `LOD_SERVER` → 파일 → 기본값)로 실기기가 붙는다 — 시험용으로는 되지만 사람이 쓸 물건은 아니다. 시안 3절이 `서버 목록 선택`을 범위 제외로 두고 있어 화면에 칸을 넣으려면 시안부터 고쳐야 한다
- **[다음/자산]** 벽 타일(`TILEAS.BMP`는 고정 크기 아님)
- **[다음/도구]** `tools/` 에는 시험 프로젝트가 없다. `IconPalettes.PaletteFor`(세 갈래 파싱)와 `icon` 명령의 칸 계산은 지금 눈으로만 확인했다 — 도구 시험 자리를 만들 때 여기부터 덮는다

<!-- NEXT-ACTION:END -->

<!--
규칙:
- 이 마커 사이는 "지금/다음 할 일" 1~3건만. 짧게(화면 한 판).
- 완료된 항목은 여기 두지 말고 WORKLOG.md 의 ## History 로 옮긴다 (단일 출처·비대 방지).
- 훅(print_next_action.py)은 이 마커 사이만 세션에 주입한다.
-->
