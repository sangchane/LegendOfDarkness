<!-- NEXT-ACTION:START -->
## ▶ 지금 할 일 (새 세션은 이 블록부터 — SessionStart 훅이 자동 주입)

- **[지금/P0-14~P0-17]** 서버 안정화 마무리 — 송신 큐, 원자적 저장, 기본 전투 정확성, 하드코딩 상수 설정화. **지인 테스트 전에 필요한 마지막 서버 작업**이다. 통신 쪽은 로그인·위치·주변 사람까지 붙었으니 여기서 한 번 서버를 마무리하는 게 순서에 맞다
- **[다음/통신]** 서버가 보내는 겉모습(`0x33`의 뒷부분: 머리·투구·갑옷·무기·색)을 읽어 사람마다 다른 옷을 입힌다. 지금은 다른 사람이 전부 같은 옷이다. 부위 번호 → EPF 파일 대응이 필요하다(`docs/original-sprite-animation.md` 7절)
- **[다음/통신]** `0x07`(물체 추가 — 괴물·상인·바닥 아이템). **이 서버의 안전 가옥에는 하나도 없어서 지금은 확인할 방법이 없다.** 괴물이 있는 맵을 띄우거나 템플릿을 넣은 뒤에 한다. 기록마다 길이가 다르고 종류 표시가 없어 그림 번호로 갈라야 한다(괴물 ≥ 0x4000, 아이템은 그 아래로 관측됨)
- **[다음/자산]** 벽 타일(`TILEAS.BMP`는 고정 크기 아님)과 아이템 아이콘. 지면·맵·캐릭터·괴물·사물은 끝났다. **`LOD_`의 복원본은 쓰지 않는다**
- **[다음/화면]** 대상 고르기·전투·인벤토리를 Godot에 올린다. 배치는 시안(`docs/ui/`)에 이미 있다
- **[대기/Mac]** 같은 커밋을 Mac에서 Godot 4.6 .NET + .NET 9로 열어 macOS 실행 후 Xcode export·arm64 iPhone 실기기 설치를 확인한다. iOS C# 지원은 experimental이며 서명 Team ID와 기준 iPhone/OS는 그때 지정한다

<!-- NEXT-ACTION:END -->

<!--
규칙:
- 이 마커 사이는 "지금/다음 할 일" 1~3건만. 짧게(화면 한 판).
- 완료된 항목은 여기 두지 말고 WORKLOG.md 의 ## History 로 옮긴다 (단일 출처·비대 방지).
- 훅(print_next_action.py)은 이 마커 사이만 세션에 주입한다.
-->
