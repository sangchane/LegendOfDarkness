<!-- NEXT-ACTION:START -->

## ▶ 지금 할 일 (2026-09-23 갱신)

- **[다음 세션에 만들 것/사용자 지시 2026-09-23]** 두 가지다.
  1. **자동 포션** — 모바일 게임들처럼 **체력이 몇 % 이하 · 마력이 몇 % 이하**일 때 포션을 저절로 쓰게 한다.
     체력 쪽은 **쿠룸·쿠라눔·엑스쿠라눔**, 마력 쪽은 **마라디움과 그 밖의 마력 회복 소모품**이다.
     사용자 말 그대로: "체력포션(쿠룸, 쿠라눔, 엑스쿠라눔), 마력포션(마라디움 기타 mp 상승 소모템)
     모바일 게임들 처럼 자동 포션 소모 기능(체력 몇%, 마력 몇%이하)일때 사용 가능하도록 하는 기능".
     **먼저 볼 것**: 서버가 아이템 사용을 어떻게 받는지, 포션마다 얼마를 채우는지(아이템 템플릿·스크립트).
     기준선(몇 %)을 화면에서 고르게 할지, 어느 포션부터 쓸지(작은 것 먼저/큰 것 먼저)는 정해야 한다.
  2. **공격 단추가 직업마다 제 기본공격 모션으로 나가게 한다** — "공격버튼이 각 직업에 기본공격 모션으로
     발동하도록 할거야". 무도가는 이미 주먹으로 나간다(서버가 무기·갑옷의 `AttackMotion` 을 골라 보내고
     `WorldView` 가 그 번호를 기억한다). 그 길을 다섯 직업으로 넓히면 된다 — `ItemTemplate.AttackMotion`·
     `Assail.BlowMotion`·`Wardrobe`·`BodyMotion` 을 함께 본다. **직업 동작은 직업 의상에서만 온전하다.**

- **[끝남/오늘 된 것]** 이형환위 방향 · 가로 소지품·장비 창 · 괴물 공격력과 재생을 5.99 식으로 ·
  무도가 기술 종류(Type 1) · 닳아 부서진 장비가 없어지는 것 · 사냥터에서도 열리는 지도 ·
  **노비스마을 장비 상점**(델란·드보이)과 무도가 너클 둘 · **괴물 금화와 잡템 값**을 팩 값으로 ·
  **때린 자리 배수**(등 뒤 ×2 · 옆 ×1.5 · 정면 ×1)와 맞은 괴물이 돌아서는 것.
  캐릭터 만들기가 직업 바이트 때문에 거절되던 것은 **해결됐다** — 서버가 만들기 패킷에서 직업을 읽고
  (`54e0aa548`) 시험이 진짜 서버에 대고 확인한다(`4d34a38c`).
  **뒤이어**: **장비 값을 원작 도감으로 되돌렸다**(160장 816칸 — 레더튜닉 300→950) ·
  **장비 드롭**(노비스는 잡템만, 우드랜드·포테는 3%) · **수오미·우드랜드 상점 셋**(가이·아돌·
  보석상여주인) · **시약 값 절반**(쿠룸 150 · 마라디움 500). 자세한 것은 `WORKLOG.md` 2026-09-23(뒤).

- **[정할 것/글러브1 이 너무 세다]** 노바 팩 값 그대로 들여온 글러브1 은 1레벨에 80~100 인데 같은 1레벨
  에페가 15~20 이다 — 네댓 배. **사용자: 일단 그대로 둔다**(2026-09-23). 무도가 무기 사다리를 다시 볼
  때 함께 정한다.

- **[근거 없음/옆 1.5배]** 때린 자리 배수 셋 중 등 뒤 ×2 만 5.99 근거가 있다(`0x416331`·`0x4168d8`).
  **옆 ×1.5 는 사용자가 정한 값**이고 실행 파일에 1.5 상수가 아예 없다. 마법에는 안 걸린다(사용자 결정).
  근거가 나오면 `Sprite.BlowFacing` 의 `FromTheSide` 한 줄만 고치면 된다.

- **[빨간불/`PoteDungeonTests` 2개]** 혼수에 걸리고 12초가 지나도 죽지 않는다. **원인 미상** — 아직 안 팠다.

- **[남음/1~10레벨이 살 장신구가 없다]** 우드랜드 보석상 물목 22개가 **전부 11레벨 이상**이고
  (반지·장갑·신발·벨트·각반·방패 여섯이 11 · 귀걸이 21 · **투구는 99 둘뿐**), **목걸이는 한 칸도 없다.**
  1~10레벨이 낄 것이 세상에 없다. 5.99 팩 목록이 그렇게 생긴 것이라, 혼든처럼 저레벨 칸이 있는 팩에서
  더 들이거나 도감에서 골라 넣어야 한다(`scripts/build-town-gear-shops.py`).

- **[정할 것/마라디움이 초반 벌이의 절반을 넘는다]** 값을 절반으로 내린 뒤에도 노비스 한 마리 벌이의
  **55~65%**(팜팻 기준 56%)가 마라디움 하나다 — 금화 600, 잡템 363~1,033 인데 마라디움만 5,208 이다
  (100마리 기준, `python3 scripts/build-pack-gold.py`). 첫 옷까지 10마리는 맞췄지만 **벌이가 시약
  하나에 얹혀 있다.** 금화나 잡템 쪽을 올려 기대는 데를 나누는 것이 맞는지 정해야 한다.

- **[정할 것/매직파나·홀리파나가 1레벨에 180~200 피해]** 같은 1레벨 에페가 15~20 이다. 수오미 가이가
  150,000전에 판다. **팩 오기로 보이지만** 이 둘은 되돌리지 않는 지팡이 10종에 들어 있어(도감 줄이
  전부 같아 되돌리면 사다리가 뭉개진다) 손대지 않았다. 지팡이 사다리를 다시 볼 때 함께 정한다.

- **[남음/죽어 있는 팩 장비 드롭 3종]** 5.99 팩이 떨구라고 적어 두었지만 아이템에 `DropRate` 가 없어
  영영 안 나오는 것들이다 — **실버아쿠아링**(크라켄1·2, Lv51) · **골드아쿠아링**(킹아크퍼스2, Lv81) ·
  **그림록퀸홀**(그림록퀸, Lv99). 아벨해안·카스마늄 갱도는 아직 손대지 않은 지역이라 그대로 두었다.

- **[정할 것/코스튬·장신구는 도감 되돌림에서 뺐다]** 되돌린 것은 5.99 **무기·갑옷** 묶음뿐이다
  (+ 세줄금반지 값 하나). 장식 132장과 반지·귀걸이·목걸이·장갑·각반·허리띠·신발·방패·투구는 팩 값
  그대로다 — 묶음째 되돌리면 219장이 한꺼번에 움직이고 방금 문을 연 보석상 물목 21개가 전부 흔들린다.
  필요해지면 `scripts/build-gear-from-original.py` 의 `VALUE_ONLY` 에 이름을 하나씩 적는다.

- **[끝남/이펙트 자리를 게임 화면에서 봤다]** `nov`(노비스평원A, 20393 — 괴물 5마리)로 `--hunt` 를 돌려
  프레임 504장을 찍었고, 타격 연출이 **제 크기로 머리 높이·몸통에** 뜨는 것을 확인했다(프레임 462~486).
  `monk`·`watch` 는 마을에 서 있어 아무것도 안 나갔던 것이고, **괴물이 있는 맵에 선 계정으로 찍으면 된다.**
  아직 못 본 것: 직업 기술·마법의 연출(그 계정은 평민이라 평타만 나간다). 무도가 `monk` 를 사냥터로 옮기면 된다.

- **[남음/빗나감 표시가 없다]** 서버는 빗나가면 소리만 보낸다(`MonkStrike.Swing` 의 `ServerFormat13`).
  클라이언트에는 머리 위 표시 자리가 이미 있다(`Actor.cs:152` 체력바, `head - 7`) — 「miss」를 거기 띄우면 된다.
  원작도 머리 위다(사용자, 2026-09-19).

- **[남음/사제 마법 19개가 이펙트를 아예 안 쏜다]** `ao cradh`·`ao beag cradh`·`ao mor cradh`·`ao ard cradh`·
  `ao puinsein`·`armachd`·`deo saighead`·`디나르콜룸`·`디소루미아`·`리젠(Lev1)`·`신성력강화`·`신의축복`·
  `엑스쿠라네라`·`일루메눔`·`콜라마`·`쿠라네라`·`쿠라누스`·`쿠러스`·`홀리쿠라네라`.
  스크립트는 있는데 `effect` 를 안 부르고 템플릿 `TargetAnimation` 도 0 이다 — 5.99 원본 스크립트에 있는지
  먼저 보고, 있으면 옮기고 없으면 원작 관찰로 정해야 한다. 전체로는 구현 92 중 27개가 그렇다.

- **[정할 것/일음지 이펙트 번호가 둘이다]** 하데스 템플릿은 `TargetAnimation: 276`, 노바온라인 팩 표는 `42` 다.
  둘은 다른 그림이다(276 은 160x120 큰 것, 42 는 13x13 머리 위 반짝임). **발경은 템플릿이 0 이라 이펙트가
  아예 안 나간다.** 구현 92개 중 **18개가 이펙트·소리·몸동작을 하나도 안 보낸다**(현황판 「안 나감」 칩).
  어느 쪽을 정본으로 할지 정하고 템플릿을 맞춰야 한다.

- **[지금/소지품이 창에 안 보인다]** 잡아도 가방이 빈다. 무도가는 과적도 아닌데(무게 4/86) 가방 0칸이었다.
  확인된 것: 서버는 접속할 때 가방을 한 칸씩 보내는데 **무게가 한도를 넘으면 안 보내고 바닥에 떨어뜨린다**
  (`GameClient.cs:505`, 한도 = 레벨/4 + 힘 + 3.5). 클라이언트는 `0x0F` 를 제대로 읽는다(`WorldClient.cs:556`).
  **남은 것**: 물건이 실제로 떨어지는 사냥을 돌려 ① 괴물이 떨구는가 ② 떨군 것이 가방에 들어오는가를 가른다.
  `nov` 는 25레벨이라 `NovicePlay`(21레벨 졸업)가 바로 끝난다 — 클라이언트 `--hunt` 로 돌리거나 시험의 졸업
  레벨을 올려서 쓴다.

- **[끝남/월드맵·수오미 연결]** 알맹이가 월드맵을 읽고(`WorldClient.Field`) 골라 보내고(`ChooseFieldAsync`)
  진짜 서버로 우드랜드입구 → 월드맵 → 수오미 → 포테의숲1존(33,47)까지 자동 시험 3개가 통과한다
  (`62e968c8`·`72b06d8a`·`96e3e423`·`87e8b614`). 화면도 확인했다(`4f0ad4d7` 의 `FieldPanel` 배선) —
  사진 두 장, 창이 뜬 것과 캐릭터를 수오미마을에 세워 놓은 것뿐이다. 고르는 길 자체는 위 자동 시험이
  증명한다(`docs/mobile-client.md` "확인한 것"). **남은 것**: 그림
  얹은 원작식 월드맵(지금은 이름 줄뿐, 점 찍은 지도 없음).
  **2026-09-19**: 월드맵 목록을 수오미·우드랜드입구 둘로 줄였다(사용자 결정). 뺀 22줄은 서버 저장소
  커밋에 그대로 남아 있으니 내용이 채워지면 하나씩 되돌린다. **되돌리기 전에 볼 것**: 드라큐라의성(20399)·
  크리스마스마을(20711) 은 워프가 0개라 들어가면 못 나온다. Lost Woods(3)·야외배틀필드(20606)·
  죽음의마을(20683) 은 월드맵을 다시 못 연다. 우드랜드 네 줄은 목적지가 같아 첫째만 집힌다.
  **2026-09-19(지도 단추)**: 위 줄 [지도] 단추로도 같은 창을 연다 — 사냥터에서도 열리고(2026-09-23,
  괴물이 있어도 막지 않는다), 닫기는 취소(`OpenFieldAsync`·`CloseFieldAsync`, `0xF0`은 우리가 만든 말,
  원작에는 없다). 노비스마을
  시작 → 지도 단추 → 수오미 가로지르기(길찾기 147칸) → 포테의숲1존 도착까지 자동 시험 하나로 이었다
  (`WorldMapMenuTests`). **고침(Task D, 2026-09-19)**: 계정·캐릭터 만들기 화면이 생겼다
  (`CreateScreen.cs`, `--screen create`) — 이름·비밀번호, 남/여, HAIR·COLOR 격자, 맨몸 미리보기를
  거쳐 알맹이(`HadesLoginClient.CreateCharacterAsync`)로 만든다. 이 여정 시험도 이제 고른 값(성별
  2·머리 31·색 40)으로 캐릭터를 만들고 `aislings/<이름>.json` 에 그대로 박혔는지 확인한다 —
  `LoginFlow.TryCreateAccount` 의 `0x01,0x01,0x01` 고정값 우회는 더 가벼운 다른 시험에만 남았다.
  **고침(2026-09-19)**: 노비스마을에서는 지도 단추가 실제로 안 열렸다 — `FormatF0Handler`
  (`GameServerHandlers.cs:1859`, `sources/wren11`)가 "이 맵에 살아 있는 Monster 형이 하나라도 있으면
  거절"이었는데, 노비스마을에 놓아 둔 장식용 주민(`templates/monsters/5.99/노비스주민1·2@노비스마을.json`,
  경험치 0·`MaximumHP` 21억)이 `Monster` 형으로 실려 거의 곧바로 서서 늘 걸렸다(격리 시험은 그 젠이
  서기 전에 끝나 우연히 통과했었다). 괴물 템플릿 567장 중 경험치 0/빈 것은 4장뿐이고 전부 꾸밈용,
  월드맵에 있는 town 20곳 중 경험치 주는 괴물이 선 곳은 없다는 것을 확인하고, 거절 조건을 "살아 있는
  아무 Monster" 에서 "경험치를 주는 Monster" 로 좁혔다(`(Template.Exp ?? 1) > 0`). `WorldMapMenuTests`
  도 주민이 실제로 나타날 때까지 기다렸다가 물어야 재발을 잡으므로 그렇게 고쳤다. 노비스마을에서 화면
  두 장을 다시 찍어 확인함(`docs/mobile-client.md` "확인한 것").

- **[남음]** 파티 · 귀환 · 나머지 NPC 상점 문구(영어) · 2차 승급 ·
  후방 공격 두 배를 실제 전투로 재기 · 입력형 마법.

- **[남음/현황판이 드러낸 것]** 새 `docs/index.html` 이 보여 준 것들이다. **현황판 작업이 만든 일이 아니라
  전부터 그랬던 것**이고, 눈에 안 보여서 못 잡고 있었을 뿐이다(2026-09-19).
  ① **지금 켜면 도나** — 서버 기동·포트·클라이언트 빌드가 성한지 화면이 모른다. `lod-server.sh` 결과를 스냅샷에 넣으면 된다.
  ② **NPC 도감이 없다** — 수오미 9맵에 NPC 15명, 노비스에 13명이 서 있는데 누가 어디 있는지 볼 데가 없다. 사범·상점을 찾으려면 파일을 뒤져야 한다.
  ③ **소리가 화면에 없다** — 효과음 165개·배경음악 64개를 쓰는데 무엇이 이어졌는지 보이지 않는다.
  ④ **노비스지하던전 C1·C2 는 갈 수 있는데 괴물이 없다**(워프 연결도에 「비어 있음」으로 뜬다). 빈 방인지 안 채운 것인지 정해야 한다.
  ~~⑤ 1레벨이 한 마리로 레벨업한다~~ — **구멍이 아니다.** 노비스도 1/7.3 이 걸려 있고(7,800 → 1,068, 커밋
  `5eca73d18`), `docs/monster-experience-rescale.md` 의 표가 우드랜드 존1 도 1→2 에 1.1 마리라고 이미 적어 뒀다.
  노바 실측(1→2 에 1.5 마리)에 맞춘 값이라 초반이 빠른 것이 의도다.

### 오늘(9/18~19) 고친 것 — 다시 건드리지 않게

| 무엇 | 어디 |
|---|---|
| 갇히면 스스로 빠져나온다 | 서버 `Area.Enclosed`·`FreeWayOut` · `GameClient.FreeIfStuck` |
| 벽을 돌아가는 길찾기 | `Lod.Mobile.Core/World/Pathing.cs` (시험 8개) |
| 남이 선 칸에는 못 들어간다(운영자도) | 서버 `Sprite.Walk` |
| 워프는 빈 칸에 내려놓는다 | 서버 `Area.FreeSpotNear` |
| 마을 NPC 는 못 때린다 | 서버 `Sprite.Attackable` |
| 레벨 차이로 경험치 깎기 | 서버 `Formulas/monsterexp.cs` — **우리가 정한 값**, 근거 없음 |
| 머리 위 체력바 · 상태 아이콘 줄 | `HealthBar.cs` · `StatusRow.cs` · 알맹이 `Ailment` |
| 원작 4.51 테마(안A) | `Greybox.cs` 한 곳 — 배치는 안 바꿨다 |
| 무도가 평타가 주먹으로 나간다 | `WorldView` 가 서버가 말해 준 동작을 기억한다 |
| 무도가 기술 정리 | 양의신권(4)·단각(2)·이형환위(42)·쿠로토(21) 넷만 |
| 밟으면 줍기 + 켜고 끄기 | `WorldView.Gather` · `Main.AutoLoot` · 소지품 창 단추 |

### 함정 — 시간 버린 것들

- **고도는 `scripts/godot.sh` 로 연다.** 시스템 `/Applications/Godot.app` 은 **C# 이 없는 판**이라 화면이
  비어 나오고, 그냥 열면 `dotnet` 을 못 찾아 ".NET SDK 를 설치하라" 고 한다.
- **`--screen game` 은 로그인을 건너뛴다.** 서버에 안 붙고 자리표(체력 99999)만 보여 준다 —
  **그걸로 찍은 화면은 확인이 아니다.** `--login <계정>:<암호>` 만 주고 찍는다. 시험 계정은 `nov`·`monk`·`watch`,
  암호는 모두 `1234`.
- **헤드리스로는 화면을 못 찍는다**(그릴 것이 없어 `GetImage()` 가 빈 값이다). 창을 띄워 찍는다.
- **캐릭터 파일에는 템플릿이 통째로 복사돼 있다.** 기술·아이템 템플릿만 고치면 이미 배운 것에는 반영되지
  않는다 — `aislings/*.json` 안의 사본도 함께 고친다. 고치기 전에 **서버를 멈춘다**(안 그러면 덮어쓴다).
- **서버를 켤 때 포트를 확인한다** — `lod-server.sh` 가 2610·2615 가 열릴 때까지 기다리고, 안 열리면
  기록을 보여 주고 실패로 끝난다. "켰습니다" 만 믿고 헤매던 일이 있었다.

## ▶ 지금 할 일 (새 세션은 이 블록부터 — SessionStart 훅이 자동 주입)

- **[지금/UI 테마 — greybox 졸업]** 사용자 결정: **원작 4.51(5.01 이전) 테마** · 애플·토스급 직관성 · 시안부터.
  3안을 그려 **안 C**(틀은 원작 돌, 속은 평평하게)를 골랐다. **규칙표가 정본: `docs/original-ui-451.md`**
  "이 테마로 무엇을 만들기로 했나" 절 — 돌 두 가지(어두운 돌=넓은 면 · 밝은 돌=작은 단추)·글자 색·치수·
  `.pip` 아이콘(18px, 그림 파일 없음)까지 값으로 적혀 있다.
  눌러볼 시안 `docs/ui/mockups-451/index.html`(게임 화면 3상태·장비창·소지품·NPC·상점·대화·로그인) ·
  3안 비교 `three-ways.html` · 원작 UI 92장 `docs/ui/original-451/`.
  **배치는 안 바꾼다** — 손댈 곳은 `mobile/client/src/Greybox.cs` 와 그것을 부르는 곳들이다.
  **위 상태판의 긴 체력·마력 막대는 뺀다** — 맥이 머리 위 막대를 넣었으므로(`2faede52`) 같은 것을 두 번 그리게 된다.
  위 판은 정확한 숫자, 머리 위는 싸움 체감. 아직 구현은 시작하지 않았다(시안 승인까지 받은 상태).
  **묻는 법**: 볼트 `data/ui-vault/`(Obsidian, 노트 138장) · 그래프 `python scripts/build-ui-graph.py` →
  `graphify query "체력 마력 아이콘 왜 납작하게" --graph data/original-ui/graph/graph.json --budget 700`
  (규칙 → 근거 → 색·치수 → 닿는 코드까지 한 번에 나온다). 단일 출처 `data/original-ui/451.json`.

- **[끝남/무도가 1→10]** 무도가가 **싸워서** 1레벨에서 10레벨까지 갔다. 우드랜드1-1 · 100마리 · 23분.
  체력 150→342 · 마력 200→299 · 레벨당 점수 2. 요구 경험치는 원작 표 그대로(2레벨 600 … 10레벨 11,742).
  운전대는 `tests/hades-characterization/LivePlay.cs` (`LOD_LIVE=<로그인포트>` 를 줘야 돈다).
  **전직은 레벨을 1로 되돌리고 점수 196을 한 번에 준다**(`ClassChooser.cs:76-77`) — 그래서 그 기록은
  점수를 0, 능력치를 10/5/5/5/5 로 되돌린 뒤 다시 잰 것이다.

- **[끝남/막힘 셋]** 아이패드에서 "괴물이 안 나온다" 던 것의 진짜 원인:
  1. `GameClient.cs:1279` 가 맵 밖 좌표로 배열을 찝어 **맥박마다** 예외(5분에 16,630번). 예외 하나가
     그 갱신을 통째로 버려 괴물·상인·걸음이 도착하지 않았다. `Area.IsWall` 로 바꿨다
  2. 클라이언트가 **맵을 안 그렸다** — 어느 맵이든 `safehouse.png` 한 장. 이제 `map<번호>.png` 를 깐다
  3. 괴물 그림이 **3장뿐**이었다. 우드랜드 다섯 종을 `hades.dat` 에서 뽑았다(`strip` 필수)

- **[지금/5.99 전 직업 기술·마법]** 5.99 스크립트를 **문장 그대로 C# 으로 옮긴다**(사용자 지시: 5.99 기준으로 완성).
  변환기 `python3 scripts/build-pack-abilities.py [--쓰기]` → `scripts/Pack599/{Skills,Spells}/<이름>.cs`.
  명령의 뜻은 통역 한 곳 `scripts/Pack599/Pack599.cs` — 없는 명령은 서버를 멈추지 않고 로그만 남긴다.
  **157블록 중 136개를 옮기고 기본공격은 하데스 Assail 을 붙였다 — 131개는 명령이 다 있다**(무도가 기술 18개는 `build-monk-skills.py` 그대로, 정권 제외).
  시험 `Pack599AbilityTests`: 전사 내려치기 · 도적 찌르기 · 성직자 쿠라노(회복) · 법사 플레어 · 메테오(±7칸) 통과.
  남은 것:
  1. 남은 명령: `discovery_skill`(탐색) · `item_info`(아들레스투)는 **나중**, 안티매직·포트리스(승급)·소매치기는
     **안 해도 된다**(사용자). 적갑옷·적무기해체는 넣었지만 두 캐릭터가 결투 맵에서 마주 서야 해 실제로는 안 쟀다.
     침묵·완전방어·델리스펠라스·속성강화·휴식·슈페이아움은 상태만 걸고 효과 크기는 비어 있다(엔진 안)
  **괴물 마법**: 5.99 `Mob_Spell.txt` 의 `Monster_이름` 19개를 같은 변환기로 옮겼고(맞는 사람 쪽에서 돈다 —
  `get_myid` 가 맞는 사람, `object_name` 이 괴물), 5.99 괴물 정의의 `스킬` 칸대로 하데스 괴물 템플릿 97장에 붙였다.
  하데스 괴물 AI 는 같은 이름의 **마법 템플릿이 있어야** 불러오고(없으면 말없이 건너뜀), **제자리 고정 괴물은 시전을
  못 켠다**(`CommonMonster.Walk` 안에서 켠다). 5.99 에 **정의가 없는** 괴물 마법을 부르는 템플릿이 198장 있다 —
  마레노3(51) · 플라모3·4(각 32) · 나르콜리2·소루마2(각 22) · 마레노4(16) · 마레노2(8) · 아듀로·아듀로2 · 플라모2.
  **모바일 화면**: 사람·괴물이 칸 사이를 걷고, 기술 이펙트(0x29)·소리(0x19 와 체력바 0x13 끝 바이트)가
  나온다(2026-09-17, 녹화 프레임으로 확인 — `docs/mobile-client.md`). 기술·마법 **몸 동작**도 서버 번호대로 그린다
  (`BodyMotion`, 부위별 b~f 시트 — `scripts/build-client-wardrobe.py`). **직업 동작은 직업 의상에서만 온전하다**(사용자) —
  녹화해 보일 때는 직업 의상(`skill.tbl` ST)을 입힌다. 옷장(갑옷·무기·투구·방패·신발)은 서버 아이템 전부를 뽑았다
  (`scripts/build-client-wardrobe.py`, 칸 120x96). 5.99 무기 111종이 서버 템플릿으로 들어왔다(`build-pack-equipment.py`).
  겹치는 순서는 5.99 `Legend.exe` 표 그대로(`Wardrobe.Rank`). **평타 동작은 5.99 서버처럼 무기 → 갑옷의 `공격모션` 으로 고른다**
  (`ItemTemplate.AttackMotion`·`Assail.BlowMotion`). 5.99 갑옷 225종도 들어와 도복 주먹·신발 금지가 된다
  (`build-pack-equipment.py`), 옷장 그림·아이콘까지. **상점·드롭은 2026-09-23 에 붙었다**(`build-town-gear-shops.py`·`build-gear-drops.py`).
  **역어셈블**: 정리 `docs/disassembly.md`(5.99 서버·클라이언트 + 원작 4.51·2005, 합쳤다). 기술 동작 옷 목록·이펙트 칸 순서는
  모바일에 옮겼다. 동작 6·21·22(손 들기·키스·손 흔들기, `03` 파일)와 감정표현(9~17·23~44, 머리 위 표정·말풍선)도 옮겼다. 남은 것: 감정표현을 보내는 버튼.
  **NPC 대화창(2026-09-17)**: 모바일이 여덟 모양을 읽고 `0x3A` 로 답한다(`TalkPanel`, 식당에서 뱀고기 사기까지 실제 서버로 확인). NPC·괴물 그림 112종을 뽑았고(`build-client-creatures.py`) NPC 템플릿 그림 번호를 0x4000 체계로 고쳐 NPC 가 보이고 눌린다 — **라이브 서버는 다시 켜야 반영된다.** 상점 판매 목록 180종 중 물약·음식·귀환 주문서 38종을 들여 쓸 수 있게 했다(`build-pack-consumables.py`, 서버 `Consumable` 스크립트) — 이어서 방패·투구·장신구·장갑·허리띠·각반·신발·장식 416종도 들였다(`build-pack-equipment.py`). 염색약·기타·퀘스트·잡화도 들여 **판매 목록 180종이 모두 서버에 있다.** 아이템 사용 스크립트 중 기다리지 않는 16개(염색약 9 · 코마디움 …)는 `ITEM_이름` 으로 돈다(`build-pack-npcs.py`). 창을 띄우는 10개(확성기·선물주머니·미션두루마리 …)는 NPC 없는 창이 필요해 남았다. 상점 문구가 영어다.
  **기술 사범(2026-09-17)**: 5.99 `Npc_Skill.txt` 29개를 C# 이터레이터로 옮겨(`build-pack-npcs.py`, 서버 `PackNpc`) 밀레스마을 직업 사범 20 · 달인 3 이 대화로 가르친다(`Pack599TeacherTests` — 가렌에게 숏블레이드). 남은 것: 승급의 공간·화론 사범 6(맵 없음), `get_son` 뜻, 기술 템플릿 `Pane` 이 비어 73번 칸에 들어감, 나머지 NPC 스크립트도 84개 모두 옮겼다(`#이름` 진행값은 `Aisling.PackVariables` 에 저장, `warp`·`item_add`·`get_sex`·`input`). 그라가스 퀘스트는 다시 접속해도 이어진다(`Pack599QuestTests`). 팩에 NPC 정의(그림)가 없는 스크립트 NPC 53곳(게시판알리미 10 · 워프할아버지 · 선진 · 미용사 · 이동 NPC …)도 그림 번호 0 으로 세웠고, 모바일은 그 자리에 "!" 표식을 그려 누르게 한다(사용자 결정: 표식만, `NpcMark`). **남은 명령:** `map_create`(개인 던전) · `mob_spawn` 계열 · `shop`·은행 · `item_one_check` · `legend_add` · `set_class`(전직, 순수 `set_son`). 남은 빈틈: 이펙트·괴물 그림도 빈 칸 번호 밀림을 다시 확인(`Epf.cs` 고침 뒤 이펙트는 아직 안 뽑음, `Mpf.cs` 미확인),
  하데스가 괴물 평타에도 그림 203 을 띄운다(5.99 기본공격은 그림 없음 — 괴물 쪽은 모름).
  2. 직업은 파일 이름 → 가르치는 NPC → 모션(`skill.tbl`) 순으로 정한다 — 공통스킬 8개만 공용
  3. `로카메아` 는 원본의 이름 없는 `$` 변수 때문에 못 옮겼다
  4. `magic 9` 뜻 미확인
  **공격력·마법 공격력·치명타 식은 5.99 서버 프로그램에서 읽었다**(`Pack599.AttackPower`·`MagicPower`·`Critical` 주석).
  서버 프로그램은 저장소 밖 `~/Downloads/5.99 서버팩/Novaonline.exe` — `objdump -d --x86-asm-syntax=intel` 로 읽는다.

- **[다음/무도가 기술 — 남은 틈]** 5.99 팩의 무도가 기술 19개 중 **정권을 뺀 18개**가 들어갔다(WORKLOG 2026-09-16).
  생성기 `python3 scripts/build-monk-skills.py [--쓰기]` · 시험 `MonkLevelTenSkillTests`.
  팩에 있는데 아직 안 옮긴 것:

  | 빠진 것 | 팩에서 |
  |---|---|
  | **배우는 조건** — 2차 직업과 함께 **나중에**(사용자 결정) | 연천단각: 승급자 · 단각 보유 · 5만 골드 · 좀비의살 20 · 좀비의막대기 10, **배우면 단각이 사라진다**. 마구때리기: 2차승급 · 어빌리티 18 · 옐로토닉 10 · 그린토닉 7. 늑대의위상: 2차승급 · 어빌리티 22 · 레드토닉 12 (`Npc_Skill.txt` 1286·1440행). 하데스에는 "배우면 지우기" 가 없고, 2차 직업 흐름도 틀만 있다 — `ClassStage` 다섯 단계와 `Stage_Required` 검사, 어빌리티 레벨 칸(`AbpLevel`)은 있지만 올리는 코드가 없다. **첫 전직(`ClassChooser.cs:75`)이 곧바로 `Master` 로 만들어** 원작표의 2차 기술(`Stage_Required: 1`)이 레벨만으로 열린다 |
  | 치명타 ×2 | 11개가 `get_critical` 로 두 배. **확률이 팩 자료에 없다**(엔진 안 `0x9C`) — 지어내지 않았다 |
  | 늑대의위상 큰 쪽 딜레이 3초 | `ExecuteAbility` 가 스크립트 **뒤에** 템플릿 쿨다운(6초)을 덮어써서 스크립트로는 못 담는다 |
  | 소수신공이 무엇을 얼마나 세게 하나 | 팩은 `sosusin(나, 120)` 한 줄뿐이고 효과는 엔진 안(캐릭터 `0x7C`)에 있다 — 그 자리를 읽는 팩 명령도 없다. 그 밖은 5.99 에 맞췄다(사용자 지시) — 마력 120 · 대기 15초 · 120초 · 「소수신공을 외웠습니다.」·「이미 걸려있습니다.」· 배우는 레벨 70. 세기만 하데스 Claw Fist(**맨손 평타만** 3배)로 대신했다 |

  범위도 들어갔다 — 백보신권·무영신공은 **보는 쪽 앞 세 칸**, 선풍각·파천각은 **둘레 네 칸**(`MonkStrike.Use` 의 `reach`·`around`).
  배우는 레벨도 5.99 NPC 에서 읽는다(생성기가 만든 템플릿만): 백보신권 87 · 발경 93 · 소수신공 70. 단각·붕각은 원작 Kick·Martial Awareness 레벨이 있어 덮지 않았다.
  마력 소모는 들어갔다(10개). **단각은 원작이 마력을 안 쓴다**(사용자) — 생성기 `NO_MANA`.
  **정권은 뺐다**(사용자 지시) — 생성기 `EXCLUDED`. Sting 은 앞 세션이 정권 공식(×2.5)을 빌려 쓴 채로 남아 있다.
  **원작 여부(사용자 확인)**: 붕신선각만 원작에 없다 → `Group: 5.99표/무도가/원작없음`. 늑대의위상은 팩에서 2차승급 기술이라 원작일 수 있다.
  `단각.json`·`붕각.json` 은 원작 `kick.json`·`martial awareness.json` 과 **같은 기술의 중복**이다(남겨 둠).
  관리페이지 기술·마법 탭에 **구현/미구현** 칩이 있다 — 구현 = 템플릿이 가리키는 스크립트가 실제로 있다
  (기술은 `ScriptName`, **마법은 `ScriptKey`**). 613 중 **69**.

  **함정.** 생성기는 이미 있는 템플릿(원작표)에서 `ScriptName`·`Sound`·`TargetAnimation`·`Cooldown`
  넷만 바꾼다 — 통째로 쓰던 때 연환포의 요구 조건(44레벨 …)이 지워졌었다.
  `MonkStrike.cs` 를 고친 뒤에는 `ScriptCompilationTests` 로 런타임 컴파일을 확인한다(`dotnet build` 는 못 잡는다).

- **[끝남·1단계/맵 세우기]** 노비스마을 · 건물 안 6 · 평원 A·B · 우드랜드1-1 은 바닥·건물·나무·벽을 클라이언트가 맞춰 그리고 벽으로는 내딛지 않는다(`scripts/build-client-maps.py`, WORKLOG 2026-09-17). **나머지 맵은 재료가 없어 바닥이 비어 나온다** — 2단계: 맵 번호를 넣어 더 뽑거나, 타일·그림 판을 맵끼리 나눠 써서 801맵 전부. 움직이는 그림(`stcani.tbl` 물·횃불)은 첫 장만. 새 캐릭터 시작 = 노비스마을 37,29(라이브 설정, 서버 재시작 필요).
  **원작 우드랜드 판정에 `maps` 폴더는 못 쓴다** — 5.99 클라이언트 `maps` 는 5.99 서버가 보내 쌓인 것이라 5.99 팩 우드랜드 8/14 와 같고 혼든은 1/28. 월드맵 `EX … 번호`(1~24)는 맵 파일 번호가 아니다(`lod22`·`lod23` 없음).

- **[끝남/포테의숲 · 노비스 사냥터 마무리]** 포테의숲 1~6존 · 보스존(5.99 개인 던전 — `map_create` 사본, 서버 `Systems/Instances`) · 수오미마을 건물 문,
  노비스·수오미·포테의숲 워프 레벨 범위(5.99 문구), 노비스·수오미·포테의숲 괴물 드롭 5.99 확률. 시험 `NoviceHuntingGroundTests` · `PoteForestTests` ·
  `SuomiTownTests` · `PoteDungeonTests`. 오솔길 함정은 5.99 역어셈블로 **혼수**(하데스 빈사)로 옮겼다. **남은 것**: 다른 지역 워프 레벨 범위(아벨 51~80 · 밀레스·VOD·광산 99 전용 — **시험을 끝낸 뒤 구현**, 사용자 2026-09-18) ·
  다른 5.99 개인 던전(튜토리얼·호러캐슬·엘리멘탈 — 같은 명령으로 돈다, 시험 안 함) — `docs/pote-forest.md`.

- **[도는 중/노비스 21레벨 플레이 시험]** `LOD_LIVE=2610 LOD_LIVE_LOG=<파일> dotnet test … --filter NovicePlay`.
  9 → 11레벨까지 확인했고 분당 570 경험치다. 시험이 찾은 것: 맵 밖으로 걸어 나감(고침) · 젠이 맵 크기를 안 따름(고침) · 마을 워프를 밟아 나감(시험이 되돌린다).
  **후방 공격 두 배는 아직 실제 전투로 못 쟀다** — 괴물이 등을 보이게 만드는 방법을 정해야 한다.

- **[지금/원작 우드랜드]** 사용자: "지금 우드랜드는 내가 알던 버전이 아니던데" → "원작 확인부터". 서버의 우드랜드는 5.99 팩의 14맵 판이고,
  원작 월드맵에는 동의·서의·남의·북의 우드랜드가 있다. 혼든 팩(동의우드랜드 입구·1-1~20-1, 워프 사슬 57)과 Novaonline 팩(동·북·무표시, 대기실 판)이
  후보인데 둘의 연결이 8개만 같다. **윈도우 세션 작업지시서 `docs/woodland-origin-work-order.md`** — 원작 설치본 맵 파일과 팩 맵 파일 해시 비교만.
  역어셈블은 맥에서 끝냈다 — 월드맵 표의 `EX x y 번호` 는 저장만 되고 읽는 곳을 못 찾아, 클라이언트에서 연결은 안 나온다. 맥 쪽 후보 해시 `data/map-origins/woodland-candidates.json`. 결과가 오면 어느 팩 기준으로 옮길지 정한다.

- **[남음/이름]** 한글 이름이 613개 중 **49개**뿐이다. 막는 것은 `Hades 영문 없음/다중` 168건 —
  아이콘 하나를 여러 기술이 나눠 쓰고(`Assail`·`Assault`·`Long Strike` 가 전부 1번), 하데스 템플릿
  258장 중 `Icon` 을 가진 것은 1장뿐이다. 아이콘 말고 다른 열쇠가 필요하다.
  **확정된 것**: 단각=Kick · 붕각=Martial Awareness · 금강불괴=Iron Skin.
  **미확정**: High Kick, 그리고 아이콘 4의 주먹 셋(`Double Punch`·`Poison Punch`·`Sting`)에
  양의신권·백보신권·통배권을 어떻게 짝짓나 — 아이콘 그림이 한 장뿐이라 눈으로는 못 가린다.

- **[끝남/아이폰 올리기]** `scripts/ios-build.sh` 로 만들고 무선으로 넣는다(무료 계정). 서명은 7일짜리지만 **스스로 갱신한다** —
  `watch-sign` 이 날마다 살펴 이틀 이하면 `renew`(Xcode 프로젝트를 기기 지정해 빌드)를 돌린다. 아이패드가 켜져 같은 Wi-Fi 에 있으면 된다. 케이블은 기기를 처음 등록할 때 한 번뿐.

- **[남음/되돌릴 것]** `mobile/client/export_presets.cfg` 의 `application/app_store_team_id` 는 `""` 로
  되돌려 두었다. 아이패드 빌드를 다시 구우려면 `HQC44HA87V` 를 넣고, **끝나면 다시 비운다**.
  `mobile/client/{server,login,hunt}.cfg` 는 커밋되지 않는다(`.gitignore`).

- **[끝남/서비스 기반]** 서버를 손으로 켜고 끄던 것을 `scripts/lod-server.sh` 한 곳으로 모았다 — 상태·기록·설정 다시 깔기·캐릭터 백업,
  그리고 맥 등록(`install-agents`): **꺼지면 3초 안에 다시 켜지고, 새벽 4시마다 백업한다**(`~/LOD-backups`, 30벌).
  기록은 `~/Library/Logs/LOD/server.log` 한 곳. 설정 둘(`LoruleConfig.json`·`MServerTable.xml`)은 저장소의 틀에서 만들고 주소는 `LOD_SERVER_IP` 하나로 정한다.
  **밖에서 접속하려면**(사용자 결정 대기): 공유기 포트 열기 + 따라가는 주소(DDNS), 또는 클라우드 서버로 옮기기. 둘 다 `LOD_SERVER_IP` 만 바꾸면 된다 — `docs/run-procedure.md` 0.4절.

### 맥에서 서버 띄우기

```bash
cd sources/wren11/Dark-Ages-Private-Server/Staging/net9.0
DOTNET_ROOT=$PWD/../../../../../.tools/dotnet-9.0.317 dotnet Lorule.GameServer.dll
```
**빌드하면 `LoruleConfig.json` 이 덮어써진다** — 절대경로 셋과 `ServerIP` 를 다시 고쳐야 한다
(`docs/run-procedure.md` 0.5·0.6 절). 하네스 배경 작업으로 띄우면 메모리 압박에 잘리므로 `nohup … & disown`.

<!-- NEXT-ACTION:END -->

<!--
규칙:
- 이 마커 사이는 "지금/다음 할 일" 1~3건만. 짧게(화면 한 판).
- 완료된 항목은 여기 두지 말고 WORKLOG.md 의 ## History 로 옮긴다 (단일 출처·비대 방지).
- 훅(print_next_action.py)은 이 마커 사이만 세션에 주입한다.
-->
