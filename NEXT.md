<!-- NEXT-ACTION:START -->
## ▶ 지금 할 일 (새 세션은 이 블록부터 — SessionStart 훅이 자동 주입)

- **[지금/이식]** **`plans/server-pack-content-port.md` 8단계 — NPC 배치 84.** 0·1·3·4·5·6·7 끝났다(맵 801 · 워프 890 · 아이템 992 · 괴물 568, 오류 0, 시험 64개). NPC 가 세상에 없는 건 스크립트가 없어서가 아니라 `templates/mundanes/` 가 비어서다 — `scripts/Mundanes/` 에 25장이 대기 중이다. **배치마다 이름을 새로 지어야 한다**(`{npc}@{맵}#{x},{y}`) — 캐시가 `Name` 열쇠라 31종을 그대로 쓰면 179배치가 31개로 뭉갠다. 이 단계의 진짜 완료 기준은 **게임 안에서 NPC 를 눌러 대화창이 뜨는 것**

- **[주의/자료]** **큰 수를 조심하라.** 팩에는 Int32 를 넘는 값이 실제로 있다(체력 42억·4.1조). 한 장이 넘치면 Newtonsoft 가 던지고 **그 갈래 적재가 통째로 멎는다** — 로그 줄 자체가 안 찍힌다. 적재기의 `whole(v, cap)` 에 한계를 꼭 준다

- **[주의/그림]** 지도 그림은 **바닥만** 그린다. 건물·상점은 맵 칸의 벽 두 칸과 `seo.dat` 안 맵별 `.hea`(34개)에 있고 읽는 코드가 없다. 벽 번호(최대 3,996)가 `.hea` 를 가리키는지 별도 타일셋인지부터 확인하면 통째로 뜯지 않아도 될 수 있다

- **[참고]** 그래프·볼트에 **먼저 물어라.** 한국어 문장 말고 **기호**로 — `graphify explain "shop1"`. 팩 자료는 `data/server-packs/vault/<팩>/`(Obsidian)

<!-- NEXT-ACTION:END -->

<!--
규칙:
- 이 마커 사이는 "지금/다음 할 일" 1~3건만. 짧게(화면 한 판).
- 완료된 항목은 여기 두지 말고 WORKLOG.md 의 ## History 로 옮긴다 (단일 출처·비대 방지).
- 훅(print_next_action.py)은 이 마커 사이만 세션에 주입한다.
-->
