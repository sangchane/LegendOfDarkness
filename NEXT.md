<!-- NEXT-ACTION:START -->
## ▶ 지금 할 일 (새 세션은 이 블록부터 — SessionStart 훅이 자동 주입)

- **[끝남/경험치]** **경험치를 원작 비율로 맞췄다.** 레벨 요구치를 원작 곡선(식 네 조각)으로 바꾸고,
  같은 식이 두 곳에 복사돼 있던 것(`monsterexp.cs` · `Monster.cs`)을 `ExperienceCurve.ToReach` 하나로 모았다.
  괴물 경험치 565 장에 계수 1/7.3 을 걸었다 — 5.99 와 노바가 레벨 표는 같은데 경험치만 9 배 달랐고 노바가 원작 비율이다.
  우드랜드1-1 이 3,400 에서 466 으로, 레벨당 마릿수가 1→2 에 1.1 · 9→10 에 20 · 49→50 에 117 이 된다.
  근거·측정: `docs/monster-experience-rescale.md`. 포크 `feature/original-exp-curve`

- **[지금/맥북]** **1~10 구간을 직접 돌려 본다 — 기술·마법을 쓰면서 레벨이 오르는지.**
  경험치·젠·전투는 시험으로 확인됐고, 남은 것은 사람이 눈으로 보는 것이다. 우드랜드1-1 입구에서
  시작해 존1→2→3 으로 올라가며 무도가 기술 다섯(`Kick`·`High Kick`·`Double Punch`·`Poison Punch`·`Sting`)을
  써 본다. **연출은 아직 안 붙어 있다** — 몸짓은 나오지만 이펙트·소리 칸이 비어 있어 아무것도 안 터진다
  (`docs/martial-artist-skill-presentation.md`).

  **맥북에서 이어 받으려면 세 가지:**
  1. 포크에 브랜치가 둘이다 — `fix/spawn-density`(젠) 위에 `feature/original-exp-curve`(경험치)가 쌓여 있다.
     서브모듈 포인터는 `5eca73d18`(둘 다 포함)을 가리킨다
  2. **맥에서 돌리려면 `fix/run-on-macos` 를 합쳐야 한다** — 경로 구분자 `\` 열두 곳과 net9.0 이 거기 있다
     (`docs/run-procedure.md` 0.5 절). 아직 안 합쳤다
  3. 빌드하면 `LoruleConfig.json` 이 덮어써진다. 다른 기기에서 붙으려면 주소가 두 군데다
     (`MServerTable.xml` 의 `<Addr>` 와 `LoruleConfig.json` 의 `ServerIP`) — `docs/run-procedure.md` 0.6 절

- **[해결됨/드롭]** 팩드롭 아이템이 안 떨어지던 것은 **드롭 기능 문제가 아니다.** 한글화 작업이 진행 중이라
  그 아이템 정의가 아직 덜 채워진 상태였던 것(2026-09-15 확인). 영문 아이템은 정상으로 떨어지고,
  시험도 영문 기준으로 둔다

- **[미결/포크]** **포크에 커밋 안 한 작업이 남아 있다.** 괴물 565종에 5.99 수치(체력·공격·방어·경험치)를 넣었고,
  드롭이 부르는 잡템 72장(`뱀고기`·`벌꿀` — 영문 표에 대응물이 없다)을 `Group: 팩드롭` 으로 넣었다. 커밋할지 되돌릴지 미정.
  적재기(`tools/pack-import/import.py`)도 그에 맞춰 고쳐 둔 상태다

- **[진행/한글화]** **아이템 한글 이름 136 → 514장(978 중 53%), 전부 고유하다.** 개발 베이스는 그대로 영문이고,
  한글은 나중에 표시 이름으로 얹는다. 짝짓기는 `scripts/compare-packs.py` → `data/pack-compare/`. 등급으로 근거를 남겼다:
  `PACK_CONSENSUS`·`EXTRACTED_SINGLE` 은 팩이 말한 것, `KO_BASE_COLLAPSED` 는 한 그림에 몰린 변종을 벗겨 모은 것,
  `AFFIX_COMPOSED`·`AFFIX_DERIVED` 는 밑말에 접사를 붙여 지은 것(유도분만 되돌리려면 `AFFIX_DERIVED` 를 거르면 된다),
  `WEAPON_TIER` 는 데미지 사다리로 이은 것.
  - 푼 것 넷: ① 그림 번호는 `Image` 가 아니라 **`DisplayImage`**(`0x8000` 오프셋) 다 — `Image` 는 인벤토리 아이콘이라
    반지 122장이 210 하나를 쓴다. ② 한 그림에 몰린 한글 이름은 **한 물건의 변종**이라 껍질을 벗기면 밑말이 하나로 모인다.
    ③ 접사 열둘은 카페 「520 아이템 속성에 관하여」가 다 적어 두었다(세오=Deoch · 뮤레칸=Sgrios · Magic 은 **마력**으로 적는다).
    ④ 무기의 `수 < 토 < 풍 < 화` 와 `Good < Fine < Grand < Great` 는 **둘 다 데미지 사다리**라 칸끼리 잇는다(28군 : 3군, 예외 0).
  - 남은 것: 다투는 것 297 · 이름이 겹쳐 내린 것 69 · 짝이 아예 없는 것 98. 사람이 고를 검토표는
    `data/pack-compare/한글이름-검토.tsv`, 까닭은 `docs/pack-comparison.md`
- **[걸림/아이템]** **하데스 978행 중 영문 이름이 겹치는 것이 36종 74행 있다** (`Broad Sword` 셋 · `Hy-Brasyl Helmet` 둘 …).
  템플릿은 이름이 열쇠라(`GlobalItemTemplateCache[template.Name]`) 그대로 실으면 서로 덮어 38장이 조용히 사라진다.
  한글화와 무관한 기존 문제다
- **[재료/아이템]** **카페가 문서화한 아이템 371장이 하데스에 없다** — `Iron Shield` · `Light Shield` · `Breath Ring` 같은
  흔한 것도 없다. 레벨·가치·내구도·무게·무장·능력치·습득처·스프라이트 주소가 다 있어 템플릿을 지을 수 있다:
  `data/game-data/items-cafe-missing.json`. 원문은 `docs/darkages-cafe/`(카페 글 245장)
- **[지금/이식]** **10단계 스모크 — 사람이 한 번 돌아본다.** 0~9 가 다 끝났다: 맵 801 · 워프 909 · 아이템 992 · 괴물 568 · NPC 94(상점 10 포함) · 기술 258 · 마법 329 · 월드맵 24노드, 오류 0, 시험 70개(전투 2개 포함). 접속 → 워프 두 번 → 월드맵 → 사냥 한 마리 → NPC 대화 → **상점에서 하나 사기** → 기술 쓰기 → 재접속해 소지품 확인

- **[지금/이름]** **기술·마법 한글 이름.** 인덱스 "기술·마법" 탭에서 직업별로 보며 채우면 된다(아이콘 붙어 있음). 채운 뒤 **표로 내보내기** → `data/기술마법-한글이름.tsv` 에 붙여 넣고 `python3 tools/pack-import/import.py --kind skills --write`(그리고 `--kind spells`)

- **[아이템/끝났다]** **아이템은 하데스 것으로 갔다.** 영문 975장을 얹고 팩 989장을 뺐다(포크 `53fdfa719`·`5383f7f4a`). 템플릿 **940장** = 하데스표 937 + 하데스 자기 것 3. 적재기의 `write_items` 는 막아 뒀다 — 안 막으면 다음 `--write` 가 되살린다

- **[지금/상점]** **상점과 드롭이 비었다.** 팩 아이템을 빼면서 같이 끊겼다 — 상점 NPC 23곳 중 **팔 물건이 남은 곳 0곳**(재고 360항목 중 실재 0), 드롭 236배치 중 **1배치**. 터지지는 않는다(`ContainsKey` 로 건너뛴다). 하데스는 상점 목록을 싣지 않으므로 **무엇을 팔지 정하는 콘텐츠 작업**이다 — 영문 940장 중에서 고르면 되고, 이름을 옮겨 잇는 일이 아니다. `shop1.cs` 가 사기·팔기·수리를 다 하므로 `DefaultMerchantStock` 목록만 채우면 된다

- **[참고/아이템]** **영문 베이스로 간다.** 하데스가 `origin/Zolian` 브랜치에 영문 아이템 **978장**을 완전한 스펙으로 싣는다 — 이미지 978 · 방어력 443 · 착용자리·직업·성별·요구레벨·스크립트. 뽑아 둔 것: `data/game-data/items-hades.json` (`scripts/build-item-base-hades.py`). 지금 서버에 실린 팩 989개는 규칙 1번 위반이라 이것으로 갈아야 한다. **한글 이름은 나중에** — 이미지+착용자리로 맞추면 되고(뜻까지 맞은 짝: `Earth/Wind/Sea Necklace`↔`대지/바람/바다의목걸이` · `Leather Greaves`↔`가죽각반`), **같은 이미지는 형용사만 다른 같은 아이템**이므로 1:1 이 아니라 묶음끼리 맞춘다. 상점·드롭·NPC 결합이 팩 한글 이름을 참조하므로 같이 옮겨야 한다. 셈은 `python3 scripts/build-truth-vault.py` → `data/truth-vault/갈래/아이템`

- **[지금/기술마법]** **587개 중 42개만 실제로 돈다.** 기술 258 중 17 · 마법 329 중 25 만 스크립트가 붙어 있다(셈은 `python3 scripts/build-formula-vault.py` → `data/formula-vault/구현/`). 순서는 **하데스 먼저, 원작 다음, 팩은 안 본다** — 하데스가 기술 30 · 마법 40 스크립트를 이미 갖고 있고 이름이 영문이라 `data/game-data/abilities.json`(원작 275·338)과 같은 체계다. 587개를 하나씩 쓰는 게 아니라 **공용 스크립트에 붙이는** 일이다(`Generic Elemental Single`·`Generic Elemental Mass` 가 속성 공격 마법 전부를 덮는다). 다음: 그 두 공용 스크립트가 무엇을 요구하는지 읽고 원작 공격 마법을 붙여 한 개를 예측값으로 검증

- **[나중/균형]** 맨몸 방어가 +100(`100 - 수준/3`)이라 초보가 두 배로 맞는다 — 설계인지 손볼 값인지. 장비로 -70(바닥)까지 내려가고 1수준 무직도 100 → 7 이 된다. 체력 210억·4.1조인 `샌드백`·`노비스주민`·`봉인의주인` 은 안 죽는 장치라 사냥터 목록에서 구분해야 한다. **기술·마법이 끝난 뒤에 본다**

- **[남은 것]** 종류 3 워프 21줄(스크립트 포탈) · 정의 없는 NPC 95 중 상점 아닌 것 · 2차 직업 분류(`skill_i.tbl` 주석: 성직자→바드·전사→검투사·도적→궁수·마법사→소환사) · 맵 그림의 건물(벽 두 칸과 `seo.dat` 의 `.hea`)

- **[규칙]** **Hades 먼저, 팩은 나중.** `database/server/` → `database/archives/*.dat`(볼트: `python3 scripts/build-archive-vault.py`) → `data/game-data/` → 그래도 없으면 팩. 팩은 운영자가 손댄 사본이다

<!-- NEXT-ACTION:END -->

<!--
규칙:
- 이 마커 사이는 "지금/다음 할 일" 1~3건만. 짧게(화면 한 판).
- 완료된 항목은 여기 두지 말고 WORKLOG.md 의 ## History 로 옮긴다 (단일 출처·비대 방지).
- 훅(print_next_action.py)은 이 마커 사이만 세션에 주입한다.
-->
