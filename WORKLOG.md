# WORKLOG — LOD

세션/에이전트 간 핸드오프 로그. **"다음 할 일"은 여기 쓰지 않는다 → `NEXT.md`.**
긴 로그는 붙이지 말고 결과만 요약한다.

## Current State
- Status: in-progress
- Focus: 무도가 1~10 공격 기술을 실제 우드랜드 전투로 확인했다. 다음은 Hades 공용 원소 마법 스크립트 연결과 예측값 검증
- Last updated: 2026-09-15

## History (append; 최신이 위)
- 2026-09-17 — **5.99 장비 나머지 칸 416종을 들였다 — 상점에 없던 물건 103 → 17.** 사용자: "순수? … 하나씩 순서대로 진행하자".
  - 순수(`get_son`)는 99레벨 전직 때 원래 직업을 다시 고른 표시다(`Npc_Script.txt` 선진: `if(@class == 새 직업){set_son 1;}`). 전직 스크립트가 없어 지금은 아무도 순수가 아니므로 0 이 맞다.
  - `scripts/build-pack-equipment.py`: 무기·갑옷에 방패 Shield 3 · 투구 Helmet 4 · 귀걸이 Earring 5 · 목걸이 Necklace 6 · 반지 Generic 7 · 장갑 Generic 9 · 허리띠 Belt 11 · 각반 Generic 12 · 신발 Boot 13 · 장식 Generic 14(하데스표 템플릿의 ScriptName·EquipmentSlot 짝). 타입 0 만 장비로 본다(속성 3 에 염색약·두루마리가 섞여 있다). 그림이 일부러 안 보이는(안보이기 1) 무기 2종도. 장식은 7.18 겉모습의 OverCoat 를 채우는 스크립트가 없어 입은 모습으로 안 그린다.
  - 시험 `Pack599ArmorTests`: 레벨 99 캐릭터가 대지의룬스톤목걸이를 걸면 목걸이 자리 6 · 최대 체력 +1000, 가죽장갑은 팔 자리. 무기·갑옷 시험도 통과.
  - 아이콘 355개(13000번대 64개는 하데스 Legend.dat 에 없어 5.99 클라이언트 것). 방패·투구·신발 옷장 그림은 `build-client-wardrobe.py --새것만`.
- 2026-09-17 — **기술 사범이 5.99 스크립트 그대로 가르친다.** NPC 95명 중 기술을 가르치는 스크립트가 붙은 NPC 가 없어 운영자 명령 없이는 배울 수 없었다.
  - 5.99 사범은 표가 아니라 대화 스크립트(`Npc_Skill.txt`: `mes` → `menu` → `get_level`·`get_class` 확인 → `skill_add`). 기술 변환기(`build-pack-abilities.py` `Translator`)를 이어 쓰고 기다리는 세 곳만 바꿨다 — `mes 1` 과 `set @x, menu(…)` 는 `yield return`, `end` 는 `yield break`. 서버 fork `scripts/Pack599/PackNpc.cs` 가 답이 오면 멈춘 자리에서 이어 간다(`mes 0` 은 기다리지 않음).
  - `scripts/build-pack-npcs.py`: 29개 모두 옮김(서버 컴파일 시험 통과). `Pack599.Call` 에 skill_add·spell_add·skill_exist·skill_del·spell_del(…2 는 2차 직업용이라 같은 것으로), get_money·money_del, get_class_sub(=ClassStage), get_ability(=AbpLevel). `get_son`(순수)은 뜻을 몰라 0.
  - `tools/pack-import/import.py`: 팩 Spawn.txt 여섯째 칸(스크립트 이름)에 옮긴 스크립트가 있으면 `ScriptKey NPC_<이름>`. **함정: `--kind mundanes --write` 만 돌리면 같은 자리 상점 NPC 14장이 pack_speaker 로 덮인다 — 곧이어 `--kind shops --write` 를 돌려야 한다.** 결과는 사범 23장의 ScriptKey 만 바뀜.
  - 시험 `Pack599TeacherTests`: 레벨 11 전사가 가렌@밀레스마을#52,46 에게 "다음" 두 번 → 메뉴 숏블레이드 → 설명 두 번 → 배움(기술 이름은 `숏블레이드 (Lev:1/100)` 처럼 레벨이 붙어 온다). 대사 NPC 시험은 밀레스마을 NPC 가 모두 사범이 돼 노비스마을 멜로린으로 옮겼다.
  - 남은 것: 기술이 73번 칸에 들어간다(템플릿 `Pane` 비어 있음), 승급의 공간·화론 사범 6(맵 없음), 나머지 NPC 스크립트.
- 2026-09-17 — **5.99 물약·음식·귀환 주문서를 서버에 들여 쓸 수 있게 했다.** 상점 판매 목록 180종 중 131종이 서버에 없었고, 있던 뱀고기도 스크립트가 없어 "쓸 수 없다"였다.
  - 5.99 소모품은 칸으로 움직인다: `체력변화 +1000`(쓰면 회복) · `이동맵 노비스마을`+`이동좌표 40,33`(쓰면 이동). 서버 fork: `ItemTemplate` 에 `HealthRestore`·`ManaRestore`·`RecallArea/X/Y`(장비의 체력변화는 최대치 보너스라 칸을 나눔, `Item.ApplyQuality` 복사에도 더함), `scripts/Items/Consumable.cs`. 하나 줄이는 것은 기존 `Format1CHandler`.
  - `scripts/build-pack-consumables.py`: 물약 10 · 음식 7 · 귀환 21 = 38종(코마디움 둘은 부활 전용 스크립트, 아벨길드성은 맵 번호표에 없어 뺌). 쌓임 100. 아이콘 24가지(`dat-extract icon`).
  - 시험 `Pack599ConsumableTests`: 체력 1 로 들어가 하급체력포션 → 최대치까지 회복·하나 줄어듦, 노비스마을리콜 → 노비스마을(20373) 40,33. 스크립트 컴파일·상점 사기 시험도 통과. 남은 판매 목록 103종(장신구 56 …).
- 2026-09-17 — **내 체력·마력·레벨·금화를 화면에 보인다 — 기능 지도 2번.** 체력바가 고정값 `180 / 250` 이었다.
  - `GameScreen`: 위 줄에 `이름 Lv`, 체력·마력 막대 두 줄(글자 `HP 425/425` 와 함께 — 색만으로 읽히면 안 된다). 서버 `0x08`(`Vitals`)이 바뀔 때만 다시 쓴다. `PackPanel`: 금화 줄.
  - 배치 검사 12번(여섯 크기 × 소지품 닫음/열음) 통과 — 처음엔 금화를 소지품 창 머리에 넣어 세로 360 에서 20px 넘쳤다. 가장 긴 숫자(`LayoutCheck.PretendVitals`, Lv99 · 99999 · 금화 9억)로 잰다.
  - 라이브 서버 사진: `watch Lv6 · HP 425/425 · MP 325/325 · 금화 28,280`. 경험치는 아직 화면에 없다.
- 2026-09-17 — **NPC 가 화면에 보이고 눌리게 했다 — 괴물·NPC 그림 112종과 NPC 그림 번호.**
  - 원인 둘: 클라이언트는 그림이 없는 괴물·NPC 를 화면에 올리지 않아 누를 수도 없었고(`WorldView.Herd`, 그림 8종뿐), 서버 NPC 템플릿 95장의 `Image` 가 팩 숫자 그대로(괴물은 0x4000+번호)라 그림 파일을 못 찾았다. 상점 NPC 24장은 전부 1(말벌 그림)이었다.
  - 팩 NPC 그림 번호는 `mns` 번호와 같다(30 대장장이 · 39 망토 기사 · 239 초록 옷 여인 — 그려 보고 확인). `tools/pack-import/import.py`: NPC 는 0x4000 + 팩 `이미지`, 상점 NPC 는 팩 정의가 있으면 그 번호(베이가 35 · 시장마스터·카르마 31), 없는 10명은 상인 모습 31. 다시 돌려 94장의 `Image` 한 줄씩만 바뀐 것을 확인하고, 손으로 만든 전직사범(Class Chooser) 한 장은 29 → 16413 으로 고쳤다(서버 fork).
  - `scripts/build-client-creatures.py`(맥용, PS 단계 대신 + NPC 포함): 괴물 86 · NPC 26 = 112종 전부 하데스 `hades.dat` 에 있었다.
  - 확인: `NpcDialogueTests` 에 카르마 그림 번호 0x4000+31 확인을 더해 통과. 격리 서버에 클라이언트를 붙여 `--pick`(실제 화면 누르기 이벤트)으로 카르마가 보이고, 눌리고, 대화창이 열리는 것을 녹화로 봤다. 라이브 서버(25425)는 건드리지 않았다 — 다시 켜야 NPC 가 보인다.
- 2026-09-17 — **NPC 대화창을 모바일에 붙였다 — 기능 지도의 모바일 최대 막힘.** 사용자: "이건 더 중요한거부터 하자" · "하고 있는거야? 아니라면 진행해".
  - 알맹이: 0x2F 의 여덟 모양(선택지·선택지+값·글 입력·물건·소지품 칸·마법·기술·잊기)을 `Dialogue` 로 읽는다. 답은 `0x3A` — `ClientFormat39` 는 값을 ASCII 로 읽어 한글 물건 이름이 깨진다. 소지품 칸 창은 번호를 한 바이트만 싣고 서버는 0x05 → 0x0500 을 기다린다. 서버 닫기는 날것 `{0x30,0x00,0x0A,0x00}` 이라 0x00 이 순번 자리로 빠져 본문이 0x0A 로 시작한다(처음에 본문[1] 로 봤다가 실제 서버 시험에서 걸렸다).
  - 화면: `TalkPanel`(소지품 창 자리, 열려 있는 동안 월드 입력 막음), 상인을 누르면 `0x43`. 시험: 알맹이 `DialogueTests` 9개(모두 213 통과), 실제 서버 `NpcDialogueTests` — 카르마@노비스마을식당에서 Buy → 물건 목록 → 뱀고기 사기 → 소지품·금화 1000→650 → 닫기. 녹화도 같은 흐름(watch 를 식당으로 옮겼다가 원래 파일로 되돌림).
  - **찾은 막힘:** 그림을 안 뽑은 괴물·NPC 는 `WorldView.Herd` 가 화면에 추가하지 않아 **누를 수도 없다** — 식당 주인도 그렇다(녹화는 임시 코드로 누름). 상점 24곳 판매 목록 대부분이 서버에 없는 아이템(노비스 잡화 21 중 2)이고 shop1 문구는 영어.
- 2026-09-17 — **감정표현(몸 동작 9~17·23~44)을 원작처럼 머리 위 표정·말풍선으로 띄운다.** 사용자: "다음 진행해".
  - 2005 `Legend.exe`: 표 `0x869880`(한 줄 64바이트 — 번호·첫 칸·칸 수·칸마다 ms·채팅 글 "^^" 등)을 `0x4e1226` 이 앞 줄을 더해 찾는다. 앞모습·한가할 때만(`0x4e1104`), 첫 H 앞에 그린다(`0x4e7ca7`). 그림은 `Legend.dat` 의 `emot01.epf` — 칸 0~10 표정, 11~41 말풍선+표정. 02~04 는 얼굴 장식(안경·물안경) 쓴 사람용.
  - 칸 짝짓기는 Arbiter `BodyAnimation` 이름과 그림이 모두 맞는다(9 Smile = 미소 … 30 Love = 하트 … 44 Confused = 어지러움). 칸을 넘기는 줄은 못 찾았지만 표가 칸을 건너뛰고 그림이 차례를 이뤄 넘기며 튼다.
  - `Emote`(알맹이, 시험 22개 · 모두 204 통과), `Actor.Show`(앞모습·한가할 때만, 떠 있는 동안 다른 동작 무시, 머리 조각 바로 아래), `WorldView.Swings`. 그림 `scripts/build-client-emotes.py` → `assets/actor/emote.png` — 옷장 칸에 (+2, +2) 로 놓으면 표정 머리 윤곽이 남녀 몸 앞모습 머리와 한 점도 안 어긋난다.
  - 녹화 확인(전사 옷·칼·방패): 동·남에서 미소가 머리카락 아래 얼굴에 맞게 겹치고, 코 방울이 커졌다 작아지고, 점이 하나·둘·셋, 하트 말풍선이 머리 위에 뜨고 끝나면 얼굴이 돌아온다. 북·서에서는 안 뜬다.
  - 아직: 우리 클라이언트에 감정표현을 보내는 버튼(0x1D)이 없다 — 지금은 남이 보낸 것만 보인다.
- 2026-09-17 — **몸 동작 6·21·22(손 들기·키스 날리기·손 흔들기)를 원작 클라이언트대로 그린다.** 사용자: "동작 6 진행".
  - 2005 `Legend.exe`(= 5.99 클라이언트) 역어셈블: 동작 → 상태(`0x4e1020`), 상태마다 시작·칸 수 초기값(`0x4e3657`), 칸 = `시작 + (칸 수 − 1) × 앞뒤 + 걸음 − 1`(`0x4e2383`~`0x4e2526`, 저장된 칸 수는 그림 수 + 1). `03` 파일: 6 → 0 | 1, 21 → 2–3 | 4–5, 22 → 6–7 | 8–9(속도 ÷3). 전에 문서에 적은 "감정표현 0–4 | 5–9" 는 틀렸다.
  - `BodyMotion.Of` 에 6·21·22, `Faster` 칸(손 흔들기 3). `Actor.MotionSheet` 가 03 동작 동안 방패·무기·무기 앞 조각을 뺀다(`0x4e84e0` 부위 0·9·10·12).
  - 옷장 `03` 그림 634장(`build-client-wardrobe.py --끝=03`, 방패·무기 조각은 안 뽑음). 전사 옷 2 · 칼 76 · 방패 5 로 네 방향 녹화 — 동작 동안 칼·방패가 빠지고 끝나면 돌아온다. 알맹이 시험 182 통과.
  - 아직: 감정 말풍선(9~17·23~33, 앞모습일 때만 `emot01.epf`).
- 2026-09-17 — **원작 클라이언트 역어셈블(윈도우 세션) 결과를 합치고 모바일에 옮긴다.** 사용자: "작업끝나서 push했어" · "진행해".
  - `docs/disassembly-original-client` 를 앞으로 감기로 합쳤다. 2005 원작 Legend.exe = 5.99 클라이언트(md5 같음), 5.99 무기 그림 27개는 팩이 바꾼 것, 기술 동작은 갑옷이 skill.tbl ST 에 있어야 재생, 이펙트는 effect.tbl 순서.
  - 5.99 갑옷 옷장 그림 293부위(갑옷·팔) · 아이콘 217장.
  - `BodyMotion.Fits`: 원작 2005 `skill.tbl` 을 알맹이에 내장(하데스 사본은 줄마다 348~352 한 번호가 더 있어 안 씀). `WorldView.Swings` 가 옷이 안 맞는 기술 동작을 안 그린다. `Actor.Play` 는 원작처럼 동작 중에 온 새 동작을 무시한다(앞서 "나중 것이 대신" 으로 바꿨던 것을 되돌림 — 하데스가 평타 묶음 기술을 한꺼번에 보내면 첫 동작만 보인다).
  - `EffectSheet`·`Flash`: `effects.txt` 가 `번호 칸수 순서…`. 이펙트를 빈 칸 자리표시 규칙으로 다시 뽑았다(102 는 15칸 중 12칸 사용). EFA(232 이상) 40개는 effect.tbl 줄이 `0` 뿐이라 순서를 뺐다.
  - 아직: 동작 6·21·22·감정 번호의 원작 칸 구간(발췌에 없음).
- 2026-09-17 — **기능 지도를 만들었다 — 39개 기능을 서버·모바일·원작 근거로 나눠 파일:줄까지.** 사용자: "싹다 확인을 해서 구성을 전부 파악할 수 있도록해".
  - 방법: 읽기 전용 탐색 두 갈래(서버 / 모바일·원작, sonnet) → 센 주장 7개를 직접 다시 열어 확인. 결과 `docs/feature-map.md`.
  - 서버는 30개가 돌아간다(교환·파티·게시판·상태 이상·능력치 올리기·소모품까지). 없는 것은 문(0x32 빈 자리)뿐이고, 틀만 있는 것은 길드·PvP·2차 직업 승급·표지판이다.
  - 모바일은 됨 6. **NPC 대화창이 통신만 있고 화면에 안 이어져** 상점·은행·배우기·퀘스트·전직이 다 막혀 있다. 체력바는 고정값 `180 / 250`.
- 2026-09-17 — **원작 클라이언트 두 판을 역어셈블했다(작업지시서 결과, 윈도우).** 사용자: "lodr4.51.exe · lodr.exe 역어셈블 니가 해서 푸시해" · "graphify 적용 … archify로 보여줄래 설명은 아주 쉽게".
  - 설치 파일은 InstallShield 6 이라 Docker 의 `unshield` 로 풀었다. **`lodr.exe`(2005) 의 Legend.exe 는 5.99 클라이언트와 md5 까지 같다** — 5.99 에서 찾은 클라이언트 규칙이 원작 규칙이다. 새로 본 것은 `lodr4.51.exe`(2001) 판.
  - 4.51 은 사람 그림 12부위(`SBNLHUDCHAWS`), 네 방향 모두 무기가 팔 위, 모든 조각 x+27. 방패·감정표현·뒤집기·EPF 머리말은 5.99 와 같다.
  - **기술 몸 동작은 입은 갑옷 번호가 `Skill.tbl` ST 에 있어야 재생된다**(두 판 모두). 없으면 아무 동작도 안 한다. 방패 검사는 클라이언트에도 없다. 우리 `BodyMotion` 은 옷을 안 본다.
  - 원작 2005 `khan.dat` 무기 197/200 이 하데스와 바이트까지 같다 — 5.99 의 무기 그림 27개는 팩이 바꾼 것.
  - `findings.json`(규칙 34) · 발췌 `original-*` · `docs/disassembly.md` "원작 클라이언트" 절 · 그래프 노드 310 · 쉬운 그림 `docs/disassembly-original-client.html`(archify, 원본 `.archify.json`).
- 2026-09-17 — **5.99 갑옷 225종을 서버 템플릿으로 들이고 도복 규칙 둘을 옮긴다. 역어셈블 작업지시서를 쓴다.** 사용자: "진행해" · "역어셈블 … 정리를 좀 해서 graphify해" · "일단 현재까지 작업 내용이랑 역어셈블 작업지시서 만들고 푸시해".
  - `scripts/build-pack-weapons.py` → `build-pack-equipment.py`: 속성 1(갑옷)도 옮긴다(치장옷·드레스·길드옷 포함). `성별제한` 1 남 · 2 여(마스터아머1 ↔ 마스터아머2). 무기·갑옷 336종, 이미 있던 같은 이름 118개(앞서 넣은 무기와 팩드롭 갑옷 7종)는 덮었다.
  - 도복(공격모션 132): 무기 없이 치면 주먹(`Assail.BlowMotion` 이 갑옷 칸을 본다), 신발과 함께 못 입는다 — `Armor.cs`·`Boot.cs`, 「신발이 불편하여 입을수가 없습니다.」(Novaonline.exe 0x41cc49·0x41d387). `Pack599ArmorTests` 통과 — 신발 검사를 빼면 "도복 차림에 신발을 막지 않았습니다" 로 실패한다.
  - 아직: 5.99 갑옷 번호의 옷장 그림(갑옷·팔)과 아이콘을 안 뽑았다(`build-client-wardrobe.py --새것만`). 상점·드롭도 아직.
  - 역어셈블 정리(`docs/disassembly.md` · `data/disassembly/`)와 그래프(`scripts/build-disassembly-graph.py`)는 에이전트가 만들었다. 원작 클라이언트 실행 파일은 윈도우 PC 에만 있어 **작업지시서 `docs/disassembly-work-order.md`** 를 남겼다 — 브랜치 `docs/disassembly-original-client` 로 푸시하게 했다.
- 2026-09-17 — **평타 몸 동작을 5.99 서버 규칙대로 장비에서 고른다.** 사용자: "니가 말한 내용이 맞을거 같다" (기억한 "방패면 기본 모션·양의신권이면 주먹" 대신 역어셈블 결과를 따른다).
  - 하데스 `ItemTemplate` 에 `AttackMotion`·`AttackSpeed` 칸, `Item.ApplyQuality` 복사, `Assail.BlowMotion`: 무기 → 갑옷 → (1, 20), 갑옷만이고 둘 다 0 이면 속도 22. 공격모션이 없는 하데스 무기는 하데스가 하던 대로(전사 양손 0x81).
  - `build-pack-weapons.py` 가 5.99 무기에 두 칸을 채운다(설단검 134·18, 아스카론 129·37, 활 142 …).
  - `Pack599WeaponTests.A_blow_with_a_599_weapon_moves_the_way_the_weapon_says`: 맨손 (1, 20), 도적이 설단검을 끼면 (134, 18) — 고치기 전에는 (1, 20) 으로 실패했다.
  - 남은 것: 공격속도가 뜻하는 평타 잠금(속도×10ms)은 하데스 평타 간격을 건드리지 않으려고 안 옮겼다. 도복 주먹(132)은 5.99 갑옷을 들여야 나온다.
- 2026-09-17 — **5.99 무기 111종을 서버 템플릿으로 들이고, 장비를 원작 클라이언트 순서로 겹친다.** 사용자: (나) 5.99 무기를 서버에 · "장비가 제대로된 위치에 착용되면 착용한거처럼 보이는게 맞잖아" · "무도가만 유독 안맞네".
  - `scripts/build-pack-weapons.py`: 5.99 아이템의 `속성` 이 장비 자리다(0·12·13 무기, 1 갑옷, 2 방패, 3 투구 … 10 신발, 11 장식) — 폴더로는 못 가린다(`전사방어구.txt` 에 도끼·곤봉). 무기 111종, 아이콘은 `0x8000 + 이미지`(5.99 설단검 91 = 하데스 Dirk 91), 직업 번호는 하데스 `Class` 와 같다. 하데스 무기는 남겨 두고(사용자), 상점·드롭은 나중. 팩드롭으로 들어와 있던 5종은 그림·아이콘 번호가 뒤바뀌어 있어 덮었다. 예전 커밋(5383f7f4a)은 "아이템은 하데스 것" 이었는데 이번은 사용자 지시로 반대다. `Pack599WeaponTests`: 운영자가 에페를 주면 아이콘 87칸으로 들어오고 끼면 겉모습 무기 번호 2.
  - **그리는 순서표를 5.99 `Legend.exe` 에서 찾았다**(에이전트 역어셈블, `0x69c200`). 방향으로만 정해지고 동작과 무관. 앞모습은 무기가 맨 아래, 뒷모습은 몸 위·팔 아래. 원작은 무기를 깔고 몸 앞에 올 조각(`p`, 무기 번호)을 따로 위에 그린다. 팔 `a`(갑옷 번호)·머리 앞뒤 `e`/`f` 도 있다. `Wardrobe.Rank` + `Actor.Face` 로 옮겼다.
  - **무도가 너클의 앞 조각이 27px 옆에 떴다** — `mp127` 은 칸 위치가 `mw127` 과 같고 바탕도 111. 도구가 111 바탕을 `w`·`c` 에만 줬다. `p` 도 넣었다.
  - **우리 5.99 클라이언트의 무기 27개 번호는 그림이 다른 무기다** — 5.99 팩 아이템 이름에는 하데스 그림이 맞는다(3 커틀라스=칼, 6 단검 4종=단검(5.99 쪽은 가시 곤봉), 7·8 지팡이, 11 매스케이드=Masquerade). 옷장 생성기는 무기·앞 조각만 하데스 우선. 갑옷은 같은 옷을 다시 그린 것이라 5.99 우선 유지.
  - 확인: 전사(레더튜닉·아스카론·가죽방패) · 도적(옷 4·설단검) · 무도가(도복·너클 127)를 클라이언트에서 임시로 입혀 네 방향 녹화 — 무기가 손에 쥐어지고 방패는 방향대로 앞뒤, 떠 있는 조각 없음.
  - **평타 몸 동작 규칙(5.99 서버 프로그램 역어셈블, 에이전트):** 무기를 꼈으면 무기의 `공격모션`·`공격속도`, 아니면 갑옷의 것, 둘 다 없으면 (1, 20). 방패·직업·양의신권은 보지 않는다(두 이름은 실행 파일에 없다). 도복(132)이면 무기 없이 주먹. 공격속도는 보내는 속도이자 그만큼(×10ms)의 잠금. 사용자 기억("방패를 끼면 기본 모션, 양의신권이면 주먹")과는 다르다 — 서버에는 없고 클라이언트 쪽은 미확인. 하데스 템플릿에는 `공격모션` 칸이 없어 아직 안 옮겼다.
- 2026-09-17 — **옷장 전체(갑옷·무기·투구·방패·신발)를 뽑아 사람에게 입힌다.** 사용자: 다음 작업 1번(옷·무기 그림) "진행해" · "도적 무기 모션 안맞잖아 그리고 무도가도 무기있어".
  - `scripts/build-client-wardrobe.py`(옛 `build-client-motions.py`): 서버 아이템 템플릿의 `ScriptName` 이 Armor·Weapon·Helmet·Shield·Boot 인 것의 `Image` 가 입은 번호(성별은 `Gender`). 부위 333개 · 그림 1,419장(서기·평타·직업 동작), 5.99 한국 클라이언트 우선. 하데스 서버의 장비는 모두 영문 하데스 아이템이다 — 5.99 장비는 아직 서버에 없다.
  - **칸 80x88 → 120x96.** 무기가 가로 114 · 세로 89 까지 뻗어 무기 파일 129개가 안 들어갔다. 도구가 칸 왼쪽 위에 맞춰 그리므로 발 자리(31.5, 83)는 그대로다.
  - **무기가 손에서 27 픽셀 떨어졌다.** 도구가 EPF 머리말의 바탕 크기로 가운데를 맞췄는데, 칼 `mw002` 머리말은 20x18(실제 111x85)이다. 참고 클라이언트 아틀라스를 세어 보니 무기·장신구는 전부 111x85, 나머지는 전부 57x85 — 부위 글자로 정하게 고쳤다.
  - **도적 단검 불꽃이 손을 떠났다(사용자 지적).** EPF 의 빈 칸(크기 0)을 읽기 코드가 버려 뒤 칸 번호가 당겨졌다 — `mw028e` 는 2·7번이 빈 칸. 자리표시로 남기게 고치고(`Epf.cs`) 옷장 전체를 다시 뽑았다. 이펙트·아이콘도 같은 읽기를 쓰므로 다시 뽑으면 번호가 원작과 맞는다(아직 안 뽑음). `Mpf.cs`(괴물)에도 같은 모양이 있다 — 미확인.
  - 5.99 여자 아카이브(`khan2.dat`)에 색표가 하나(`palb00`)만 있어 남자 아카이브 색표를 안 열던 것도 고쳤다.
  - 방패는 방향에 따라 겹치는 순서를 바꾼다(등 → 몸 뒤, 앞 → 맨 위). 기술 동작 중에는 방패 파일에 동작이 없어 그리지 않는다.
  - 확인: 전사(옷 2 · 칼 2 · 나무 방패 1) · 도적(옷 4 · 단검 28) · 무도가(옷 3 · 손톱 117)를 클라이언트에서 임시로 입혀 평타와 직업 동작을 네 방향 녹화 — 무기가 손에 붙고 궤적·불꽃도 무기에 붙는다.
  - 서버가 무기를 1바이트로 보내(`ServerFormat33`) 번호가 255 를 넘는 무기 4종(280·612·647·665)은 전달되지 않는다. 원작 아카이브에도 그 그림은 없다.
- 2026-09-17 — **기술·마법 몸 동작을 서버가 보낸 번호대로 그린다.** 사용자: "모션도 방향 있지 않나?" · 마법은 돌지 않는다(나) · "전사, 도적 모션도 찾아봐".
  - 동작 번호: `1` 평타(`02`), **`128 + NO`** 가 원작 `skill.tbl` NO 줄(파일 글자 b~f · 시작 · 칸 수). Arbiter·ETDA 의 이름표와 같다. `BodyMotion` 에 18줄을 두고 "파일마다 빈틈없이 한 번씩 덮는다" 를 시험한다. 속도는 동작 전체를 1/100초로 읽는다(하데스 평타 30 이 그동안의 0.14초/칸과 맞음 — 적힌 곳은 없다).
  - 하데스는 **평타를 누르면 평타 묶음 기술까지 한꺼번에** 돌려 1 → 131 → 133 이 한순간에 온다. 동작 중이면 새 동작을 버리던 것을 "나중 것이 대신" 으로 바꿨다(내가 누른 평타 겹치기만 다시 시작 안 함). 기술 버튼의 미리 휘두르기는 뺐다 — 무슨 동작인지는 서버만 안다.
  - 캐릭터 몸의 불꽃은 **괴물 평타 그림(203)** 이었다 — 90초 기록 68개 전부 괴물→나. 하데스는 괴물에게도 `assail` 템플릿(그림 203)을 쓴다. 5.99 기본공격은 `effect @target, 0, 0, 75` 로 그림이 없다(괴물 평타는 엔진 안이라 모름).
  - **바지·갑옷 그림이 하데스 아카이브와 5.99 한국 클라이언트에서 다르다.** 몸·신발·머리는 같다. 하데스 바지 `mn001c` 는 14칸뿐이라 전사 139~141 에서 바지가 사라졌다. `scripts/build-client-motions.py` 가 부위마다 한 아카이브(5.99 우선)에서 01·02·b~f 를 뽑는다.
  - **직업 동작은 직업 의상에서만 온전하다**(사용자). `skill.tbl` ST 가 동작별 옷 목록이다. 전사 옷 2 · 도적 옷 4 를 입혀 녹화하니 129·130·134·135 가 네 방향 온전했고, 139(검투사)·142(궁수)는 그 옷 목록이 아니라 맨몸 — 원작대로. 5.99 아이템 `직업제한` 1 전사 · 2 도적 · 3 마법사 · 4 성직자 · 5 도가.
  - **서버가 캐릭터 파일을 ASCII 로 읽어 한글을 전부 `?` 로 바꿨다**(`AislingStorage.Load`). 5.99 한글 기술은 다시 들어오면 `????` 가 되어 사라졌다 — watch 계정에서 재현. `CharacterSaveTests` 로 재현(RED: `???? (Lev:1/100)`) 후 고쳤다. 실제 `monk` 캐릭터에는 한글 기술이 없어 피해가 없었다.
  - 격리 시험 둘(`World_says_which_map…`, `Freshly_created_character…`)이 Safe House 대신 우드랜드에서 시작해 실패한다 — 로컬 서버 설정 `StartingMap: 20015` 를 시험이 복사해 가서다. 코드 문제 아님.
  - `--say '/skill "단각" 1'` 로 배운 기술이 `??` 였던 것도 위 ASCII 버그였다(배울 때는 맞고, 다시 들어올 때 깨짐).
- 2026-09-17 — **괴물이 방향대로 그려지고, 때릴 때 상대 쪽으로 돈다.** 사용자: "걷기 때처럼 모션도 방향 있지 않나?"
  - 괴물 시트는 구간마다 등 보이는 N장 + 앞 보는 N장인데 `CreatureMotion` 이 앞 벌만 골라, 좌우만 뒤집힌 채 늘 뒷모습이었다. 방향(`Side`)을 받게 했다 — 방향을 무시하게 되돌리면 시험 3개가 실패한다.
  - 서버는 괴물이 때리기 직전 `0x11`(돌기)을 보내는데 클라이언트가 받지 않았다(보내기만 했다). 이제 읽는다.
  - 녹화 프레임으로 확인: 말벌이 오른쪽·왼쪽 어느 쪽에서 오든 플레이어 쪽으로 머리를 두고 쏘고, 플레이어도 말벌 쪽을 본다.
  - 아직: 기술·마법의 **몸 동작**(0x1A 의 동작 번호 — 직업별 `b~f` 파일)은 안 그린다. 기술을 써도 평타 휘두르기만 나오고, 마법은 동작이 없다. 마법을 멀리 있는 대상에게 쓸 때 돌아서는지는 사용자에게 묻는 중.
- 2026-09-17 — **모바일 화면에서 괴물이 걷고, 기술 이펙트·소리가 나온다.** 사용자 질문 "몬스터도 모션 제대로 적용돼서 이동하는 거지?" 에 답이 "아니다(순간이동)" 여서 붙였다.
  - 걷기: 서버는 지금 칸만 준다. `Actor.GoTo` 가 한 칸 차이면 내 캐릭터와 같은 시간(`StepSeconds`)으로 미끄러지며 걷는 프레임을 넘기고, 더 멀면 그냥 선다. 매 프레임 `Face` 를 부르던 것이 서 있는 그림으로 되돌려 걷는 동작을 지우고 있어 방향이 바뀔 때만 부른다.
  - 이펙트: **5.99 통역이 0x29 의 그림 순서를 거꾸로 보내고 있었다.** 원작 클라이언트는 첫 그림을 첫 번호(맞는 쪽)에 그리는데 하데스 `ServerFormat29` 인자 이름이 `CasterEffect` 먼저라 헷갈렸다. `Pack599AbilityTests` 에 "플레어 102 가 표적 위에" 를 넣고 되돌려 빨개지는 것을 봤다.
  - 그림: 232 번부터는 `roh.dat` 에 없고 5.99 한국 클라이언트에만 EFA 형식으로 있다 — `dat-extract efa` 를 더했다(zlib RGB565 + 밝기로 투명도). 뽑기는 `scripts/build-client-effects.py`(이펙트 102 · 소리 165).
  - **소리가 한 번도 안 났다.** 하데스는 맞는 소리를 0x19 가 아니라 체력바(0x13) 끝 바이트로 보내는데 클라이언트가 그 칸을 버렸다. 이제 둘 다 받는다(0·255 는 없음).
  - 확인은 사진 한 장으로 안 돼서 `--write-movie` 로 사냥 40초를 프레임으로 떨궈 봤다 — 괴물이 칸 사이를 걸어오고 맞을 때 베기 그림(203)이 괴물 위에 뜬다. 35초 사냥에서 이펙트 30번·소리 94번(임시 로그, 지웠다).
  - 맥 `LoginScreen` 이 매 프레임 "Virtual keyboard not supported" 를 찍는 것은 원래 있던 것이다(데스크톱에 화면 키보드가 없다).
- 2026-09-16 — **5.99 전 직업 기술·마법을 스크립트 변환으로 넣기 시작했다.** 사용자 지시: "하데스에 구현된 게 몇 개 없으니 5.99 스크립트 기반으로, 5.99 기준으로 완성". 모양마다 정규식을 붙이던 무도가 방식으로는 157블록을 감당할 수 없어, 5.99 문장(`set`·`if`·`for`·`goto`·계산식)을 C# 으로 옮기고 명령만 통역(`Pack599.Call`)으로 보내게 했다.
  - 5.99 원본 흠 두 가지를 넘었다: 줄 맨 앞의 `}`·남는 닫는 괄호(괄호 짝으로 자른다), `else { go: … }` 로 뛰어드는 `goto`(표시를 켜고 `if` 앞으로 간다). 오류 하나가 모든 스크립트를 막으므로 `ScriptCompilationTests` 로 매번 확인했다.
  - 명령 뜻은 팩이 쓰는 모양에서 읽었다: `enare`·`focus` 는 긴 시간이면 걸고 1·0 이면 확인, `magic 1` 은 세기(15~60)만큼 방어를 나쁘게 하는 저주(렌토~어둠의각인 — 원작 cradh 넷과 같은 줄), `immortal` 은 dion. 프라보는 피해 마법이 아니라 저주였다.
  - **공격력·마법 공격력·치명타 식을 5.99 서버 프로그램(Novaonline.exe)에서 역어셈블로 읽었다.** 팩 자료·다른 팩·카페 어디에도 식이 없어서다. 캐릭터 능력치 재계산 함수(`0x45d4xx`)가 장비 14칸을 돌며 캐릭터 칸 0x8C·0x98·0x9C 를 채운다:
    - 공격력 = Σ장비 (최소공격력1+최대공격력1)/2 + 주먹단련(Lev1) +10 + **힘×10** + 무도가 레벨 보너스(≤20 +25 · ≤50 +70 · ≤80 +210 · 그 위 +260) → 포만도 50↓ ×75% · 25↓ ×50% → 에나르마(0x156) ×1.3 · 수페라에나르마(0x158) ×1.6 → 피닉스모드(0x7A) +30% · **소수신공(0x7C) +40%**
    - 마법 공격력 = Σ장비 (최소공격력2+최대공격력2)/2 + **지능×10** → 포만도 보정 → ×1.3 · ×1.6. `공격력2` 가 마법 쪽이다(매직마르시아 물리 0·마법 7~12).
    - 치명타(%) = Σ장비 크리티컬 + 명중률향상(Lev1) +10 · (Lev2) +20, 100 에서 자르고 + 민첩÷11.
    - 그동안 무도가가 쓰던 `힘×4 + 민첩×2 + 무기` 는 하데스 평타 식이었다. 무도가(`MonkStrike`)와 5.99 변환(`Pack599`) 모두 이 식으로 바꿨고, 소수신공은 Claw Fist 대신 공격력 +40% 다. 하데스에 포만도·장비 마법 대미지·장비 크리티컬이 없어 그 부분은 0 이다.
  - 사용자가 뜻을 알려 준 명령: 코마디아(빈사 풀기 — 하데스 skulled) · 리베라토(효과 지우기, 저주 제외) · 센스·센스몬스터·품뒤져보기(대상 정보) · 칸의눈(마법 방어 20%, `magic 5`) · 포효(바투, `magic 6`) · 어둠의각인(프라보보다 강한 저주, `magic 8`).
  - **괴물 마법도 옮겼다.** 괴물 시험에서 서버가 통째로 죽는 버그를 잡았다 — 값 타입 `V` 의 글자 더하기를 `a.ToString() + b` 로 썼더니 C# 이 문자열 더하기 대신 `V` 의 연산자를 다시 골라 끝없이 불렀다(스택 넘침). 5.99 스크립트가 `object_name() + "가(이) …"` 처럼 글자를 잇는 순간 서버가 죽는다. 앞선 기술 시험은 우연히 그 줄을 안 지났다. 괴물 AI 가 예외를 삼키고 프로세스는 로그 없이 죽어서, 괴물 AI 와 통역에 임시 로그를 넣어 "명령 object_name 다음에 끊긴다" 까지 좁혀 찾았다(임시 로그는 지웠다).
  - 시험에서 메테오가 "마력 0" 을 못 본 것은 자연 회복이 곧바로 채워서였다 — 쓰는 조건(20000) 아래로 떨어졌는지로 본다.
- 2026-09-16 — **5.99 팩의 무도가 기술 19개를 전부 넣었다.** 남았던 일곱(구양신공·늑대의위상·마구때리기·일음지·발경·소수신공·이형환위)에 모양을 하나씩 더했고(`MonkStrike`), 생성기가 팩 블록에서 숫자를 읽어 찍는다.
  - **체력 명령을 거꾸로 읽고 있었다.** `set_vital X` 는 체력을 X 로 **맞춘다** — 팩이 `set_vital get_vita(@myid) - get_basevita(@myid)/100*10` 처럼 뺄셈을 직접 적고 `set_vital 1` 도 쓴다. `get_vita` 는 `set_vital` 이 쓰는 자리(0xCC)를 읽으니 **현재** 체력, `get_basevita`(0x90)가 최대 체력이다. 그래서 달마신공은 "최대체력 30% 로 때리고 그만큼 잃는다" 가 아니라 **현재 체력 30% 로 때리고 내 체력을 그 값으로 맞춘다**. 게다가 그동안 달마신공은 `Use(…, 0, 0, …)` 로 넘겨 **피해가 1** 이었다.
  - 디버프 짝은 팩 NPC 대사가 알려 줬다: 일음지 「눈을 멀게」→ `debuff_blind` 10초, 발경 「스턴」·「몸이 얼어」→ `debuff_frozen` 15초(전사 기술도 괴물엔 `magic 7`, 사람엔 `mobsor_delay` 를 같은 자리에 쓴다), 소수신공 「주먹을 강화」→ `buff_clawfist` 120초. 하데스 디버프 길이는 클래스에 박혀 있어 `Timer.Tick` 을 당겨 맞췄다.
  - 허공답보는 이형환위와 같은 두 칸 건너뛰기에 `공격력×50 + 최대체력 + 지구력×100` 한 방이다(사용자 확인). 전에는 건너뛰기와 `+최대체력` 이 빠져 있었다. 마구때리기는 사용자 지시대로 5.99 의 ×5000 을 썼다(Nova 는 ×4000).
  - **생성기가 원작 템플릿을 팩으로 덮고 있었다.** 앞 커밋에서 연환포의 요구 조건(44레벨·지능 17·민첩 30)이 지워졌고, 이번에 구양신공(99레벨·2차)과 이형환위(3레벨 …)도 지워질 뻔했다. 이제 있는 템플릿은 네 칸만 바꾼다.
  - **원작 여부를 사용자에게 물었다.** 원작 아카이브·MetaFiles 에는 한글 이름이 없어 자료로는 못 가린다(사람이 채운 `data/기술마법-한글이름.tsv` 에 무도가 7개뿐). 답: 허공답보·백보신권·정권·발경·소수신공·무영신공·파천각·연천단각·마구때리기는 원작, **붕신선각은 원작에 없다**(지우지 않고 `원작없음` 표시). 팩 NPC 로 보니 마구때리기·늑대의위상은 "2차승급기술", 연천단각은 승급 후 단각을 바꾸는 상위 기술이다.
  - 마력 소모를 넣었다(`MonkStrike.Spend`, 11개). 정권은 팩이 `주먹단련` 유무로 갈라 ×2.5·25 / ×2.0·15 인데 하데스에 그 마법이 없어 뒤쪽이 맞다 — 그동안 ×2.5 였다. 치명타는 **확률이 팩 자료에 없어** 넣지 않았다. 마나를 빼지 않게 망가뜨리면 시험이 "기대 920, 실제 1000" 으로 실패한다.
  - 관리페이지 기술·마법 탭에 구현/미구현 칩. 옛 셈(영문 이름과 같은 `[Script]` 가 있다)은 65 였지만 하데스는 템플릿 `ScriptName` 으로 붙이므로(`Skill.cs:70`) 실제로 도는 것은 **26** 이다.
  - 사용자 지시로 **정권을 뺐고**(스크립트·템플릿 삭제, 생성기 `EXCLUDED`) **단각의 마력 소비를 없앴다**(원작은 안 쓴다, `NO_MANA`).
  - 소수신공을 5.99 에 맞췄다(사용자 지시): 성공·거절 문구와 배우는 레벨(생성기가 5.99 NPC 의 `get_level(@myid) < N` 검사를 읽는다 — 메뉴 글자가 아니다, 연환포는 메뉴 75·검사 74). 이 생성기가 만든 템플릿에만: 백보신권 87 · 발경 93 · 소수신공 70. 단각(팩 31)·붕각(59)은 원작 Kick(1)·Martial Awareness(10)와 같은 기술이라 덮지 않았다. 세기는 5.99 자료에도 없어 Claw Fist 그대로다. 하데스에 2차 직업은 틀만 있다 — 첫 전직이 곧바로 `Master` 로 만들어 2차 기술이 레벨만으로 열린다. 2차 직업은 나중에 한다(사용자 결정).
  - **범위를 잘못 옮기고 있었다.** 백보신권·무영신공은 팩이 `get_side` 로 방향을 잡고 **앞 세 칸 일직선**을, 선풍각·파천각은 방향 없이 **둘레 네 칸**을 친다 — 전부 앞 한 칸으로 들어가 있었다(처음엔 "네 방향 여러 칸" 이라고 잘못 적기도 했다). 시험: 표적을 등지고 선풍각, 세 칸 물러서서 백보신권. 범위를 앞 한 칸으로 되돌리면 둘 다 실패한다. 걸은 뒤에는 서버가 걷는 사람에게 위치를 안 알려 줘 `RefreshAsync` 로 물어야 한다.
  - **"42개를 연결했다" 는 틀린 보고였다.** 42개는 전부 마법이고, 하데스는 마법을 `ScriptName` 이 아니라 `ScriptKey` 로 붙인다(`Spell.cs:37`) — 42개 모두 원래 `ScriptKey` 로 붙어 있었다. 관리페이지가 마법의 `ScriptKey` 를 안 봐서 미구현으로 셌을 뿐이다. 붙였던 `ScriptName` 은 되돌리고 연결 스크립트는 지웠다. 페이지는 기술 `ScriptName` · 마법 `ScriptKey` 로 센다 — 구현 69.
  - 시험 `Every_599_monk_skill_that_is_not_a_plain_blow_does_what_its_pack_script_says` — 처음엔 30% 를 31% 로 바꿔도 통과했다(새 캐릭터 체력 60 → `60/100*30 = 0` → 둘 다 1). 체력을 1000 으로 두고, 서버 쪽 변이 셋(달마 31% · 구양 3% · 실명 표적 못 찾음)이 모두 실패하는 것을 확인했다.
- 2026-09-15 — **무도가 1~10 공격 진행선을 구현하고 우드랜드에서 실제로 굴렸다.** Hades 기존 식만 사용해 `Kick`(Str×4+Dex×2) · `High Kick`(Str×5+Dex×3) · `Poison Punch`(Str×4+Con×2+독) · `Sting`(Str×4+Dex×2)을 추가하고, 앞칸 판정·훈련·은신 해제·피해/모션 보고는 공통 처리했다. 기존 `Double Punch`를 포함한 다섯 기술의 학습 조건은 바꾸지 않고 `ScriptName`만 연결했다.
  - 격리 Hades를 `우드랜드1-1`에 띄우고 10레벨 무도가가 `Kick` 5 · `High Kick` 4 · `Double Punch` 6 · `Poison Punch` 10 · `Sting` 1을 모바일 프로토콜로 직접 사용해 다섯 번 모두 몬스터 체력 보고를 받았다. 런타임 스크립트 전체 컴파일도 통과했다.
  - 서버 프로젝트 빌드 성공(오류 0), 관련 격리 시험 7개 통과, 모바일 코어 131개 통과. 전체 characterization은 76개 중 74개 통과했고, 기존 포크가 괴물 템플릿의 명시 수치를 보존하도록 바뀐 뒤 옛 레벨 파생값을 기대하는 `CombatSmokeTests` 2개가 그대로 실패한다. 이번 무도가 변경의 회귀는 아니다.
- 2026-09-14 — **NovaOnline을 셋째 서버팩으로 수집하고 Obsidian/Graphify 비교 자료로 만들었다.** `sources/novaonline`은 소스 저장소가 아니라 32비트 Windows 네이티브 서버 실행파일과 CP949 DB가 든 로컬 런타임팩이다(C/C++ 여부는 확정 불가). `db/**/*.txt` 350개만 UTF-8로 옮기고 출처·인코딩·실행파일 SHA-256을 기록했다. 정규화 결과는 아이템 3,508 · 괴물 607 · NPC 409 · 기술 155 · 마법 156 · 맵 748 · 워프 4,370 · 스크립트 467, 퀘스트 영속변수 48, 엔진 명령 322. vault 7,004장, Graphify 7,003노드·10,353간선. Windows의 `uv tool` Graphify 탐지도 고쳤다.
  - 셋 모두 이름이 같은 것은 괴물 22 · 아이템 357 · NPC 2 · 기술 49 · 마법 44 · 맵 81뿐이고, 그중 그림까지 맞는 것도 각각 16 · 320 · 1 · 40 · 38이다. 이름만으로 합치지 않는다. 특히 5.99와 Nova는 같은 NovaOnline 계보일 수 있어 둘의 합의를 독립 표 2개로 세지 않는다. 다음은 N팩 비교 JSON과 충돌 감사표이며 Hades에는 아직 쓰지 않았다. 상세: `docs/server-packs/novaonline.md`.
- 2026-09-13 — **범위를 넘겼다. 사용자가 "뭘 하려고 이렇게까지 하느냐" 고 물어 멈췄고, 세어 보니 방향도 틀렸다.**
  - 원래 일은 "이식한 기술·마법이 실제로 도는지 확인" 이었다. 예측값을 맞추다 나온 셋(방어 공식·기술 레벨·괴물 수치)은 **예측이 안 맞아서** 어쩔 수 없이 나온 것이라 필요했다. 그런데 그 뒤로 장비·직업·상위 사냥터까지 계획을 넓혔다 — 그건 **균형 시험**이고 기술·마법이 도는지와 상관이 없다. `NEXT.md` 에서 `[나중/균형]` 으로 내렸다.
  - 세어 보니: **587개 중 38개만 스크립트가 붙어 있다**(기술 258 중 13 · 마법 329 중 25). 나머지 549개는 이름과 아이콘만 있고 눌러도 아무 일이 없다. 그런데 개수는 맞는다 — 이 저장소가 반복해 맞은 그 함정이다.
  - **팩을 기준으로 쓰면 안 된다는 규칙이 여기서 정확히 왜인지 드러났다.** 하데스는 기술 26 · 마법 40 스크립트를 이미 갖고 있고 이름이 **영문**이다. `data/game-data/abilities.json` 의 원작 목록(기술 275 · 마법 338)도 영문이라 **같은 체계**이고, 그대로 붙는 것이 기술 18 · 마법 26 이다. 팩은 한글이라 안 붙는다. (다행히 실려 있는 기술·마법은 이미 원작 쪽이다 — `Group: 원작표`.)
  - 그리고 587개를 하나씩 쓸 일이 아니다. 하데스에 **공용 스크립트**가 있다 — `Generic Elemental Single` · `Generic Elemental Mass` 가 속성 공격 마법 전부를 덮는다. 원작 목록에 없는 하데스 스크립트 27개가 그런 것들이다.
  - 볼트에 넣었다: `data/formula-vault/구현/기술·마법이 실제로 도는가` (개수를 **매번 다시 센다** — 구현이 진행되면 숫자가 따라 움직인다), 그리고 근거 노트 하나 추가(장비가 방어를 100 → -70 까지 내린다 · `CheckReqs` 가 요구레벨·직업·성별·내구도를 검사한다).
  - 지나가며 고친 것: **벽 판정은 `sotp.dat` 을 봐야 한다.** 칸의 왼벽·오른벽이 0 이 아니어도 벽이 아니고, `sotp` 에서 그 번호가 `0x0F` 일 때만 벽이다. 이걸 빼고 세어 `노비스지하던전A1` 을 98% 벽으로 봤는데 실제로는 14% 다. `docs/what-hades-already-has.md` 에 적었다.
- 2026-09-13 — **괴물 정의 파일이 체력을 적어 두는데 서버가 안 읽고 있었다.** 사용자가 "왜 자꾸 팩 정보를 밀어넣고 생각하려 그러느냐, 괴물마다 json 이 이미 있는 거 아니냐" 고 되물어 프레임이 틀렸음을 알았다 — 허락 받을 일이 아니라 그냥 고칠 일이었다.
  - 덮어쓰던 줄은 `obj.Template.MaximumHP = (int)hp` 다. `obj.Template` 이 사본이 아니라 템플릿 캐시의 그 객체라서, 한 번 젠하면 파일에 적힌 숫자가 **메모리에서도** 사라졌다. 팩 문제가 아니다 — 저장소가 싣는 `spider.json` 도 `Level 5 · 체력 680` 인데 275 로 덮였다. `bees`·`minion` 은 체력을 **0** 으로 적어 두는데 그게 "레벨로 만들어라" 라는 뜻이고, 그 관례는 그대로 뒀다.
  - 공격력·방어력·경험치는 적을 칸이 아예 없었다. `MonsterTemplate` 에 `DmgMin`·`DmgMax`·`Ac`·`Exp` 를 더하고 세 곳이 읽게 했다(`Creations/monsters.cs` · `Formulas/damage.cs` · `Formulas/monsterexp.cs`). 전부 nullable 이다 — **방어 0 은 적어 둔 값**이라(초보 괴물이 정확히 0) 0 을 빈 값으로 쓸 수 없다.
  - **레벨은 필요 없었다.** 레벨 칸을 찾아 아이템 `레벨제한`(484/989)과 퀘스트까지 이름으로 맞춰 봤지만 둘 다 실패했다 — 아이템은 대부분 99 라 등급이 아니라 상한이고(체력과의 순위상관 +0.18), 괴물 이름이 나오는 퀘스트는 60건 중 4건에 레벨 숫자도 없다. 그런데 팩 243마리가 **전부** 체력·최소·최대공격력·방어력·경험치를 갖고 있다. 체력에서 역산하면 103마리가 300레벨 이상이 된다(하데스는 1~99레벨 전체가 체력 91~13,812, 팩 중간값은 60,000).
  - 그 숫자들이 실제로 사다리를 만든다. 1수준(한 방 24점·체력 150)으로 `(튜토리얼)팜팻`(체력 10) 한 대, `노비스풀뱀`(130) 여섯 대, `그린팜팻`(1,950·공격 80~88)부터 위험, `좀비`(70,000·1600~1650)는 즉사. 전에는 **565배치가 전부 체력 91·한 방 7점**이라 모든 사냥터가 똑같이 쉬웠다.
  - 시험은 그림 번호로 때린 놈의 정의를 짚어 그 정의의 체력·방어로 예측한다. 방은 `노비스지하던전A1`(정의 4개, 전부 방어 0, 한 방 1~12) 로 옮겼다 — 40×40 이라 서서 기다리면 시야(12칸) 밖이어서, 네모지게 걸어 찾는다.
  - **그 과정에서 조용한 실패를 하나 잡아 하네스에 넣었다.** `damage.cs` 에 쓴 변수 이름이 아래쪽 `target is Monster monster` 와 겹쳐(CS0136) **스크립트 115장이 통째로 0개**가 됐는데 서버는 그대로 켜지고 포트를 열었다. 찍히는 줄은 `Scripts Loaded and Compiled: 0` 하나다. 이제 격리 서버가 그 줄을 보고 즉시 멈추며 컴파일러가 한 말을 보여준다 — 같은 실수가 2분이 아니라 **14초**에 드러났다.
  - 격리 시험 70개 통과(11분 25초). 전투 시험 단독 3회 통과(1분 22초 ~ 4분 37초 — 젠이 무작위라 편차가 크다).
- 2026-09-13 — **방어력 공식은 있었다. 마지막 줄이 깎은 값을 되돌리고 있었다.** 사용자가 "방어가 피해를 줄이지 않는다는 뭔소리냐, 방어력 공식이 있을텐데" 라고 되물어 다시 보니, 지적이 맞고 내 표현이 틀렸다.
  - `ac.cs` 의 배수 `(Ac+101)/99` 는 제대로다(맨몸 +100 → 2.03배 · 중립 -2 → 1.00 · 최고 -70 → 0.31). 문제는 `return calculatedDmg + Math.Abs(value - calculatedDmg)` 로 끝나는 것이었다 — **두 값 중 큰 쪽**이라 배수가 1 미만일 때 감소분이 그대로 사라졌다. 최고 방어가 중립과 똑같이 맞고 맨몸은 3.06배를 맞았다. 그 줄을 뺐다.
  - `Math.Abs` 도 같이 뺐다. `Sprite.Ac` 가 -70 에서 막으므로 한 번도 동작한 적이 없고, 그 막음이 풀리면 좋은 방어를 다시 손해로 뒤집는다 — **이 버그를 가려 준 것이 그 함수다.**
  - 잰 값: 괴물의 한 방이 사람에게 10점 → **7점**. 다섯 번째 휘두름이 괴물에게 70점 → **49점**. 등 뒤 한 방이 105점(체력 91 즉사)에서 **74점**이 되어, 예측값 두 개가 모두 0 이 아닌 값으로 갈라졌다 — 시험이 더 날카로워졌다.
  - 방향은 자료 쪽도 맞았다. 이식한 아이템 992개 중 **547개**가 `AcModifer` 를 들고 있고 전부 `Operator.Remove` 라 걸치면 Ac 가 내려간다. 다만 수준만으로는 `100 - 수준/3` 이어서 99수준 맨몸도 +67 이다 — 배수를 1 아래로 내리는 것은 장비의 일이다.
  - 시험은 특성 기록에서 **고친 쪽을 지키는 것**으로 뒤집었다. 격리 시험 70개 통과(7분 41초).
  - **앞 커밋이 너무 컸다는 지적도 맞다.** 포크 커밋·`0x08` 리더·시험 둘 재작성·방 교체·무관한 시험 둘 수정·볼트·문서 넷을 한 번에 넣어서, 결론만 남고 "어느 줄이 무엇을 한다" 가 빠졌다. 이제 한 건씩 끊는다.
- 2026-09-13 — **"체력이 좀 깎였다" 를 예측값 검증으로 바꿨다. 바꾸자마자 서버 쪽 사실 일곱 개가 나왔다.**
  - 전에는 첫 체력 보고가 100% 아래이기만 하면 통과했다. 한 대에 1% 가 깎이든 90% 가 깎이든 같은 값이라 **식이 바뀌어도 초록불**이었다. 이제 `Str×4 + Dex×2` 에서 시작해 방어와 속성을 지나 **깎일 점수를 먼저 계산하고** 실제로 때려 본다.
  - 맞춰 보니 계속 어긋났다. 어긋난 이유가 전부 서버 쪽 사실이었다: **기술이 휘두를 때마다 한 단계 오른다**(`(int)(0.10 / LevelRate)` 가 0 이라 `Uses++ >= 0` 이 항상 참 — Assail 의 `LevelRate` 는 0.5). **방어는 피해를 줄이지 않는다**(`ac.cs` 가 맨 피해와 방어를 거친 피해 중 **큰 쪽**을 준다. 새 캐릭터는 +100, 1수준 괴물은 +69 — 둘 다 더 아프다). **새 캐릭터는 150 중 60 으로 깨어난다.** **헛친 것도 serial 0 의 체력 0 으로 알려준다**(그래서 보고를 세면 몇 번째 휘두름인지 나오고, 그게 기술 수준이다). **속성 없는 쪽끼리는 0.50 배.** 재생과 레벨업이 체력을 밀어 올리므로 괴물의 한 방은 백분율이 아니라 **점수**로 재야 한다.
  - **괴물 템플릿의 체력은 버려진다**는 것도 여기서 확인됐다. 젠할 때 `Template.MaximumHP` 를 `50 + Lv×(Lv+40)` 로 덮어써서, 팩이 17,550 을 준 `가스` 가 **91** 로 선다. 배치 565개가 전부 1수준이라 전부 91 이다. 공격력도 표가 없고 수준에서만 나온다. `NEXT.md` 에 올렸다.
  - 시험이 흔들리던 것 셋도 원인이 각각 있었다. **젠이 벽에 걸린 시도도 한 번으로 센다** — 정의가 둘뿐인 방(`신죽마집안5-2`)은 1분에 여섯 번만 굴려 몇 분을 기다려도 빌 수 있다. 정의 7개짜리 `지하수로D-2` 로 옮겼다. 들어온 뒤 선 괴물은 **먼저 물어봐야** 목록에 들어온다(새로고침). 옷 시험 둘은 "겉모습이 **달라지면**" 을 기다려서, 첫 묘사가 옷 없이 오면 그다음 맨발 묘사를 답으로 집었다 — **찾던 모습**을 기다리게 고쳤다.
  - 함께: 모바일 코어가 `0x08`(내 수치)을 읽는다. 네 조각이 따로 오고 안 온 조각은 값을 유지한다 — 그래서 한 대 맞을 때마다 힘이 0 이 되지 않는다. 캐릭터창이 필요한 값이고, 이 시험이 서버에서 받은 힘·민첩·방어로 예측하는 근거다.
  - `SpellBook` 이 실행마다 달라지는 것은 `StarterSpellOnCreate` 가 준 마법의 ID 가 무작위라서다. `SkillBook` 과 같은 이유로 고정 자료에 사유를 적어 허용했다.
  - 결과: 격리 시험 **70개 통과**(7분 20초) · 모바일 코어 131개 통과. 볼트에 "돌려 보고 알아낸 것" 일곱 장이 근거 줄·재 본 값·지키는 시험과 함께 들어갔다(`python3 scripts/build-formula-vault.py`).
- 2026-09-13 — **모바일에서 기술·마법 한 줄이 실제 서버 효과까지 이어졌다.** 월드 입장 때 오는 기술 `0x2C`·마법 `0x17`을 보관하고 원본 `gui06.pal` 아이콘으로 첫 기술·첫 마법 단축키를 그린다. 기술 `0x3E`, 마법 `0x0F`를 보내며 대상형 마법은 화면에서 고른 대상을 쓴다. 입력형 마법은 아직 입력창이 없어 안내만 한다.
  - 격리 Hades에서 `Assail`의 몸동작과 `beag ioc fein`의 실제 회복 스크립트 실행을 확인했다. 이식기가 마법 스크립트 열쇠를 잘못된 `ScriptName`이 아니라 `ScriptKey`로 쓰도록 바로잡고, 첫 세로 조각에는 확인된 런타임 값만 채웠다.
  - 슬롯 삭제 `0x2D`·`0x18`도 반영한다. 전에는 능력을 잊거나 옮겨도 옛 버튼이 남았고, 같은 슬롯의 내용만 바뀌면 캐시가 아이콘·이름을 갱신하지 않았다. 코어 127개 통과, Godot C# 빌드 경고 0, Hades 실동작 시험 통과, 세로 레이아웃 통과.
- 2026-09-13 — **기술·마법 아이콘의 원래 색을 복원했다.** `item007.pal`은 56개 후보 중 픽셀 차가 작은 것을 고른 추정이었는데, 참고 뷰어가 정답을 코드로 갖고 있었다. `DADataViewer/SkillsForm.cs`는 기술과 마법 모두 `setoa.dat`의 `skill001.epf`·`spell001.epf`를 같은 아카이브의 **`gui06.pal`**로 그린다.
  - 그림도 `Legend.dat` 사본이 아니라 뷰어가 실제로 여는 `setoa.dat` 것을 썼다. 이 판본은 기술 114칸·마법 266칸이고, 자료가 쓰는 번호 범위(기술 0~98·마법 3~186)를 모두 덮는다.
  - `docs/ability-icons/skill.png`·`spell.png`를 다시 만들고 화면의 “색표는 짐작” 경고를 근거 설명으로 바꿨다. 재생성 명령과 출처는 `docs/where-the-answers-are.md` 4.9절에 남겼다.
- 2026-09-12 — **10인 soak 가 첫 판에 큰 결함을 잡았다. 셋 중 둘이 실패하고 있었다.** 4분 예비 실행이었는데 30분을 돌릴 이유가 없었다.
  - 증상: 로그인은 빨랐는데(월드 입장 중앙값 41ms) **자기 위치가 오지 않아 화면이 빈 채**로 남았다. 혼자 시험할 때는 늘 왔다 — 동시에 붙어야 드러나는 것이었다.
  - 원인 둘이 같은 모양이었다. **월드의 모든 아이슬링을 훑는 조건식**이, 다른 연결이 한창인 사이 없을 수도 있는 것을 건드렸다. `Insert` 는 `aisling.Username` 비교에서 터졌고 그 예외가 **월드 입장 한가운데서** 나와 캐릭터가 놓이지 않았다 — **던진 횟수 320회 = 실패 320회**. `ObjectCheckPoint` 는 캐릭터를 쥐지 않은 채 `Aisling.Username` 을 읽어 **아이슬링 하나마다** 던졌다(4분에 167,000회).
  - **167,000회가 로그를 덮고 있었다는 것도 결함이다.** 진짜 오류를 찾을 수 없게 만든다. 고친 뒤 로그가 1,839,936줄 → 2,964줄이 됐다.
  - 결과: 성공 168 → **1,435**, 실패 320 → **4**(0.28%), 남은 소켓 1개(연결 약 1,439회 중).
  - 남은 것: `IndexOutOfRange` 2회 · `OutOfMemoryException` 1회 · 30분 기준 실행. `NEXT.md` 에 올렸다.
  - 시험 도구는 합성 하네스가 아니라 **모바일 클라이언트가 쓰는 바로 그 경로**(`HadesLoginClient`·`WorldClient`)를 쓴다. 기기는 하나도 필요 없다 — 10인은 연결 10개다.
- 2026-09-12 — **서버를 세우면 세계를 먼저 기록하고 끝낸다.** `Ctrl+C` 와 `kill` 이 그냥 프로세스를 없애고 있었다.
  - **먼저 갑작스러운 종료가 얼마나 위험한지 쟀다** — 이미 세 겹으로 덮여 있다: `SaveRate` 10초 주기 저장 · `SaveComponent` 45초 일괄 저장 · 접속 종료 시 저장. 저장 자체도 원자적이라 도중에 죽어도 안 깨진다. **전원이 뽑혀도 최대 10초** 분량만 잃는다. 그래서 이 작업은 "기록이 안 남는다" 를 고치는 게 아니라 **그 10초를 0으로 만드는 것**이다.
  - **저장을 `Abort()` 앞에 둬야 한다.** 뒤에 뒀더니 접속자가 분명히 있는데도 `saved 0 character(s)` 가 찍혔다 — `Abort()` 가 리스너를 닫고 **모두를 끊는데**, 끊으면 클라이언트 목록에서 빠져서 뒤따르는 저장이 아무도 못 찾는다. 돌려 보지 않았으면 "됐다" 고 적었을 것이다.
  - 신호 처리도 한 번 헛짚었다. `pgrep | head -1` 이 서버가 아니라 그것을 감싼 셸을 집어서, 셸만 죽고 서버는 살아남았는데 "핸들러가 안 돈다" 고 의심했다. 프로세스 이름으로 걸러 다시 재니 바로 나왔다.
  - 확인: 접속자를 둔 채 `SIGTERM` → `Shutting down — saved 1 character(s).` · 종료 코드 0. 격리 시험 59개 통과.
- 2026-09-12 — **`data/` 에 계보가 둘이 됐다. 하나만 표시돼 있어 지도를 붙였다.** 서버팩 작업을 검토하다 나온 것이다.
  - **새로 들어온 쪽은 제대로 나뉘어 있다** — `server-packs/<팩>/`, 산출물도 `extracted/<팩>`·`vault/<팩>` 으로 팩마다 따로이고, `build-server-pack-vault.py` 는 그 밖으로 못 쓰게 단언까지 걸어 뒀다.
  - **문제는 기존 자료 둘이었다.** `game-data/` 와 `legend-tables/` 는 버전 표시가 없고 문서는 그냥 "원작" 이라고 부른다. 이제 `data/` 안에 원작이 둘이라 그 말이 가려지지 않는다.
  - **얼마나 다른지 재 봤다.** 같은 "기술 표" 인데 `game-data/abilities.json` 은 `"Assail"` · `statCosts [3,3,3,3,3]` 로 영문 613개이고, `5.99-server/db/skill/Skill.txt` 는 `이름 기본공격 · 이미지 1 · 레벨 100` 로 한글 605줄이다. 같은 항목의 다른 표기가 아니라 **다른 계보의 다른 표**다.
  - `data/README.md` 를 만들어 어느 폴더가 어느 계열인지, 인용할 때 폴더까지 적으라는 규칙을 못박았다. `legend-tables/README.md` 와 `docs/game-data.md` 머리에도 "계열: Hades (7.18)" 을 붙였다.
  - 기준은 `LoruleConfig.json` 의 `ClientVersion 718` 이다.
- 2026-09-12 — **비밀번호를 해시로 저장한다. 옛 계정은 첫 로그인에 스스로 옮겨진다.** 지인 테스트 전에 가장 위험하던 것이다.
  - 캐릭터 파일에 **친 그대로** 들어가 있었다. 그 폴더를 읽을 수 있으면 모든 비밀번호를 읽을 수 있었고, 사람들은 비밀번호를 다른 곳에도 쓰므로 피해가 이 게임에서 끝나지 않는다.
  - PBKDF2-SHA256 · 무작위 소금 · 210,000회. 한 문자열이 방식·횟수·소금·해시를 다 지닌다 — 횟수가 값마다 함께 저장되므로 나중에 올려도 기존 값이 무효가 되지 않는다.
  - **옛 계정을 잠그지 않는 것이 핵심이었다.** 검사기가 평문도 받아들이되 "옮겨야 한다"고 알려 주고, 로그인 경로가 **평문이 손에 들어온 그 한 순간**에 해시로 바꿔 저장한다. 못 들어오게 만드는 쪽이 고치려던 결함보다 나쁜 실패다.
  - 실제 평문 계정으로 끝까지 확인했다 — 로그인 → 파일이 `pbkdf2-sha256$210000$…` 로 바뀜 → 평문 사라짐 → 두 번째 로그인은 해시로 통과 → 틀린 비밀번호는 거부.
  - **시험 하나가 빨간불이 됐고 그게 맞았다.** `CharacterStateCharacterizationTests` 는 실행마다 달라져도 되는 필드를 못박아 둔 것인데, 소금 때문에 `Password` 가 달라진다. 의도한 변화이므로 이유를 적어 fixture 에 등록했다. 59개 통과.
- 2026-09-12 — **벽 판정 다툼을 지도가 끝냈다. Hades 가 맞다.** 서버에서 걸어 봐야 Hades 코드가 하는 대로 할 뿐이라 새 정보가 없다 — 대신 **연결성**을 쟀다.
  - 난민촌(lod2, 70×70)에서 두 규칙의 지나갈 수 있는 칸을 4방향 연결 구역으로 나눴다. **`OR`(Hades): 2,661칸 · 구역 2개**(2,631 + 30). **`AND`(ETDA): 2,778칸 · 구역 65개**(2,635 + 30 + 1~9칸짜리 63개).
  - **`AND` 로 117칸을 열면 아무 데도 안 이어지는 1~9칸 주머니가 63개 생긴다** — 건물·구조물 *안쪽* 칸이다. 정작 주 구역은 2,631 → 2,635 로 **4칸** 늘 뿐이라 실제 통로를 여는 것이 아니다. 벽 면이 하나라도 있으면 막는 쪽이 지도와 맞는다.
  - **`0x8F`(벽+투명) 54개는 이 데이터에서 판가름할 수 없고, 할 필요도 없다** — 맵 다섯 개 어디에서도 그 번호를 벽으로 쓰지 않는다(합계 0칸). 맵이 하나라도 쓰기 시작하면 그때 본다.
  - ETDA 는 남의 저장소이고 우리가 고칠 것이 아니다. 우리 도구(`walls`)와 서버는 이미 같은 규칙이므로 바꿀 코드가 없다 — 바뀐 것은 "모른다" 가 "안다" 가 된 것뿐이다.
- 2026-09-12 — **벽 판정이 두 구현에서 갈린다. 난민촌 117칸이 다르다.** 벽을 읽는 도구를 만들다 나온 것이다.
  - 양쪽 벽이 다 있을 때 Hades 는 `왼쪽 || 오른쪽` 으로 막고(`Types/Area.cs:156`), ETDA 봇은 `왼쪽 && 오른쪽` 으로 막는다(`ETDA/BotCore/Components/Map.cs:265`). 앞의 세 분기(둘 다 0 / 한쪽만 0)는 **글자 하나까지 같다** — 같은 함수를 옮겨 적은 것이고 마지막 연산자만 다르다. 한쪽이 틀렸다.
  - **얼마나 다른지 셌다**: lod1 안전 가옥 163=163, lod3 잃어버린숲 542=542, lod99999 하데스 472=472, **lod2 난민촌만 2,239 대 2,122 으로 117칸 차이**. 맵 넷 중 셋이 같으니 여태 드러날 일이 없었다 — 시험을 전부 안전 가옥에서 했다.
  - **확인은 걸어 보면 된다.** 난민촌에서 갈리는 117칸 중 하나에 서 보는 것으로 끝난다. 코드를 더 읽어 정할 문제가 아니다.
  - 함께 남은 것: `0x8F`(벽+투명) 54개는 두 구현 모두 막지 않는다. `da-lib` 의 `TileFlags` 는 `[Flags]` 라 의미상 벽인데, 마스크로 검사하는 구현이 어디에도 없다.
- 2026-09-12 — **벽을 도구가 읽는다.** `dat-extract walls <맵.map> <sotp.dat> [가로칸]` 이 막는 칸을 세고 그림으로 찍는다. 아카이브가 필요 없다 — `.map` 과 `sotp.dat` 이면 끝이다.
  - 서버와 **같은 바이트를 같은 규칙으로** 읽는다(`Types/Area.cs`): 칸마다 벽 번호가 둘이고, 0 은 "그쪽에 벽 없음", 나머지는 `sotp[번호-1]` 이 `0x0F` 면 막는다. 규칙을 새로 지어내면 서버와 어긋나므로 그대로 옮겼다.
  - 안전 가옥(lod1)에서 **방이 그대로 나온다** — 위 한 줄이 통째로 벽, 왼쪽 가장자리와 안쪽 구조물. 벽 번호가 붙은 171칸 중 163칸이 막는다.
  - **시험을 쓰다가 하나 걸렸다: 벽이면서 투명한 타일(`0x8F`)은 막지 않는다.** 서버가 `== 0x0F` 로 정확히 비교하기 때문이고, 실제 `sotp.dat` 에 그런 타일이 54개 있다. 일부러인지(뒤로 지나가는 벽) 아니면 `(flag & 0x0F) == 0x0F` 여야 하는지 `NEXT.md` 에 올렸다 — 도구는 서버와 같게 두었다.
  - 번호가 표 밖이면 도구는 "벽 아님" 으로 답한다. 서버는 검사 없이 색인해 그 경우 예외가 난다.
  - 시험 19개.
- 2026-09-12 — **벽 타일은 찾을 파일이 아니었다. 벽은 타일의 속성이고 `sotp.dat` 에 있다.** 지식 그래프를 만들자마자 나온 답이다.
  - `da-lib/DALib/Definitions/Flags.cs` 의 `TileFlags` 는 주석에 "as used in **sotp.dat**" 라고 적혀 있다 — `None=0` · **`Wall=15`** · `Transparent=128`.
  - **이 저장소의 파일이 그 말을 그대로 증명한다.** `database/server/static/sotp.dat` 는 20,423바이트이고 등장하는 값이 네 가지뿐이다: 15가 15,782개, 0이 4,265개, 128이 322개, 143(=15|128, 벽이면서 투명)이 54개. 합이 정확히 파일 크기다. 즉 **타일 한 칸당 한 바이트**의 속성표다.
  - 서버는 이미 이 파일을 읽고 있었다(`Types/Area.cs`) — 어제 경로 구분자를 고치면서 지나간 바로 그 줄이다.
  - **그래프가 없었으면 계속 헛다리를 짚었을 것이다.** `TILEAS` 를 벽 타일셋으로 오해한 채 "벽 그림 파일"을 찾고 있었는데, 그래프 질의가 `da-lib` 의 `Wall` 플래그와 `FallenDev/DAMapEditor/Tileset.cs`(한 번도 안 열어 본 두 번째 맵 에디터)를 함께 집어 줬다.
  - 남은 것: 도구가 이 플래그를 읽어 벽을 가려 그리게 하는 것, 그리고 20,423 이 `TILEA`(19,243)와도 `TILEA+TILEAS`(22,048)와도 안 맞는 이유.
- 2026-09-12 — **눈 맵은 여기서 더 갈 수 없다는 것을 확인했다.** 모르는 채로 두는 것과 "왜 모르는지 아는 것" 은 다르므로 적어 둔다.
  - **참고 구현이 없다.** 저장소 16개에서 눈 맵을 그리는 코드를 찾지 못했다 — `dark-ages-ts` 는 `MapFlags.SnowTileset = 128` 을 **선언만** 하고 한 번도 쓰지 않고, `da-lib` 에는 눈 관련 코드가 아예 없다. 맵 에디터만 `snow ? TILEAS : TILEA` 로 기본 세트를 고르는데, 고른 세트를 **0번부터** 색인한다(`TileCollection(_baseTileSet)`) — 즉 "아무 맵을 눈으로 칠한다" 가 아니라 "눈용으로 만든 맵이 쓰는 별도 기본 세트" 라는 모델이다.
  - **시험할 맵도 없다.** 이 서버의 area 넷은 `Flags` 가 모두 106240 으로 같고 눈 비트(128)가 꺼져 있다.
  - 맵별 바닥 번호 범위(참고): lod0 1~18,272 · lod1 6,937~11,508 · lod2 0~11,732 · lod3 1~786 · lod99999 4,112~4,664. `lod3` 만 `TILEAS` 의 2,805칸 안에 들어가지만, 그것만으로 눈 맵이라 말할 수는 없다 — 낮은 번호는 `TILEA` 에도 있다.
  - **`map` 에 눈을 붙이지 않은 채로 둔다.** 정답을 맞춰 볼 것이 없는데 옵션만 있으면, 보통 맵에 줬을 때 조용히 빈 그림을 내놓는 함정이 된다.
- 2026-09-12 — **타일셋 읽는 자리를 `TileSource` 한 곳으로 합쳤다. 그리고 `map` 에는 눈을 붙이지 못한다는 것을 알았다.**
  - `tiles` 에 눈 지원을 넣으면서 `TileSource` 가 하던 일을 그대로 한 벌 더 만들어 뒀었다. 합쳐서 눈 규칙(어느 파일을 읽고 팔레트 번호를 얼마나 밀지)이 한 곳에만 있게 했다.
  - **`map` 에 같은 낱말을 붙였다가 되돌렸다.** 한 칸도 안 그려졌다 — 맵의 바닥 번호는 `TILEA` 의 19,243칸 공간을 가리키는데 `TILEAS` 는 2,805칸뿐이라 전부 범위 밖이다. 돌려 보지 않았으면 "붙였다"고 적을 뻔했다.
  - 그래서 **눈 맵을 어떻게 그리는지가 빠진 조각으로 남는다.** 서버는 `MapFlags.SnowTileset`(128)로 눈을 지시하는데, 그 깃발이 켜졌을 때 번호를 어떻게 바꿔 읽는지를 아직 모른다. `NEXT.md` 에 그대로 적었다.
  - 곁가지: 합치면서 타일 시트의 배경이 검정에서 투명으로 바뀌었다(`TileSource.Draw` 는 투명 픽셀을 건너뛴다). 타일 해석은 그대로다.
- 2026-09-12 — **눈 타일의 팔레트를 찾았다. 두 타일셋은 번호가 이어진다.** `TILEAS` 의 N번 타일은 팔레트 표에서 **`TILEA` 칸 수(19,243) + N** 으로 찾는다.
  - **근거는 `da-lib` 의 `Tileset` 주석이다** — "Ensure the tile ids you add to the PaletteTable are based on **appending to the existing tileset**". 타일셋 둘이 한 줄로 이어진 번호 공간을 쓴다는 뜻이고, 팔레트 표(`mpt*.tbl`)가 그 공간을 센다.
  - 그 전에는 `TILEAS` 의 번호를 그대로 표에 대서 `TILEA` 쪽 행을 읽었고, 그래서 색이 무작위로 튀는 줄이 나왔다. 오프셋을 더하니 흙·돌·깊은 물·**흰 눈과 얼음**이 또렷하게 나온다.
  - 오프셋은 `TILEA` 를 통째로 읽지 않고 바이트 길이 ÷ 1512 로 센다.
  - `dark-ages-ts` 의 `MapFlags.SnowTileset = 128` 로 서버가 눈 타일셋을 지시한다는 것도 확인했다 — **`map` 명령은 아직 그 깃발을 보지 않는다**(NEXT 에 올림).
- 2026-09-12 — **`TILEAS` 는 벽 타일이 아니라 눈(snow) 바닥이고, 고정 크기가 맞다.** 할 일에 "벽 타일(`TILEAS.BMP`는 고정 크기 아님)" 이라고 적혀 있던 두 가지가 모두 틀렸다.
  - **S 는 snow 다.** 참고 저장소 전체에서 `TILEAS` 를 쓰는 곳은 맵 에디터 한 군데뿐이고 `snow ? TILEAS : TILEA` 로 고른다.
  - **고정 크기다.** 산수가 먼저 말해 줬다 — `TILEA` 29,095,416 ÷ 1512(56×27) = 19,243 칸, `TILEAS` 4,241,160 ÷ 1512 = 2,805 칸, 둘 다 나머지 0. 도구로 읽히는 칸 수가 정확히 그 값이다.
  - `dat-extract tiles … 눈|snow` 로 뽑을 수 있게 했다. 칸은 깨끗한 마름모로 나온다.
  - **남은 것은 팔레트다.** `mpt*.tbl` 은 `TILEA` 번호 기준이라 `TILEAS` 에 그대로 대면 색이 어긋나는 줄이 나온다. `seo.dat` 에 `TILEAS` 전용 표는 안 보인다 — `gndani.tbl`·`gndattr.tbl` 이 후보다.
  - **벽·구조물이 어느 파일에 있는지는 여전히 모른다.** `TILEA*` 가 아니라는 것만 확실해졌다.
  - 도구를 이 Mac 에서 돌리려고 .NET 8 런타임을 `.tools/` 에 같이 깔았다(도구는 net8.0, SDK 는 9). 커밋되지 않는다.
- 2026-09-12 — **서버 주소는 빌드 설정으로 간다고 정했다.** 로그인 화면에 주소 칸을 두는 쪽은 쓰지 않는다.
  - **근거는 취향이 아니라 프로토콜이다.** 클라이언트는 로그인 서버 하나만 알면 되고 그 다음 접속처는 서버가 `MServerTable.xml` 로 내려준다 — "클라이언트가 서버 목록을 고른다"는 개념이 원작에 없다. 시안 3절이 `서버 목록 선택`을 범위 제외로 둔 것도 같은 이유로 읽는다. 그래서 시안은 고치지 않고, 레이아웃 12판도 그대로다.
  - `server.cfg` 를 임시방편에서 정식 통로로 올렸다 — `server.cfg.example` 을 커밋해 형식과 이유를 적었다(`server.cfg` 자체는 계속 커밋 안 함). 읽는 순서는 `--server` → `LOD_SERVER` → 파일 → 기본값.
  - 다른 서버를 가리키려면 다시 빌드한다. 지인 테스트는 서버가 하나뿐이라 이것으로 충분하다.
- 2026-09-12 — **`tools/` 에 시험 자리를 만들었다.** `tools/tests/DatExtract.Tests` 12개. 지금까지 눈으로만 확인하던 두 곳을 덮는다.
  - **`IconPalettes.PaletteFor` 의 세 낱말 갈래.** 같은 모양이 마지막 낱말의 부호에 따라 범위가 되기도, 기분(mood)이 되기도 한다 — `30 5 -2` 는 30번 타일에 팔레트 5, 기분 -2 이지 30~5 범위가 아니다. 그 갈래와 함께 두·네 낱말, 덮이지 않은 타일, 줄 건너뛰기, 먼저 맞는 줄이 이기는 것을 덮었다.
  - **`icon` 명령의 칸 계산을 `Program.IconCell` 로 빼냈다.** 큰 메서드 안에 박혀 있어 부를 수가 없었다. 0x8000 을 떼는 것과 타일이 1부터 센다는 두 가지가 틀리기 쉬운 곳이다 — 266번째가 첫 파일의 마지막, 267번째가 둘째 파일의 처음이라는 것을 못 박았다.
  - 시험은 도구 폴더 **밖**에 둔다. 안에 두면 `DatExtract.csproj` 가 시험 파일까지 끌어간다(Compile 항목을 명시하지 않는다). 내부 타입을 보려고 `InternalsVisibleTo` 를 썼다 — 시험 때문에 public 으로 올리면 인터페이스가 아닌 것을 인터페이스라고 말하게 된다.
  - 도구는 net8.0 그대로 두고 시험만 net9.0 이다. 맞추려면 net8.0 런타임을 따로 깔아야 하는데 시험을 돌리려고 그럴 일은 아니다.
- 2026-09-12 — **떠난 연결의 소켓을 실제로 닫는다. 격리 시험 52개가 전부 통과한다.** 인증 전에 떠난 연결 50개가 모두 `CLOSE_WAIT` 으로 남던 것을 고쳤다.
  - **한 곳에서 두 가지가 틀려 있었다.** `Socket.Disconnect` 는 연결만 끊고 **핸들을 놓지 않는다** — 애초에 닫는 함수가 아니다. 그리고 그것이 `Socket.Connected` 로 가려져 있었는데, 0바이트 수신으로 여기 오는 시점에는 이미 `false` 다 — **정작 중요한 경우(상대가 떠남)에 그 줄을 건너뛰고 아무도 소켓을 닫지 않았다.** 목록에서만 지워졌다.
  - 무조건 닫고, `Shutdown` 은 최선 노력으로 둔다(상대가 이미 없을 수 있고, 한 클라이언트에 두 번 불릴 수 있다).
  - **시험이 먼저 옳았다.** `ConnectionLifetimeTests` 는 Windows 기준으로 쓰였고 거기서는 0개로 측정됐다. 그 시험을 이 플랫폼에서 돌린 것만으로 결함이 드러났다 — 고칠 것은 시험이 아니라 코드였다.
- 2026-09-12 — **한 연결이 다른 연결을 굶기던 것을 고쳤다.** 계획서가 "미해결"로 남겨 둔 항목(2.3절 아래)이다. 나가는 패킷 **모두**가 `ServerContext.SyncLock` 하나를 잡고 직렬화·암호화를 했다 — 갱신 요청을 몰아 보내는 연결 하나가 서버의 모든 응답을 자기 뒤에 줄 세웠다.
  - **그 잠금이 지키던 것은 전부 연결별 것이었다.** `Writer`·`Encryption` 은 생성자에서 연결마다 만든다. 게다가 같은 파일의 `Send(NetworkFormat)` 은 이미 `lock (Writer)` 를 쓰고 있었다 — 올바른 본보기가 몇 줄 위에 있었던 셈이다.
  - **병목만 넓힌 것이 아니라 경쟁 상태도 닫았다.** `Send(byte[])` 는 `Writer` 에 쓰면서 전역 잠금을 잡아 `Send(NetworkFormat)` 과 서로 배제하지 못했다 — 한 연결의 두 송신이 같은 버퍼에서 뒤섞일 수 있었다.
  - **회귀 없음을 기준선으로 확인했다.** 격리 시험 52개 중 51개 통과이고, 남은 하나는 **수정을 되돌려도 똑같이 실패한다.** 추측 대신 되돌려 재는 쪽을 택했다.
  - 하네스가 `Staging/net5.0` 을 보고 있어 net9.0 으로 맞췄다(시험 csproj 의 `Darkages.dll` 참조도 같이).
  - **새로 드러난 것: macOS 에서 인증 전 연결 50개가 전부 `CLOSE_WAIT` 으로 남는다.** 계획서는 Windows 에서 0개로 측정했다 — 플랫폼 차이다. `NEXT.md` 에 올렸다.
  - **곁가지:** 경로 구분자를 고친 뒤로 서버가 시작할 때마다 `areas/*.json` 4개를 다시 쓰고 있었다(플랫폼마다 다르게). 저장 값을 한 가지 철자로 정규화하고 그 결과를 커밋해 실행이 트리를 더럽히지 않게 했다.
- 2026-09-12 — **돈 합산을 눈으로 확인했다.** 같은 칸에 세 번(250+100+150) 버린 금이 한 더미가 됐고, 한 번 주우니 `You've Received 500 coins.` 가 왔다. 버릴 때마다 일련번호가 바뀌는 것도 "걷어내고 다시 놓는" 동작 그대로다.
  - **확인하려고 클라이언트 코어에 금 버리기를 붙였다**(`WorldClient.DropGoldAsync`, `0x24`). 금은 소지품 칸이 아니라 캐릭터에서 바로 빠지므로 금액과 칸만 보낸다.
  - **두 번 헛다리를 짚었다.** ① 내 발밑에 버리면 바로 되주워진다. ② 옆 칸도 마찬가지였다. 원인은 `AUTO LOOT GOLD` 게임 설정이었다 — `ObjectComponent` 가 주기적으로 돌면서 그 설정이 켜진 플레이어에게 보이는 금을 **즉시 준다**(`Network/Game/Components/ObjectComponent.cs`). 그래서 서버가 "버렸다" 와 "받았다" 를 연달아 말했다. 시험 계정에서 그 설정을 끄니 바닥에 남았다.
  - **바닥에 돈이 안 남는다고 서버를 의심하기 전에 이 설정부터 본다.** 기본값이 켜짐이다.
  - 코어에는 아직 나가는 월드 패킷을 시험하는 틀이 없다(`DropAsync`·`AttackAsync` 도 같다). 실서버로 확인하고 남겨 둔다.
- 2026-09-12 — **돈이 같은 칸에서 합쳐진다.** `Money.Create` 가 떨어질 때마다 새 더미를 만들고 있었다 — 한 칸에 여러 더미가 쌓여 따로따로 주워야 했다. 같은 칸의 것을 걷어 금액에 더하고 하나로 다시 놓는다.
  - **그림 등급이 금액에서 나오므로**(`CalcAmount`) 옛 더미에 금액만 더하면 바닥 그림이 어긋난다. 그래서 제자리 수정이 아니라 걷어내고 다시 놓는 쪽으로 했다. 등급 경계는 서버가 정한 대로 두었다(할 일에 그렇게 적혀 있었다).
  - 넘칠 만큼 큰 합은 `int.MaxValue` 에서 멈춘다. 이 서버가 그만큼 들 수 없기도 하고, 음수로 감기는 쪽이 더 나쁘다.
  - **아직 눈으로 못 봤다.** 서버는 깨끗하게 뜨지만(콘텐츠 12종, 오류 0, 포트 2) 쌓이는 것을 보려면 같은 칸에 두 번 떨어뜨릴 수 있는 판이 필요하다. `NEXT.md` 에 그대로 올려 뒀다.
  - fork 브랜치 `fix/run-on-macos` (`8c416c276`).
- 2026-09-11 — **아이패드에서 Mac 의 서버에 붙어 월드에 들어갔다.** 서버 로그에 `lodtest : Welcome to Lorule`. 절차는 `docs/mobile-client.md` 3절 "실기기에서 서버에 붙기".
  - **막은 것이 넷이었고 전부 다른 층이었다.** ① 주소를 코드에 박을 수 없다 — 실기기는 인자도 환경변수도 못 받는다. 커밋되지 않는 `server.cfg` 를 빌드에 실어 읽는다. ② `OS.HasFeature("mobile")` 이 Godot 4 iOS 에서 거짓이라 기기가 `127.0.0.1` 에 붙었다 — `OS.GetName()` 으로 바꿨다. ③ iOS 로컬 네트워크 권한이 없으면 **묻지도 않고 조용히 막는다**(`NSLocalNetworkUsageDescription`). ④ 서버가 알려 주는 주소가 두 군데다.
  - **④ 가 가장 오래 걸렸다.** `LoruleConfig.json` 의 `ServerIP` 를 고쳐도 로비→로그인 리다이렉트는 **`MServerTable.xml`** 을 본다. 거기가 `127.0.0.1` 이면 서버가 클라이언트에게 "네 자신에게 접속하라" 고 말한다 — 같은 기계에서는 성공하고 다른 기기에서는 "연결이 거부되었습니다" 가 난다. 첫 연결은 실제로 닿기 때문에 방화벽·IP 를 의심하게 된다. `run-procedure.md` 0.6절.
  - **갈라서 확인하는 것이 결정적이었다.** 엔진 없이 `Lod.Mobile.Core` 의 로그인 경로만 콘솔로 돌려 Mac 에서 월드 입장까지 확인하니, 서버·계정·프로토콜이 정상이라는 것이 못 박혔고 남은 것이 기기 하나로 좁혀졌다.
  - 계정은 하네스(`LoginFlow.cs`)의 생성 절차(`0x02`·`0x04`)를 그대로 써서 만들었다. 일회용 도구라 저장소에 넣지 않았다.
  - **카메라가 캐릭터를 가운데 두지 않았다.** 지도 가장자리에서 화면을 멈추던 규칙(`WorldView.Look`) 때문인데, 시작 칸이 (4,4) 구석이고 화면은 원작보다 커서 캐릭터가 구석에 박혀 있었다. 제한을 없애 항상 정중앙에 둔다 — 지도 밖 빈 공간이 조금 보이는 쪽이 낫다고 정했다.
- 2026-09-11 — **Hades 서버가 Mac 에서 돈다.** 2610·2615 가 열리고 맵 4·아이템 3·괴물 3·워프 4·팝업 3·월드맵 1·국가 1·기술 1 이 실린다. fork 브랜치 `fix/run-on-macos`, 절차는 `docs/run-procedure.md` 0.5절.
  - **막은 것은 둘뿐이었다.** .NET 5 는 Apple Silicon 런타임이 없어 서버 프로젝트 넷을 `net9.0` 으로 올렸다(Windows 전용 편집기·도구는 그대로 뒀다 — 서버를 돌리는 데 필요 없다). 그리고 .NET 6 이 `Enumerable.DistinctBy` 를 들여와 자체 구현과 이름이 겹쳤다 — 동작이 같으므로 자체 것을 걷어냈다.
  - **진짜 문제는 경로 구분자였다.** 저장 경로가 전부 `\` 로 이어져 있다. Unix 에서 그것은 경로가 아니라 백슬래시가 든 파일 이름 하나다 — 서버가 `server\templates` 라는 **이름의** 폴더를 만들어 놓고 템플릿을 전부 0개로 보고했다. 오류는 없다. 열두 군데를 `/` 로 바꾸니 다 실렸다(Windows 의 .NET 은 `/` 를 그대로 받는다).
  - **0개는 조용하다.** "Templates Loaded: 0" 이 열한 줄 찍히는데 그것이 유일한 신호다. 예전에 스크립트가 컴파일되지 않아 세계가 비었던 것과 같은 모양이다 — 비어 있음을 실패로 알리는 것이 없다.
  - 남은 것: 아이패드에서 붙으려면 서버 주소를 바꿀 수 있어야 한다(`NEXT.md` [다음/클라이언트]). 그리고 서버가 실행 중에 `database/server/areas/*.json` 을 다시 쓴다 — 런타임 산출물이라 커밋하지 않았다.
- 2026-09-11 — **클라이언트가 아이패드에서 떴다 — 로그인 화면까지.** 그전까지 기기에 올린 것은 스모크 껍데기뿐이었다.
  - **`EXPORT SUCCEEDED` 가 거짓말을 한다.** iOS 는 AOT 게시인데 ILC 의 트림·AOT 분석 경고가 수십 개 나온다 — 전부 GodotSharp 안이라 우리가 못 없앤다. 작업공간의 `TreatWarningsAsErrors` 가 그걸 오류로 올려 .NET 게시를 죽이고, **Godot 은 그 실패를 삼킨 채 .ipa 를 만든다**(C# 프레임워크가 빠진 채로). 서명·설치 다 되고 기기에서 엔진이 올라온 직후에 죽는다. 로그에 남는 것은 `Failed to build project. Check MSBuild panel for details.` 한 줄뿐이다. `LodClient.csproj` 가 iOS RID 일 때만 `IlcTreatWarningsAsErrors` 를 끈다 — 데스크톱은 엄격 그대로.
  - **크기로 확인하면 속는다.** C# 이 빠진 빌드와 제대로 된 빌드가 둘 다 23MB 였다. `unzip -l … | grep LodClient.framework` 로 봐야 한다. 스모크 앱과 나란히 놓고 `Frameworks/` 를 비교해서 찾았다.
  - **`.sln` 이 없으면 C# 을 아예 게시하지 않는다.** `mobile/client` 에 없었다 — Godot 이 "no solution file exists" 라고 적고 역시 성공으로 끝낸다. 윈도우에서는 `dotnet build` 를 csproj 로 직접 부르므로 드러나지 않던 구멍이다.
  - **smoke 게이트 통과가 클라이언트를 보장하지 않는다.** `experiments/` 는 `mobile/Directory.Build.props` 를 물려받지 않아 smoke 는 처음부터 잘 나갔고 클라이언트만 죽었다.
  - 새로 넣은 것: `mobile/client/export_presets.cfg`(iOS 프리셋, Team ID 는 비움), `mobile/client/LodClient.sln`.
- 2026-09-11 — **실기기에서 떴다. Mac·iOS 게이트를 닫는다.** iPad 9세대(iPad12,2, iPadOS 26.5.2)에 설치해 실행까지 확인했다. `UIDeviceFamily [1,2]` 라 iPhone·iPad 둘 다 받는다.
  - **Godot 의 iOS export 는 기기를 등록하지 못한다.** `xcodebuild` 를 부르면서 `-allowProvisioningDeviceRegistration` 을 주지 않아, 프로파일에 없는 기기는 설치에서 `0xe8008012` 로 막힌다 — **다시 export 해도 소용없다.** 같은 프로파일을 계속 쓰기 때문이다. 생성된 `.xcodeproj` 를 그 플래그와 함께 **한 번** 직접 빌드하면 프로파일이 그 기기로 다시 발급되고, DerivedData 의 `.app` 을 `xcrun devicectl device install app` 으로 넣으면 된다.
  - **확인은 프로파일을 직접 읽어서 한다** — `security cms -D -i <app>/embedded.mobileprovision` 의 `ProvisionedDevices` 에 UDID 가 있는지. 막혔을 때 이걸 먼저 봤어야 했다. 서명 오류가 사실 하나로 바뀐다.
  - 잔 함정 둘. 개발자 모드는 **재시동 뒤 잠금 해제할 때 뜨는 알림까지 수락해야** 켜진다. 그리고 `Failed to allocate RSD device` 는 케이블이 아니라 맥의 CoreDevice 계층이다 — Xcode 를 한 번 열자 풀렸다.
- 2026-09-11 — **서명된 arm64 .ipa 가 나왔다.** 실기기 설치만 남았다. 게이트는 `experiments/godot-csharp-mobile-smoke` 이고 결과는 그 README.
  - Godot 4.6 Mono · .NET 9.0.317 · `4.6.stable.mono` export templates · Xcode 26.5(iOS 26.5 SDK)로 31MB `MobileSmoke.ipa`. arm64, `iPhoneOS`, Apple Development 서명, 팀 프로비저닝 프로파일 자동 발급. `--import`·`--build-solutions`·`dotnet_publish_project`·`generate_xcframework`·xcodebuild archive/export 전부 headless 로 통과했다.
  - **Xcode 는 처음부터 있었다.** `xcodebuild` 가 "requires Xcode" 라고 거부하길래 없는 줄 알았는데, `xcode-select` 가 Command Line Tools 를 가리키고 있었을 뿐이다. `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` 면 sudo 없이 쓴다.
  - **Team ID 는 인증서 이름 뒤 괄호가 아니라 OU 다.** 괄호 안(`W7Y2PYS4FD`)을 Team ID 로 쓰면 안 된다 — `security find-certificate -c "Apple Development" -p | openssl x509 -noout -subject` 의 OU 를 본다. export 하는 동안만 프리셋에 넣고 곧바로 되돌렸다. 커밋하지 않는다(그 README 의 규칙).
  - 처음 한 번은 `build/ios/` 가 없어 실패한다 — Godot 이 만들어 주지 않는다.
- 2026-09-11 — **Mac 에서도 돈다.** 같은 커밋을 Apple M2(macOS 26.5)에서 열어 빌드·시험·실행·촬영까지 확인했다. 절차는 `docs/mobile-client.md` 3절 "macOS".
  - **도구는 윈도우와 같은 버전으로 맞췄다** — .NET SDK 9.0.317(osx-arm64), Godot 4.6-stable **mono**. 버전을 맞춰야 다르게 나오는 것이 곧 플랫폼 차이가 된다. 둘 다 `.tools/` 라 커밋되지 않는다.
  - 결과: 코어 시험 **123개 통과**(38ms), `LodClient` 빌드 경고 0, 게임 화면이 OpenGL 4.1 Metal(GL Compatibility)로 그려지고 촬영도 된다. 레이아웃 12화면 전부 통과.
  - **한글이 멀쩡한데 화면은 깨진다고 말한다.** `Main.BuildTheme` 이 `C:/Windows/Fonts/malgun.ttf` 가 있는지만 본다 — macOS 는 Godot 이 시스템 글꼴로 대신 그려 한글이 전부 나오는데도 "글꼴 없음 — 한글이 깨집니다"가 뜬다. 검사가 **그려지는지**가 아니라 **윈도우 글꼴 파일이 있는지**를 보고 있다. 글꼴을 넣어야 한다는 결론 자체는 그대로다(iOS·Android 에는 기댈 시스템 글꼴이 없다).
  - **세로에서만 GL 텍스처가 샜다 — 글꼴이 원인이었다(같은 날 고침).** 131072 는 256×256 LA8 한 장, 글꼴 아틀라스 한 쪽 크기다. 윈도우 글꼴이 없을 때 테마에 글꼴을 아예 안 넣었고 한글은 엔진이 뒤에서 대신 그려 주고 있었다 — 그 아틀라스를 테마가 쥐고 있지 않아 끝날 때 못 놓아줬다. 세로에만 있는 기록 줄이 글자를 더 그려 한 쪽이 더 필요했던 것이 세로에서만 난 이유다. 시스템 글꼴을 명시적으로 넣으니 사라졌다(360×780 세로에서 확인).
  - **`--screen login` 이 매 프레임 경고를 쏟는다** — `LoginScreen._Process` 가 `DisplayServer.VirtualKeyboardGetHeight()` 를 무조건 부르는데 데스크톱엔 화상 자판이 없다(2초에 191줄). 윈도우도 같을 것으로 추정하나 확인 안 했다.
  - **못 돈 시험이 초록으로 보인다.** `tests/check-layout-script.test.js` 는 `powershell.exe` 를 부른다 — Mac 에서 둘은 실패하고 **셋은 거짓으로 통과한다**(`status !== 0` 이면 되는데 실행이 안 되면 `status` 가 `null` 이라 조건이 그냥 맞는다). 나머지는 성하다: `node --test tests/*.test.js` 37개 중 그 둘만 실패.
  - **못 한 것.** iOS export·실기기는 Xcode 가 없어 손대지 못했다. `scripts/check-layout.ps1` 은 PowerShell 이 없어 Mac 에서 못 돈다 — 같은 12번을 bash 로 옮겨 돌렸고 저장소에는 넣지 않았다.
  - **훅이 Mac 에서 죽는다.** `.claude/settings.json` 의 훅 다섯이 전부 `python` 을 부르는데 이 Mac 에는 `python3` 만 있다. 이번에는 고치지 않았다.
- 2026-09-10 — **잡으면 바닥에 떨어진다.** 괴물 → 전투 → 처치 → 떨어진 물건까지 한 줄로 이어졌다.
  - **서버를 고쳤다.** 바닥 물건만 네 바이트 짧게(방향도 종류도 없이) 쓰고 있었고, 기록 안에 그렇다는 표시가 없었다 — 바닥에 뭐라도 있으면 **그 뒤 기록이 전부 어긋난다.** 원작 형식엔 없는 구멍이라 다른 것과 같은 17바이트로 맞췄다(종류 = 밟고 지나가는 것). fork 에 push.
  - 클라이언트는 그림이 없어 **자리 표식**으로 그린다(시안 8절의 "자리 표시"). 아이콘 아카이브에서 아직 아무것도 안 뽑았다.
  - **확인이 계속 흔들렸다.** 말벌이 돌아다녀 정면에 없고, 캐릭터 위치는 판마다 이어져 조금씩 동쪽으로 밀렸다. 말벌을 제자리에 세우고(`PathQualifer` 고정) 바로 옆 칸에 두고 캐릭터 자리를 되돌리니 한 번에 됐다. 시험용 데이터는 `tmp/hades-run` 에 그대로 뒀다.
  - 원작 자료를 다시 뒤져 본 결과: **드랍표는 원작에 없다** — Hades 가 만든 것이다(`LootTable`·`LootDropper`, 괴물 템플릿의 `Drops`). **움직임·전투 방식은 있다** — `MobTile.tbl`(이미 뽑아 둠, 4절). 말벌 행은 `SFCnt 2 · WFCnt 2 · AFCnt 2 · fStop 0 · fChgDir 1` 이고 MPF 머리말과 일치한다.
  - 시험 59 + 47 통과.
- 2026-09-10 — **휘두르는 동작을 그린다.** 평타는 걷기와 **다른 파일**(끝 `02`)이고 네 칸뿐이다 — 등 0–1, 앞 2–3. 부위마다 그림이 두 벌이 됐고, 자기 평타 그림이 없는 부위(모자)는 서 있는 그림 그대로 둔다(원작도 그렇다).
  - **숫자는 참고 클라이언트에 그대로 있었다** — `paper-doll-animations.ts` 의 `Attack: startUp 0, startDown 2, frames 2, prefix '02'`. 우리 문서 3.3절과 한 칸도 안 어긋난다.
  - **남이 휘두르는 것도 그린다**(`0x1A`). 그 패킷은 동작 번호도 주는데 무시한다 — 그릴 수 있는 동작이 하나뿐이고, 참고 클라이언트도 똑같이 전부 평타로 그린다. 스킬 동작을 가르려면 `skill.tbl` 이 필요하다(3절).
  - 내 공격은 서버 답을 기다리지 않고 바로 그린다 — 걸음과 같다. 허락된 공격에 서버는 답하지 않으므로 기다리면 버튼이 고장 난 것처럼 보인다.
  - **전투를 끝까지 봤다**: 말벌 옆에서 휘둘러 체력 21%까지 깎고, 잡으니 `"You've Received 527 coins."` 가 왔다. 사진에는 스윙 자세와 서버 문구가 같이 찍혔다.
  - 시험 58 + 47 통과.
- 2026-09-10 — **전투를 붙였다.** `공격`이 서버에 닿고(`0x13`, 몸통 없음), 서버가 하는 말이 아래 줄에 뜨고(`0x0A`, 두 바이트 길이 문자열), 맞은 대상의 체력이 위 가운데에 막대와 숫자로 뜬다(`0x13`, **백분율**이다 — 서버는 남의 절대 HP를 알려주지 않는다).
  - 확인: 말벌 옆에 서서 한 번 휘두르니 서버가 `"Assail has improved. (Lv. 7)"` 라고 답했다. 체력은 이번 스윙이 빗나가 안 왔다 — 말벌이 돌아다녀 정면에 없었다.
  - **가는 길에 유령 사람을 잡았다.** `0x0C`(누가 걸음)은 사람과 괴물이 같이 쓰는 패킷인데, 우리가 모르는 번호는 전부 사람으로 등록하고 있었다. 그래서 **말벌이 걸을 때마다 그 자리에 이름 없는 사람이 하나 서서** 빌린 옷을 입고 있었다(이전 스크린샷들에 계속 찍혀 있던 그 사람이다). 이미 괴물로 아는 번호는 괴물로 옮긴다.
  - 한 번 누르면 한 번 휘두른다 — 쫓지도, 반복하지도 않는다(시안 AC-009). 서버가 거부해도 피해를 임의로 반영하지 않고 서버가 한 말만 보여 준다.
  - 시험 55 + 47 통과, 레이아웃 12화면 통과.
- 2026-09-10 — **괴물이 화면에 섰다.** `0x07` 을 읽어 그린다. 사람과 같은 방식으로 서 있고, 사라지면 지워지고, **탭해서 고를 수도 있다**(대상 이름에 나온다).
  - **기록 읽는 법은 참고 클라이언트가 정답을 갖고 있었다** — 자리·일련번호·그림 뒤 17바이트째가 **종류**고, 상인(2)만 이름이 따라온다. 그걸 안 보고 고정 길이로 읽으면 상인 뒤의 기록이 통째로 어긋난다(시험이 그 경우를 붙잡는다).
  - **바닥 아이템은 아직 못 읽는다.** 우리 서버는 그것만 13바이트로 쓰는데(방향도 종류도 없이) 괴물 기록의 앞부분과 구별할 표시가 없다. 원작 형식엔 없는 구멍이다 — 떨어뜨릴 수 있게 되면 그때 본다.
  - 괴물 번호는 `0x4000` 부터 센다 — 서버가 16385 라 하면 그림은 `MNS001` 이다. 그래서 자산 스크립트가 같은 그림을 `creature/mns001.png` 로도 둔다. 칸 크기는 시트 높이에서 읽으므로 괴물마다 숫자를 박지 않는다.
  - 시험 52 + 47 통과, 레이아웃 12화면 통과.
- 2026-09-10 — **스크립트가 처음부터 컴파일되지 않고 있었다.** 괴물이 안 뜨던 진짜 이유다. 이 서버의 "빈 세계"는 콘텐츠가 없어서가 아니라 **스크립트가 통째로 없었기 때문**이다 — 괴물도, 상인도, 괴물 행동도 전부 스크립트가 움직인다.
  - **찾은 길.** 코드를 읽는 것만으로는 안 끝났다(스폰 조건이 전부 맞아 보였다). 스크립트가 **실행 중에 컴파일**된다는 것을 알고 `monsters.cs` 에 로그를 심었는데, 그 로그가 `System.Console` 참조가 없다며 컴파일을 깼다 — 그 김에 **그 위에 이미 CS0009 두 개가 있었다**는 것이 보였다.
  - **원인.** `ScriptManager` 가 작업 폴더 아래 **모든 `.dll`** 을 컴파일 참조로 넣는다. 이 배포본은 원본 게임 파일을 옆에 두고 있어 `game/Mss32.dll` 같은 네이티브 DLL이 섞이고, **하나만 못 읽어도 컴파일 전체가 실패**한다. 실패하면 어셈블리를 아예 안 싣고, 이유는 시작 로그 백 줄 사이에 `[INFORMATION]` 으로 한 줄 남는다.
  - **최소 실험으로 확인.** 네이티브 DLL 4개를 서버 폴더 **밖으로** 옮겼더니 오류 0개 → 클라이언트에 `0x07` 이 왔다. (처음엔 서버 폴더 *안의* 하위 폴더로 옮겨서 그대로 걸렸다 — 재귀 검색이다.)
  - **고침은 서버 쪽.** `CreateFromFile` 은 파일을 읽지 않아 그 자리에서 못 잡는다(오류가 나중에 진단으로 온다). `AssemblyName.GetAssemblyName` 은 읽고 던지므로 그걸로 걸러 이름을 남기고 건너뛴다. fork `fix/hades-network-boundary` 에 push 했다.
  - **가드.** `ScriptCompilationTests` — **처음 쓴 것은 고침 없이도 통과했다.** 격리 서버 폴더엔 네이티브 DLL이 없어서였다(이 결함이 오래 살아남은 이유이기도 하다). 시험이 직접 `game/mss32.dll` 을 만들어 놓게 고치니 빨간불이 떴고, 고침을 되돌렸다 되살리며 양쪽을 확인했다. 47 + 49 통과.
- 2026-09-10 — **화면이 다른 크기에서 깨지는지 미리 본다.** `--layout` 이 각 줄의 자리를 찍고 넘치거나 겹치면 실패로 끝낸다. `scripts/check-layout.ps1` 이 시안의 두 기준과 그 양옆(16:9~21:9, 세로 640~800)을 인벤토리 닫은 채·연 채로 열두 번 돈다.
  - **첫 실행에 진짜 버그를 잡았다.** 조작 줄의 `ThumbSpanMaximum`(680)이 **최소** 너비로 걸려 있어서 640 폭 화면에서 좌우로 20씩 삐져나갔고, 세로 칸은 가장 넓은 자식을 따라가므로 **상태 막대까지 같이 끌려 나갔다**. 상한은 상한이어야 한다 — `Main.Capped` 가 남는 폭을 좌우 여백으로 돌린다.
  - 세로에서는 인벤토리 줄이 닫혀 있어도 자리를 먹어 월드 아래 죽은 띠가 생겼다. 시안대로 **월드 아래 전폭**이고, 닫히면 줄째로 사라진다.
  - **높이가 폭보다 먼저 터진다.** 세로 780은 시안이 52+400+76+252 로 이미 다 쓰고 있어서, 새 줄에 최소 높이를 주면 반드시 무언가가 밀려난다. 새 칸은 남는 높이를 나눠 갖게 한다.
- 2026-09-10 — **괴물 세우기 시도 — 아직 안 뜬다.** 전투가 막혀 있는 유일한 이유라 안전 가옥에 템플릿을 넣어 봤다(`bees` 를 베껴 AreaID 1, 24,27, `safehouse_wasp`).
  - 서버는 읽는다(`Monster Templates Loaded: 3 → 4`). 스폰 조건도 다 맞아 보인다 — `SpawnType 4` 는 `Defined`(`SpawnQualifer`), `GlobalSpawnTimer` 는 1초, `MonsterCreationScript "Create Monster"` 는 `scripts/Creations/monsters.cs` 에 그 이름으로 등록돼 있고, 맵 넷 다 `Rows`·`Cols` 가 0 이 아니다(`ManageSpawns` 는 0 인 맵을 만나면 `continue` 가 아니라 **`return`** 이라 전체가 멈춘다 — 지금은 해당 없음).
  - 그런데 클라이언트에 `0x07` 이 한 번도 안 온다. 다음에 볼 곳은 `Create` 뒷부분의 null 반환과 `AddObject`.
- 2026-09-10 — **인벤토리.** 서버가 보낸 소지품(`0x0F`)을 읽어 오른쪽 패널에 목록으로 보여준다. 열려 있는 동안 걷기와 월드 탭이 막힌다(`WorldView.Frozen`). 장착·사용·버리기는 시안대로 범위 밖.
  - **먼저 확인하고 만들었다.** 이 세션에서 반복된 함정이 "만들고 나서 확인할 수 없다" 였다 — 그래서 시작 전에 캐릭터 소지품을 열어 봤고, 59칸이 전부 비어 있었다.
  - **`give` 는 채팅 명령이 아니라 주문이다.** 서버가 시작할 때 찍는 `(Usage: give <item> [amount])` 줄은 명령 목록이 아니라 **주문 목록**이었다. 캐스팅 경로를 만드는 대신 캐릭터 파일의 `Inventory.Items` 에 최소 모양으로 넣었다(`Template.Name` 만 맞으면 서버가 로그인할 때 채운다). 방법은 `docs/mobile-client.md` 4절.
  - **같은 목록에 `cm`(괴물 생성)도 있다.** 그것도 주문이지만, 괴물 템플릿 폴더에 넣는 길이 남아 있다 — 전투가 막혀 있는 유일한 이유가 그것이라 다음 할 일로 올렸다.
  - 헛디딘 것 둘: 패널에 최소 높이를 박았더니 위·아래 줄이 **화면 밖으로 밀렸고**, "바뀐 게 없으면 다시 그리지 않는다"는 가드가 **빈 목록의 첫 상태를 걸러** 안내 문구가 안 나왔다.
  - `--pack`·`--say` 를 붙였다(손 없이 확인용). 시험 49 + 46 통과.
- 2026-09-10 — **대상 고르기.** 사람을 탭하면 발밑에 고리, 머리 위에 화살표, 위 가운데에 이름이 뜬다. 색이 아니라 모양 둘로 표시한다(시안 7절 "색만으로 구분하지 않음").
  - **HUD가 화면 전체의 탭을 먹고 있었다.** 가로 모드에서 HUD가 월드 위에 통째로 얹혀 있고, 그 안의 빈 칸(`Control`)이 기본값 `Stop` 이라 어디를 눌러도 월드까지 닿지 않았다. HUD 컨테이너와 빈 칸만 `Ignore` 로 바꿨다 — 판·버튼은 그대로 자기 탭을 받는다. **버튼이 아닌 곳을 눌러도 아무 일이 없다면 이걸 먼저 의심한다.**
  - `--pick` 을 붙였다. 서버가 보여 준 첫 사람을 **진짜 탭 이벤트로** 골라 본다(`Viewport.PushInput`), 손 없이 확인하려고. 성공하면 `GREYBOX_PICKED <이름>` 이 찍힌다.
  - 그 리허설이 처음엔 빈 바닥을 눌렀다 — 사람이 나타난 그 프레임에는 **화면이 아직 서버가 말한 자리로 미끄러지는 중**이라 좌표가 어긋난다. 60프레임 기다렸다가 누른다.
  - 전투는 여기서 멈춘다. **이 서버 안전 가옥에는 괴물이 없어** 대상 HP·공격 결과를 확인할 방법이 없다.
- 2026-09-10 — **막히지 않을 장치를 만들고, 그걸로 색까지 끝냈다.**
  - **`scripts/find-in-sources.ps1`** — 참고 저장소 16개를 `git grep` 으로 한 번에 뒤진다. 커밋된 파일만 보므로 `node_modules` 에서 멎지 않고 1초 안쪽이다. 길잡이는 **`docs/where-the-answers-are.md`** (질문 → 어디를 보나 표, 순서 규칙, 함정 셋).
  - 만들자마자 세 가지가 걸렸다. ① **색 규칙** — 팔레트 98번부터 여섯 칸을 색표로 덮어쓴다(`da-lib` 의 `Palette.Dye`, `PALETTE_DYE_INDEX_START=98`). ② **내 바지 처리가 틀렸다** — 몸 바이트 아래 반쪽은 바지 *번호*가 아니라 바지 *색*이고, 남자는 언제나 `mn001`, 여자는 바지가 없다(그래서 `wn001` 이 아카이브에 없었다). ③ **`Legend.dat` 이 저장소에 있다** — 문서에는 "설치 클라이언트에만 있다"고 적혀 있었다(`.../database/archives/legend/Legend.dat`). 표 16개는 이미 뽑아 둔 것과 같아 결과는 같았지만, 그 문장 때문에 한 번 접었다.
  - **원작 `0x33` 과 우리 서버의 `0x33` 은 다르다.** 참고 클라이언트의 패킷 정의와 대조했다 — Hades 는 무기를 1바이트로 쓰고(원작 2바이트), 장신구 색·장신구 셋째·도포 색·몸 색·투명·얼굴형·무리 이름을 안 보낸다. 우리 파싱은 **Hades 기준**이 맞고, 실서버 시험이 그것을 확인한다.
  - **색.** 색깔 수만큼 그림을 뽑으면 파일이 폭발하므로, 염색 자리를 **표시색**(`255,0,250`~`255,0,255`)으로 구워 두고 클라이언트가 그 화소만 갈아 끼운다(`Palettes.cs`, 조각·색 쌍마다 한 번). 표시색이 팔레트의 다른 자리와 겹치면 굽는 쪽이 멈춘다. 염색되는 부위는 머리·신발·바지 셋뿐이다.
  - 확인: `wren`(머리색 20 파랑)·`friend`(40 주황)으로 붙여 찍었다. 시험 46 + 46 통과.
- 2026-09-10 — **무기 오프셋을 찾았고, 나도 서버가 말한 옷을 입는다.**
  - **답은 참고 클라이언트가 이미 뽑아 둔 아틀라스에 있었다** — `sources/FallenDev/dark-ages-ts/apps/client/public/aislings/*/*.atlas` 의 `offsets:` 줄. 부위마다 **바탕 크기**가 적혀 있고, 겹칠 때는 **바탕의 가운데를 포갠다.** 왼쪽 위를 맞추면 무기가 사람 옆에 뜬다.
  - **바탕 = max(EPF 머리말, 57x85).** 작은 조각(방패 15x28·모자 20x16·갑옷 27x39)은 머리말이 그림 크기를 담고 있어 그대로 믿으면 안 되고, **무기·장신구만 111x85** 를 선언한다. 그래서 무기는 (111−57)/2 = 27 만큼 왼쪽으로 당겨야 손에 들린다.
  - 확인 순서: EPF 머리말 넷째 칸(둘 다 `0000`) → `khan.dat` 안 `.tbl`(전부 색표) → **아틀라스**. 앞 둘은 헛짚었고 세 번째가 답이었다.
  - 칸이 `47x83` → `80x88` 로 넓어졌다(무기가 옆으로 뻗는다). 칸이 넓어지면 발 위치를 칸 가운데로 계산할 수 없어 `Sheet` 가 발 자리(31.5, 83)를 직접 들고 있게 바꿨다.
  - **내 캐릭터.** 서버는 나에 대해서도 `0x33` 을 보내는데 `Show` 가 나를 버리고 있었다(`0x05` 로 내 번호를 알기 *전에* 오면 남으로 들어갔다가 지워졌다). `Self` 로 따로 들고, 도착하면 그때 내 figure 를 다시 세운다.
  - 확인: `wren`(머리 3)·`friend`(머리 8)로 붙여 둘이 다르게 나오는 것을 찍었다. 시험 42 + 46 통과.
- 2026-09-10 — **사람마다 다른 옷.** `0x33`은 아홉 바이트 뒤에 **옷 21바이트와 이름**을 더 보내고 있었는데 우리는 전부 버리고 있었다. 그래서 남이 전부 같은 갑옷을 입고 일련번호로 불렸다.
  - **이름을 되읽는 것이 곧 옷 자리의 증명이다.** 이름은 옷 21바이트 **뒤에** 오므로, 실제 서버에서 이름이 맞게 나오면 그 사이 모든 자리가 맞은 것이다(`MobileClientProtocolTests`).
  - **`0x0C`(걸음)에는 옷이 없다.** 그냥 새로 만들면 걷는 순간 사람이 발가벗는다 — 알던 사람을 `with` 로 옮기게 고쳤다.
  - **몸 한 바이트는 두 가지다** — 위 반쪽이 몸 종류(16 남·32 여·48 남유령…), 아래 반쪽이 바지 번호. 그래서 `Wardrobe` 가 성별 글자와 바지를 한 바이트에서 뽑는다.
  - **부위는 칸 좌표를 공유한다.** 그래서 따로 뽑아도 **같은 칸 크기**로만 뽑으면 그대로 겹쳐진다(`pose … 47x83`). 칸을 부위마다 맞추면 모자가 벗겨진다. `assets/actor/parts/` 에 22장.
  - **여자 그림에는 색표가 없다.** `khan2.dat` 에 팔레트가 없어 그리다 널 참조로 죽었다 — `pose` 가 옆집 `khan.dat` 색표를 빌리게 했다.
  - **무기는 안 겹쳐진다.** `mw001`은 몸이 19–57을 쓰는데 63부터 그려진다. 기준점이 다른 것으로 보이는데, 이 서버에 무기 든 사람이 없어 맞춰 볼 대상이 없어 빼 뒀다. 색(팔레트 교체)도 남았다.
  - 확인: 지역 서버에 `wren`·`friend` 둘을 붙여 한 장 찍었다 — friend 가 자기 여자 몸과 머리로 나온다. 시험 42 + 46 통과.
- 2026-09-10 — **P0-14~17 완료. 서버 안정화 항목이 전부 닫혔다** (격리 시험 46개 통과, 약 1분).
  - **P0-14 송신** — 송신 경로가 셋이었고 각자 다르게 패킷을 잃었다. `FlushAndSend`는 다른 송신 중이면 **말없이 안 보냈고**, `BeginSend` 후 `EndSend`를 안 해 나간 바이트 수를 본 적이 없다. 나머지 둘은 `Socket.Send` 반환값을 버려 **패킷이 반만 나갈 수 있었다**(받는 쪽이 길이를 읽고 기다리므로 스트림이 영영 어긋난다). 연결마다 쓰기 스레드 하나로 바꾸고, 안 받아준 나머지를 다 나갈 때까지 다시 내민다. 한도(`SendQueueDepth` 512) 넘으면 그 연결을 포기.
  - **P0-15 저장** — `File.WriteAllText`가 비우고 채워서, 중간에 멈추면 캐릭터가 사라졌다. 옆에 쓰고 디스크까지 내린 뒤 바꿔 넣고, 옛 것은 백업으로 남긴다. 한 파일 한 번에 하나. 못 읽으면 백업으로 폴백.
  - **P0-16 전투** — 공격 간격 검사가 `(마지막 - 지금) > 지연`이었다. 마지막은 과거라 늘 음수 → **한 번도 동작한 적이 없다**(설정값의 500배로 공격 가능). 그리고 **시전 루프가 무한**이 될 수 있었다 — 빈 슬롯 시전이 오면 스택에서 안 빼고 루프 처음으로.
  - **P0-17 설정화** — 객체 서버 주소가 코드에 박혀 한 PC에 서버 한 대. 설정 키 + 환경변수(`ServerConfig__ObjectServerPort`)로 빼고 harness가 실행마다 빈 포트를 준다. **덕분에 격리 시험이 병렬로 돌아 3분 → 1분.**
  - **네 가지 모두 되돌려서 시험이 빨개지는지 확인했다** — 옛 비교식·박힌 포트·잠금 제거 전부 해당 시험을 실패시킨다.
  - **흔들리는 시험 하나를 지웠다.** "읽지 않는 클라이언트가 남을 굶히는가"는 **세 번에 한 번 실패**했다. 성질은 진짜고 답도 "그렇다"로 보이지만, 서버가 얼마나 일할지에 달려 측정이 흔들렸다(`Format38Handler`는 `IsRefreshing`이면 무시). 3분의 1로 빨개지는 시험은 빨간불을 무시하게 만든다 — 계획서 2.3절에 빈도까지 적어 두고 지웠다. 상한 정책 자체는 `SendQueueTests`가 결정적으로 확인한다.
  - 서버 fork: `fix/hades-network-boundary`에 4개 커밋 추가, push 완료.
- 2026-09-10 — **주변 사람이 서버에서 온다.** 다른 사람이 접속하면 나타나고, 걸으면 따라 움직이고, 나가면 사라진다. 접속 상태에서는 우리가 세워 둔 가짜 주모·말벌이 없어진다(오프라인에서는 남는다 — 물어볼 서버가 없으니까).
  - **코드를 읽기 전에 먼저 들어 본 것이 더 나았다.** 입장 직후 서버가 보내는 것: `0x04 0x05 0x08 0x0A 0x15 0x2C 0x33 0x49`. **`0x07`은 하나도 안 온다** — 이 서버의 안전 가옥에는 괴물도 상인도 없다(템플릿 폴더가 비어 있음). 만들었어도 확인할 방법이 없었을 것이라 남겨 뒀다.
  - 읽는 것: `0x33` 사람 표시(칸·방향·serial), `0x0C` 누가 걸음(**걸어온 칸**을 주므로 방향을 더해야 지금 칸이 된다), `0x0E` 사라짐.
  - **나와 남을 가르는 데 `0x05`가 필요했다** — 로그인 서버가 준 번호는 문을 여는 번호일 뿐, 월드가 캐릭터를 부르는 번호가 아니다. 처음엔 그걸로 걸러서 **자기 자신을 남으로 보고 있었다.**
  - 한 서버에 클라이언트 둘을 붙여 확인: 상대가 입장 칸에 서 있는 것을 보고, 동쪽으로 한 걸음 딛는 것을 방향까지 맞게 보고, 자기 자신은 목록에 없고, 상대가 끊으면 사라진다. 실제 화면으로도 두 캐릭터가 나란히 찍혔다.
  - 지역 서버 두 번째 계정은 `tmp/hades-run/database/server/aislings/friend.json`(wren 사본). 클라이언트에 계정 생성이 없어서 파일로 만들었다.
  - 남은 것: 사람마다 다른 옷(`0x33` 뒷부분), `0x07`(괴물 있는 맵이 생긴 뒤).
- 2026-09-10 — **위치를 서버에서 받는다.** 캐릭터가 우리가 정한 자리가 아니라 서버가 말한 칸에, 서버가 이름 붙인 지도 위에 선다. 걸으면 서버에 알린다.
  - **서버는 허락한 걸음에 아무 말도 하지 않는다** — 주변 사람에게만 알린다. 그래서 클라이언트가 자기 그림을 먼저 움직이고 정정을 기다린다. 읽기는 문답이 아니라 루프여야 한다: `WorldClient.PumpAsync`가 최신 상태를 들고 있고 모르는 패킷은 넘긴다.
  - **거절할 때만 진짜 칸을 보내온다.** 그리고 **너무 빨리 걸으면 거절한다**(속도 제한). 둘 다 무시하면 걸음마다 튕긴다 — 서버가 마지막으로 말한 자리로 스냅한다.
  - 패킷: `0x15` 지도(번호·크기·이름), `0x04` 위치(칸), `0x06` 걷기(방향 0북 1동 2남 3서 + 걸음 수), `0x38` 새로고침. **`0x15`·`0x04`는 암호화돼 있고 재접속 안내(`0x03`)는 평문**이다.
  - `IsometricFloor` — 칸이 그림 어디에 떨어지는지. `tools/dat-extract`의 렌더러와 **한 픽셀도 다르면 안 된다**. 시험이 그림 크기(1708×820)와 "화면 한 걸음 = 칸 한 걸음"을 못 박는다.
  - 실행 확인: 서버가 21,29에 남겨 둔 캐릭터로 로그인해 그 자리에 그려졌고(오프라인 기본값 4,4가 아니라), 북쪽 4번 걸으니 HUD가 `Safe House · 22,25`가 되고 등을 보였다.
  - **시험 두 개를 다시 썼다.** 거절 시험이 **아무것도 검사하지 않고 통과**하고 있었다 — 걷기 전부터 칸이 기대값과 같았다. 고쳐 놓은 두 번째 판은 **지도 끝이 아니라 속도 제한을 재고** 있었다(열린 바닥으로 걸어도 통과). 지금은 재는 것을 그대로 이름 붙였고 **대조군을 같은 시험 안에** 넣었다 — 천천히 한 걸음은 침묵, 몰아서 여러 걸음은 정정.
  - **주의(멀티 에이전트)**: `git add tests` 로 디렉터리째 담다가 다른 에이전트가 만든 `tests/docs-dashboard.test.js`가 내 커밋에 섞여 들어갔다. 같은 작업 폴더를 나눠 쓸 때는 **경로를 명시해서 stage** 한다.
- 2026-09-10 — **Godot에 원작 그림을 올렸다.** greybox의 회색 상자가 사라지고 안전 가옥 바닥·도포 입은 캐릭터·주모·말벌이 실제로 그려진다. 자산은 `scripts/build-client-assets.ps1`이 저장소 `.dat`에서 직접 뽑는다(손으로 고친 그림 없음).
  - 방향 규칙을 `Lod.Mobile.Core.Art`로 옮겼다 — 엔진 없이 시험할 수 있다. 북·서는 등, 동·남은 앞이고 서·남은 좌우 뒤집기. 시험 4개.
  - **배우의 원점은 발 사이**다. 그래야 뒤집을 때 제자리에서 돈다 — HTML 시안에서 걸렸던 그 함정이다.
  - **카메라 Y 정렬**을 켰다. 마름모 바닥에서는 화면 높이가 곧 깊이라, 앞에 선 사람이 앞에 그려져야 한다.
  - HUD를 고쳤다 — 평평한 회색 위에서는 필요 없던 일인데, 금색 바닥 위에서는 글자와 버튼이 안 보인다. 상태 줄·알림·모든 버튼에 불투명 판을 깔았다. 텍스처는 보간 없이 가져온다(2007년 픽셀 그림을 부드럽게 하면 다 뭉갠다).
  - 실행 인자 `--walk NESW` 추가(기존 `--login`과 같은 성격). 화면에 손 안 대고 걷는 모습을 확인했다 — 서 4칸·북 6칸·동 4칸 모두 방향과 그림이 맞는다.
  - `.gitattributes` 추가: Godot가 쓰는 `.import`·`.uid`는 Godot가 쓰는 줄바꿈 그대로 두고, 원본 게임 자료는 건드리지 않는다.
  - **`dat-extract mpf`의 `투명` 인자가 PowerShell에서 안 넘어갔다** — 한글 인자가 네이티브 실행 파일로 전달되며 깨져 말벌 시트에 배경이 칠해져 나왔다. `transparent`도 받게 했다.
  - 남은 것: 월드의 위치는 아직 우리가 정한 값이다. 서버가 보내는 지도·주변 사람·이동을 읽는 것이 다음.
- 2026-09-10 — **모바일 클라이언트가 실제 서버에 로그인해 월드에 들어갔다.** P0의 목적이 처음으로 관통됐다.
  - **이전 세션이 시험만 쓰고 끝나 있었다** — `mobile/tests`에 3종(프레임 부호기·CP949·7.18 로그인)이 있는데 `mobile/src`에 `.cs`가 0개였고 `using Xunit;`도 없어 컴파일조차 안 됐다. 커밋도 안 된 상태. 시험이 잘 쓰여 있어 다시 쓰지 않고 통과시켰다.
  - `Lod.Mobile.Core.Protocol` — 프레임(`0xAA`+길이 BE+본문), CP949 StringA(길이는 **바이트 수**), `HadesCipher`(서버와 같은 방식, 자기 자신이 역함수).
  - **시험이 자기 정답지를 보는 문제**를 두 가지로 막았다 — (1) 서버 어셈블리를 오라클로 삼아 암호 결과를 바이트 단위 비교, (2) 격리 서버를 띄워 실제 로그인. **반증 확인**: 비밀번호를 틀리면 실패한다(ordinal을 바꾸는 건 통과하는데, ordinal은 패킷에 실려 가므로 그게 맞다).
  - `Lod.Mobile.Core.Net` — `HadesConnection`(소켓 하나를 프레임 단위로, 남은 바이트 보관), `HadesLoginClient`(인사→로비→로그인→월드, 소켓 3개).
  - **알아낸 두 가지**: 재접속 안내(0x03)는 평문인데 그 옆의 쪽지(0x02)는 **암호화**돼 있다 — 거절 사유를 읽으려면 풀어야 하고, 서수는 명령 다음 첫 바이트에 평문으로 실려 있다. 그리고 **로그인이 거절되면 서버는 쪽지만 보내고 조용해진다** — 제한 시간이 없으면 틀린 비밀번호에 그대로 멈춘다. 성공하면 쪽지 직후 안내가 오므로 쪽지를 받은 뒤 2초만 더 기다린다.
  - Godot 로그인 화면이 실제로 접속한다. **화면은 로그인 작업에서 트리를 건드리지 않는다** — 진행 상황을 큐에 넣고 `_Process`가 꺼낸다(Godot 노드는 메인 스레드에서만 바꿀 수 있다).
  - 실행 확인: `tmp/hades-run` 서버에 붙어 화면에 "wren 님, 월드에 들어왔습니다.", 서버 로그에 `wren : Welcome to Lorule`. 틀린 비밀번호는 서버의 원문 "Sorry, Incorrect Password."를 그대로 보여준다.
  - 실행 인자 추가: `--login <이름>:<비밀번호>`, `--shot-after <초>` (기존 `--screen`·`--orient`·`--shot`과 같은 성격).
  - 정리: 격리 시험 묶음을 net9.0으로 올렸다(모바일 라이브러리·Godot과 맞춤). 기존 23개 그대로 통과, 지금 26개. `.gitignore`에 `bin/`·`obj/`가 없어 빌드 산출물 162개가 한 번 커밋됐다 — 되돌리고 규칙을 넣었다.
- 2026-09-10 — **동작 구간의 권위를 찾음 + 좌우 뒤집기 버그 수정.** 사용자가 "북·서가 같고 동·남이 같다"를 지적했고, 원인이 두 겹이었다.
  - **버그**: 시안의 `Actor.draw`가 `face()`/`step()`이 걸어 둔 `flip` 클래스를 바로 다음 줄에서 지우고 있었다. 그래서 좌우 뒤집기가 한 번도 안 걸렸다. 뒤집는 축은 칸 한가운데가 아니라 **발이 닿는 자리**여야 한다(원작 조각이 칸 안에서 치우쳐 있음) — `transform-origin: calc(50% + var(--ox)) center`.
  - **구조**: 원작은 방향을 네 벌 그리지 않는다. 등을 보이는 벌과 앞을 보는 벌 **두 벌**뿐이고 서·남은 좌우 뒤집기다. 저장소의 참고 클라이언트 `sources/FallenDev/dark-ages-ts`가 같은 규칙(`directionFrame`, `setScale(-1,1)`, 방패 z순서 교체)을 쓴다.
  - **권위 표를 찾음**: `Legend.dat`의 **`skill.tbl`** 에 원작 개발자의 한글 주석과 함께 들어 있다. `NO`(스킬번호) `FN`(직업 그림 b=1 성직·c=2 전사·d=3 도가·e=4 도적·f=5 마법) `SI`(시작 칸) `FC`(한 방향 프레임 수) `ST`(입을 수 있는 옷 번호). **한 스킬 = `SI..SI+FC-1`(등) + `SI+FC..SI+2FC-1`(앞)**. 18행이 다섯 직업 파일을 빈틈없이 덮고, 우리가 EPF에서 센 실제 칸 수(b14·c30·d18·e36·f12)와 정확히 일치한다 — 교차 검증됨.
  - **"파일을 반으로 나눈다"는 추정은 틀렸다.** `01`·`02`에서만 우연히 맞는다(묶음이 하나뿐이라). `c`처럼 묶음이 다섯이면 SI/FC로 계산해야 한다.
  - `MobTile.tbl`(몬스터 행동: `fStop`·`fChgDir`·지연)도 같은 아카이브에 있고, 1번(말벌)의 `SFCnt/WFCnt/AFCnt=2/2/2`가 우리 `mpf` 파서 결과와 일치.
  - **`Legend.dat`은 이 저장소에 없다** — 설치 클라이언트(`C:\Program Files (x86)\KRU\Dark Ages`)에만 있다. `cious.dat`도 마찬가지.
  - 정리: `docs/original-sprite-animation.md` (방향 규칙·동작 접미사·skill.tbl 전체 표·효과 그림·서버 패킷·파일 이름 읽는 법).
- 2026-09-10 — **사람·괴물 그림 형식 해독 + 세로 화면을 가로와 같은 무게로 설계.** 남아 있던 아카이브 세 개를 모두 열었다.
  - **HPF**(`ia.dat` 19,664개, 실내 사물) — `55 AA 02 FF`로 시작하는 적응형 splay-tree 허프만. 표가 파일에 없고 읽는 쪽이 바이트마다 트리를 다시 만든다. `LOD_`가 이 폴더를 "standing_chars"로 불렀지만 실제 내용은 가구와 벽장식이었다.
  - **EPF**(`khan.dat` 10,417개 남 / `khan2.dat` 10,404개 여) — 압축 없음. 12바이트 머리말 뒤 전부가 12번째 바이트 기준 주소. 칸 순서는 `top, left, bottom, right`이고 화소는 행 우선(저장소의 두 구현이 서로 다른 해석을 쓰는데, `sources/wren11/da-lib`의 것이 맞고 회전이 필요 없어 단순하다). 팔레트는 부위 글자별 표(`palh.tbl`=모자, `palc.tbl`=망토 …)에서 번호로 찾고, `-1`은 남자 전용 `-2`는 여자 전용 행이다.
  - **MPF**(`hades.dat` 742마리) — 머리말에 서기·걷기·공격 프레임 구간이 적혀 있다. 742개 중 133개만 `-1,-1` 칸에 자기 팔레트 번호를 싣고 나머지는 `mns000.pal`을 쓴다.
  - 부위 글자: `mb`몸 `ma`팔 `mh`모자 `mc`망토 `mi`도포 `mu`갑옷 `mn`바지 `ml`신발 `ms`방패 `mw`무기 `mg`날개 `mo`얼굴가리개 `mp`장신구. 한 사람은 이것들을 **겹쳐 그린다** — `pose` 명령이 그렇게 만든다.
  - 도구: `tools/dat-extract`에 `epf` `mpf` `pose` 추가, `map`에 잘라내기 인자 추가.
  - **세로 모드**(사용자 지시: "hud는 세로 모드도 처음부터 진행해") — 세로는 가로를 눌러 담은 것이 아니다. 780줄 중 월드가 400을 통째로 쓰고 조작이 그 아래 따로 앉아 **손가락이 겨누는 것을 가리지 않는다**. 그렇게 남는 자리에 원작이 화면 아래에 두었던 대화창이 되살아난다. 가로 360줄에는 그 여유가 없어 HUD가 월드 위에 겹치되 겹치는 자리를 아래 양쪽 모서리로 몬다. 근거와 칸 나눔은 `docs/mobile-test-v1-wireframes.md` 2.3절.
  - 반영처: `docs/ui/hud-mockup.html`(실시간 전환 + 방향 버튼으로 실제 걷기 + 카메라 추적), `docs/ui/wireframes.html`(6장 전부 전환), `mobile/client`(`--orient portrait`).
  - 미해결: 벽 타일(`TILEAS.BMP`는 고정 크기 아님), 아이템 아이콘. `mobile/src`·`mobile/tests`에 이전 세션의 프로토콜 작업이 커밋 안 된 채 남아 있다.
- 2026-09-10 — **원본 `.dat` 직접 추출 + 화면 6장 + 작업 현황 페이지.** 사용자 지시로 `LOD_` 산출물 반입을 중단하고 저장소 자체 아카이브에서 자산을 뽑는 방식으로 전환했다(설치 클라이언트와 대조: 10개 중 9개 바이트 동일, `setoa.dat`만 버전 차이). `tools/dat-extract` 신설 — 저장소에 있으나 아무도 안 쓰던 `Hades.Client.Base`를 감쌌다. `list`/`dump`/`tiles`/`map` 네 명령. **`seo.dat`에서 타일 19,243장·팔레트 74개·팔레트 표 488개를 읽고, `lod1.map`(안전 가옥 30×31)의 바닥을 실제로 그렸다.** 시안 배경을 전부 이것으로 교체했다.
  - `docs/ui/wireframes.html` — 와이어프레임 6장(로그인·캐릭터 확인·기본 게임·NPC 대화·전투·인벤토리)을 눌러볼 수 있게 구현.
  - `docs/index.html` — **작업 현황 페이지.** 왼쪽 메뉴로 지금·로드맵·작업 흐름·화면·자산·결정을 본다. Graphite 스택 개념을 처음 보는 사람 기준으로 설명하고, 커밋 타임라인에서 작업 공간 커밋과 서버 사본 커밋을 색으로 구분한다.
  - 미해결: 벽 타일(`TILEAS.BMP`는 고정 크기 아님), 캐릭터 스프라이트(아카이브 위치 미확정). 시안에서는 점선 자리로 정직하게 표시했다
- 2026-09-10 — **greybox 2장 완성 + 발견 사항 수정**(`mobile/client/`, Godot 4.6 + C#). 로그인과 기본 게임 HUD를 세우고 실제 렌더링을 캡처했다. 기준 뷰포트를 800×360으로 잡아 **1 단위 = 1dp**가 되게 했다 — 1280×720이면 코드의 48이 기기에서 3분의 1로 줄어 터치 크기 판단이 틀어진다. 한글은 정상 표시되며 화면이 실제 사용 글꼴을 스스로 보고한다(윈도우 맑은 고딕. **안드로이드·iOS에는 그 경로가 없어 번들 글꼴이 필요**). `--screen`·`--shot`으로 에디터·기기 없이 캡처할 수 있어 화면 회귀 검사에도 쓸 수 있다.
  - **고친 것 ①** 로그인 폼이 가로 화면 세로를 거의 다 써서 키보드가 올라오면 가려졌다 → 캡션을 필드 옆으로 옮겨 두 줄을 줄이고, `VirtualKeyboardGetHeight()`만큼 폼이 올라가게 했다(와이어프레임 2.1의 키보드 규칙 구현).
  - **고친 것 ②** 왼손 엄지가 화면 왼쪽 아래 월드를 가렸다 → 하드코딩 좌표 대신 **HUD 두 행을 실측해 "게임 초점 영역"을 계산**하고 그 안에만 객체를 둔다. greybox에서는 그 영역을 테두리로 보여준다.
  - **새로 발견(미해결)** 이동 패드가 3행이라 **화면 높이의 42%를 차지**해, 초점 영역이 객체 한 줄 높이밖에 안 된다. 패드를 월드 위에 반투명으로 겹칠지 등은 실기기 검토 사항으로 남긴다.
  - 배운 것: `MarginContainer`에 자식을 여럿 넣으면 모두 같은 rect를 받아 겹친다. 두 화면 모두 세로 3단(상단·확장·하단) 구조로 통일했다. Godot 노드 이름에는 `/`를 쓸 수 없어 표시 글자와 노드 이름을 분리했다
- 2026-09-10 — **와이어프레임 결정 4건 확정 → greybox 착수 가능**(문서 v0.3, 상태: 승인됨). ① UI 문구는 한국어 고정, 서버 메시지는 영문 그대로 — 한글의 위험은 번역이 아니라 렌더링(글꼴·버튼 내 넘침·CP949↔UTF-8)이므로 껍데기만 한국어로 두면 번역 없이 그 셋을 첫 화면부터 검증한다. ② 스프라이트는 `.dat` 추출이 선행돼야 하므로 공통 회색 사각 자리 표시 — 자리가 비면 터치 영역·간격 판단이 불가능하고 그게 greybox의 목적이다. ③ 뒤로 가기 = 캐릭터 선택 로그아웃(확인 1회) / 대화·인벤토리 닫기 / 게임 화면은 "한 번 더 누르면 종료". ④ 인벤토리는 단순 목록(현재 아이템 템플릿 3종뿐이라 슬롯 격자는 빈칸 그리기). 아울러 **사용자 피드백 반영** — 지금까지 산출물이 전부 눈에 안 보이는 서버 작업이라 진행 상황을 이해하기 어렵다는 지적. 대응으로 같은 테스트 23개를 원본 서버에 돌려 비교 수치를 뽑았고(**원본 16통과/7실패 → fork 23통과/0실패**) 현황을 Artifact 한 장으로 정리했다. 우선순위를 화면 greybox로 올리고 서버 P0-14~17은 병행한다
- 2026-09-10 — **P0-13 완료 — 멈춘 프레임 시간 제한**(테스트 23개 통과, fork edab84719). 게임 서버엔 유휴 검사가 있었지만 **로그인 서버엔 없어서** 헤더 3바이트만 보내고 조용해지면 소켓을 영원히 붙잡을 수 있었다. 무조건적 연결 수명 제한은 사람이 계정을 만드는 중인 연결까지 끊으므로, **"프레임이 진행 중인 채로 멈춘 연결"**만 1초 주기로 걷어낸다(`NetworkSocket.HasPartialFrame` + `LastReceivedUtc`). 프레임 사이에서 쉬는 연결은 진행 중이 아니라 안 끊긴다 — 유휴 연결이 같은 시간을 버티는지 대조군으로 확인. 제한은 `IncompleteFrameTimeoutSeconds`(기본 15초) 설정값이고, harness가 3초로 낮춰 쓰므로 **설정 키가 실제 반영된다는 것까지 테스트가 증명**한다
- 2026-09-10 — **P0-13 일부 — 연결 수명**(테스트 22개 통과, fork 9a81df0f9). 측정 결과 **소켓 정리는 이미 올바르다**: 인증 전에 떠나는 연결 50회 뒤 서버가 붙잡은 CLOSE_WAIT 소켓이 0개다. 이 좋은 소식을 테스트로 고정했고, 소켓 열거가 실제로 이 서버 연결을 본다는 대조군(연결 중 ESTABLISHED > 0)도 넣어 검사가 허위 통과하지 않게 했다. 접속자 컬렉션 동기화는 고쳤지만 **재현에 실패했다** — `Clients`가 접근마다 새 리스트를 만드는 프로퍼티라 `lock (Clients)`는 아무것도 잠그지 않았고 `AddClient`/`RemoveClient`는 소켓 콜백에서 잠금 없이 `Dictionary`를 변경했으나, 150개 동시 연결이 트래픽을 보내도 이 PC에선 터지지 않았다. 코드 검토 근거로 수정했고 관련 테스트는 증명이 아니라 회귀 방지임을 커밋에 명시했다. 남은 것: handshake 시간 제한(P0-10에서 넘어온 잘린 본문 처리)
- 2026-09-10 — **P0-12 완료 — 입장 티켓 인증 순서**(테스트 20개 통과, fork 0dfa1463f). `Format10Handler`가 `EnterGame`을 **먼저** 실행하고 그 뒤에 검사해서, 검증되지 않은 연결이 캐릭터를 로드하고 월드에 들어간 다음에야 끊겼다. 검사 대상도 요청이 아니라 방금 로드된 `client.Aisling`이었다. 요청의 이름으로 티켓을 먼저 소비하도록 바꿨다 — `Redirects.Remove`가 "이번 호출이 가져갔는지"를 반환하고 기존 `SyncLock`이 그걸 원자적으로 만든다. 계획서 5.2 정량 기준을 테스트로 확인: 고치기 전에는 **같은 티켓으로 2개 연결이 모두 입장**했고, 지금은 정확히 1개다. 중복을 피하려고 로그인→티켓 획득 구간을 `LoginFlow.LoginAndCaptureTicket`으로 뽑아 `EnterWorld`도 그것을 쓴다
- 2026-09-09 — **P0-11 완료 — 캐릭터 이름 정책**(테스트 19개 통과, fork 06b5c699a). 서버는 클라이언트가 보낸 어떤 이름이든 그대로 파일로 만들었다 — `a`·`two words`·`wren!`은 물론 Windows 장치명 `con`·`nul`·`com1`까지 캐릭터 파일이 됐다. 허용을 **영문·숫자·한글 완성형(U+AC00~U+D7A3)**으로 제한하고 길이는 설정값(`CharacterNameMinLength`/`MaxLength`, 기본 2~12)으로 뺐다. **한글 지원은 사용자 지시로 확정된 요구사항**이며, 근거도 확인했다 — 리더 `GetEncoding(0x3B5)`와 라이터 `GetEncoding(949)`는 **같은 CP949**라서(0x3B5=949) 프로토콜·서버는 이미 한글 이름을 나른다. 못 치는 것은 영문 7.18 클라이언트의 입력 필드뿐이다. 그래서 **정책을 넣기 전에** 한글 저장 테스트를 먼저 통과시켜 정책이 한글을 막지 못하게 고정했다. 테스트 클라이언트의 StringA도 ASCII에서 CP949로 바꿨다(길이는 문자 수가 아니라 바이트 수). 남은 UX 공백: 거부된 이름에 대해 클라이언트는 생성 성공으로 보이고 서버 로그에만 남는다 — 별도 응답 메시지는 P0-12 이후 다룬다
- 2026-09-09 — **P0-11 — 캐릭터 이름을 통한 임의 파일 쓰기 차단**(테스트 17개 통과, fork e49473f3b). 이름이 네트워크에서 그대로 저장 경로에 붙어 있었다. 테스트로 실증: `../escaped` → `database/server/escaped.json`, `../../escaped` → `database/escaped.json`. 서버가 쓸 수 있는 어느 위치든 **이름 선택만으로** 닿는다. `AislingStorage`의 load·save가 함께 지나가는 한 곳에서 경로를 해석하고 캐릭터 폴더 바로 아래가 아니면 거부하도록 했다(load는 이미 "없는 캐릭터"로, save는 로그 후 건너뛰기로 처리돼 호출부 수정 없음). 이 테스트도 처음엔 가짜로 통과했다 — 0x04 응답을 안 읽고 연결을 닫아 서버가 저장까지 가지 않았다. **정상 이름은 실제로 저장된다는 대조군**을 넣어 검사 유효성을 확보했다. 남은 것: 이름 자체의 정책(길이·제어문자), CP949 한글 이름을 막지 않도록 별도 결정 필요
- 2026-09-09 — **P0-10 완료 — 프레임 검증**(테스트 16개 통과, fork 2843f850e). `EndReceiveHeader`가 아무 3바이트나 헤더로 받아들이고 있었다. magic이 틀리거나 길이가 0이거나 수신 버퍼(65534)를 넘는 프레임이 **오지 않을 본문을 기다리며 연결을 영원히 붙잡았고**, 과대 길이는 버퍼보다 1바이트 더 읽으려 했다. 모든 읽기가 지나가는 한 곳에서 검증하고 socket error를 보고하게 했다 — 두 수신 콜백이 이미 그 조건에서 끊으므로 호출부 수정이 없다. 측정해보니 악성 프레임 5종이 같은 성격이 아니어서 테스트를 나눴다: **잘린 본문은 느린 송신자와 구분 불가**(P0-13의 handshake 시간 제한으로만 처리 가능), 미등록 명령은 구조적으로 멀쩡한 프레임. 그리고 연결 종료 탐지기가 처음엔 가짜였다 — read timeout도 `IOException`이라 전부 "닫힘"으로 보고했다. 타임아웃을 구분하고 **정상 연결은 안 닫힌다는 대조군**을 테스트에 넣어 검사 자체를 검증한다
- 2026-09-09 — **S1 착수: fork 생성 + P0-10 서버 크래시 수정**(테스트 15개 통과). `kimsangchan/Dark-Ages-Private-Server` fork를 만들고 submodule 리모트를 `upstream`(원본)/`origin`(fork)으로 나눴으며 root `.gitmodules`도 fork를 가리킨다. `fix/hades-network-boundary`에 첫 수정을 올렸다(49a0329a3). **악성 프레임 하나로 서버 프로세스 전체가 죽던 결함** — `ServerContext.Error`가 한 번도 할당되지 않는데 모든 소켓 콜백 catch 블록이 그것만 호출해, 수신 경로 예외가 IO 완료 스레드에서 `NullReferenceException`으로 unhandled 종료를 냈다. 오류 sink에 스스로 던지지 않는 기본 구현을 주는 한 곳 수정으로 해결. 테스트를 먼저 써서 RED(서버 사망 → 정상 클라이언트 접속 불가)를 확인한 뒤 고쳤다. 남은 것: 악성 프레임을 보낸 연결의 socket 정리(P0-13과 겹침). 또 S1 진입에 맞춰 원본 무변경 테스트를 "clean이어야 한다"에서 "실행 전후로 달라지지 않아야 한다"로 바꿨다 — fork에 작업 중 변경이 있어도 실행이 파일을 건드리지 않는 보장은 그대로 유지된다
- 2026-09-09 — **P0-02 완료 → S0 gate 통과**(테스트 14개). 갓 생성된 캐릭터의 최소 상태를 `Fixtures/hades-718-character.json`에 고정하고, 독립 실행 2회를 비교하는 의미 비교기를 붙였다. 실행 간 달라지는 필드는 `Created`·`LastLogged`·`SkillBook` 셋뿐이고 각각 이유를 fixture에 적었다 — `SkillBook`은 `GiveAssailOnCreate`로 주는 스킬의 `ID`가 난수 시리얼이라 다르고 나머지 필드는 완전히 같다(두 실행의 SkillBook을 실제로 떠서 확인). 고정값 비교기는 값을 일부러 틀리게 바꿔 RED가 나는 것까지 확인했다. 로그인 흐름은 재사용 가능한 `LoginFlow.EnterWorld`로 뽑아 두 테스트가 공유한다. 전제: 서버 선빌드 필요(`Staging/net5.0`), 2620 제약으로 스위트는 직렬 약 30초
- 2026-09-09 — **P0-01 완료 — 로그인→월드 입장 golden fixture**(테스트 12개 통과). 합성 계정 `lodharness`로 격리 서버에 붙어 16단계를 완주했다: 배너 → 버전 718 → 암호화 파라미터 → 0x57 → 로비 리다이렉트 → (재접속) 배너 → 0x10 → 0x60 → 계정 생성 0x02 → 캐릭터 생성 0x04 → 로그인 0x03 → 게임 리다이렉트 → (게임 포트) 0x10. 성공 판정은 서버 로그 `lodharness : Welcome to Lorule`이고, 리다이렉트가 격리 게임 포트를 가리키는 것도 함께 확인한다. fixture에는 명령 순서만 넣고 seed·salt·해시·serial은 매 실행 달라지므로 제외했다. 암호화는 재작성하지 않고 `Staging/net5.0/Darkages.dll`의 `SecurityProvider`를 참조해 정본으로 쓴다(net5.0 어셈블리가 net8.0 테스트에서 정상 로드됨, 빌드 산출물이 없으면 빌드 명령을 알려주는 MSBuild Error로 실패). 특성화가 잡아낸 예상 밖 동작: **재접속마다 handshake 배너(0x7E)가 다시 온다.** 또 xunit이 테스트 클래스를 병렬 실행해 격리 서버 2대가 2620에서 충돌하던 것을 발견해 스위트를 직렬화했다
- 2026-09-09 — **P0-01 착수 — 평문 핸드셰이크 재현**(테스트 11개 통과). 격리 서버를 상대로 `S2C 0x7E → C2S 0x00(버전 718) → S2C 0x00(서버테이블 해시 + 암호화 파라미터)` 순서를 잡아 `Fixtures/hades-718-login-flow.json`에 명령 순서만 고정했다(seed·salt·해시·serial 등 매 실행 달라지는 값은 의도적으로 미고정, 실제 계정·비밀번호 없음). 이 과정에서 **두 번째 격리 구멍**을 찾아 막았다 — `MServerTable.xml`의 `Port`가 2610으로 박혀 있어 로비 리다이렉트가 격리 밖 포트로 클라이언트를 보낸다. harness가 실행마다 격리 로그인 포트로 다시 쓰도록 했다. 사용자 지시(하드코딩은 장기적으로 정리)에 따라 계획서에 **2.3 하드코딩 상수 목록**과 **P0-17 설정화 작업**을 추가해 S1 검토가 반드시 집어가게 했다. 프레임 코덱이 `mobile/src/Lod.Mobile.Core`와 중복인데, 그쪽은 .NET 9 SDK가 없어 빌드가 안 되므로 지금은 분리해 두고 코드 주석에 단일 출처 통합 대상으로 표시
- 2026-09-09 — **P0-00 격리 harness 완료** (`tests/hades-characterization/`, net8.0 xunit, 테스트 9개 통과 15초). `IsolatedHadesServer`가 `Staging/net5.0`과 `database/server`를 임시 경로로 복사하고, 비어 있는 `aislings`·사용 중이 아닌 포트를 배정한 `LoruleConfig.json`을 써서 서버를 띄우고 종료 시 지운다. 원본 무변경 테스트는 격리를 일부러 깬 상태에서 실제로 실패하는 것까지 확인(`areas/*.json` 4개가 수정됨 → `git checkout`으로 원복). 진행 중 확인한 것 2가지: ① 이전 세션의 수동 Hades 서버(PID 89316)가 계속 떠서 2610·2615·2620을 잡고 있었음 — 사용자 승인 후 종료 ② `GameServer.Start`가 `http://localhost:2620/`을 **하드코딩**으로 열고, 그 `HttpListener` 예외가 `StartServers`의 `catch (SocketException)`을 통과해 **로그인 서버 없이 프로세스만 살아 있는** 상태가 된다. 계획서 2.1에 실증 항목으로 추가하고 harness는 2620 사전 검사로 즉시 실패하게 했다. 원본은 수정하지 않았고 S0 harness는 fork 대신 root 저장소에 둔다(계획서 1.1). 반복을 막기 위해 `scripts/stop-hades.ps1`(남은 서버 종료 + 2610·2615·2620 확인)을 추가하고 `docs/run-procedure.md` 2절 A·10절 A단계에 실행 전후 종료 단계를 넣었다
- 2026-09-09 — `docs/hades-p0-stabilization-plan.md`로 Hades 안정화 순서와 회귀 테스트 경계를 확정. 전면 리팩터링·DB 교체·추측성 성능 최적화는 선행하지 않고, S0 정상 7.18 특성화 → S1 네트워크·입장 인증·연결·송신·원자적 저장·기본 전투 안전선 → S2 비밀번호·남용 방지·정상 종료·지원 LTS·10인 soak 순서로 진행한다. fixture 기반 Godot 작업은 S0 뒤, 실제 Hades 연동은 S1 뒤, 5~10인 비공개 테스트는 S2 뒤로 gate를 분리했다. 기존 미커밋 모바일 테스트 골격과 원본 submodule은 변경하지 않음
- 2026-09-09 — GDC의 게임 UX 실무 모델과 Apple·Android·Godot 공식 가이드를 참고해 `docs/mobile-test-v1-wireframes.md` v0.2로 개정. 독립 화면 6장이 아니라 로그인·캐릭터 확인·게임 월드의 **3개 기본 프레임 + 대화·전투·인벤토리 3개 변형 상태**로 관리하고, 흐름 지도→box wire→상태 시트→실제 게임 화면 위 in-engine greybox→visual skin 단계로 분리했다. 초광폭 일괄 레터박스 대신 full-bleed 월드와 safe-area anchor, 정량 양손 도달성 검증을 사용하도록 PRD AC-014도 v0.5로 조정. 이번 변경은 조사·문서뿐이며 greybox 구현은 승인 후로 남김
- 2026-09-09 — PRD v0.4를 기준으로 `docs/mobile-test-v1-wireframes.md` 작성. 로그인·단일 캐릭터 확인·기본 게임 HUD·NPC 대화 모달·전투 상태·읽기 중심 인벤토리만 포함하고, Mermaid 흐름도와 가로 구조 와이어프레임·전환 조건·48×48 터치 영역·글자 크기·16:9~20:9 대응·21:9 이상 레터박스·노치/홈 표시 안전 영역·접근성 초점 규칙을 정의. 색상·장식·구현은 제외하고 검토 대기로 전환
- 2026-09-09 — **D-001 Windows/Android 기준 채택: Godot 4.6 + C#**. `experiments/godot-csharp-mobile-smoke/`의 RED 계약 커밋 뒤 구현했으며, Godot 4.6 Mono + .NET SDK 9.0.317에서 계약 검사, C# restore/build(경고·오류 0), Windows headless 실행을 통과했다. Android API 35 APK(ARM64+x86_64, minSdk 24, targetSdk 35)를 export하고 v2/v3 서명을 검증했으며, Android 15 x86_64 에뮬레이터에서 설치·최상위 실행·`MOBILE_SMOKE_OK | Godot C# | Android | .NET 9.0.19` 로그와 실제 가로 화면 렌더링을 확인했다. 최종 iOS 적합성은 Mac gate에서 확정한다
- 2026-09-09 — **Mac 인계 가능한 소스 구성 확인**. 공유 대상은 Godot 프로젝트·C# solution·애플리케이션 소스·scene·asset·Android/iOS preset 뼈대다. 검사 대상 애플리케이션 파일에는 Windows 절대 경로와 Windows 전용 API가 없다. 같은 Git 커밋을 macOS의 Godot 4.6 .NET + .NET 9에서 이어서 검증할 수 있다. 다만 iOS C#은 experimental이고 실제 export/서명/arm64 iPhone 설치는 macOS+Xcode가 필요하므로 Mac 후속 gate로 남겼다. `.godot/`, `bin/`, `obj/`, `build/`, 서명 정보는 공유하지 않는다
- 2026-09-09 — Android 검증 중 확인·해결한 도구 이슈: ETC2/ASTC import 설정이 없으면 Godot가 Android export를 거부하므로 활성화했고, 생성된 `android/**`·`build/**`가 C# 중복 컴파일되지 않게 제외했다. 플러그인이 없는 smoke 앱은 종료 hang이 발생한 Gradle export 대신 Godot 기본 APK export를 사용한다. SwiftShader 검은 화면은 호스트 NVIDIA GPU 재실행으로 해소했다
- 2026-09-09 — iPhone 지원 요구 반영. Godot 4.6 공식 문서에서 C# Android·iOS 내보내기가 모두 가능하지만 experimental임을 확인(Android는 .NET 9 이상, iOS는 macOS+Xcode 필요). D-001은 양 플랫폼 빈 프로젝트 export smoke test 통과를 조건으로 Godot 4.6+C# 선택, 실패 시 Unity 전환으로 기록
- 2026-09-09 — **`src/Hades.Client` 재사용 검토(spike): 불가 판정.** 클라이언트가 아니라 미완성 접속 테스트 도구다 — `Program.cs`의 Main이 `//TODO: Implement client that has been written.` + 스레드 대기이고, UI는 공격 패킷 1개를 보내는 버튼 하나뿐, 코드에 `Thread.Sleep(5000)`이 박혀 있다. 프로토콜 커버리지는 로그인까지: 서버가 처리하는 클라 패킷이 로그인 11종 + 월드 41종인데 `Client.cs`가 읽는 서버 패킷은 5개(0x7E·0x00·0x02·0x03·0x05)뿐이고 월드 진입 후 패킷은 하나도 파싱하지 않는다. 이식성도 없다 — .NET Framework 4.6.1(Windows 전용), `Hades.sln`에 미포함, 실제 빌드 시 참조 196개 미해결로 실패(`..\packages` 부재; C# 문법 오류는 0개). Windows 전용 의존은 `Forms/ClientForm*.cs`에만 있어 통신부 자체는 깨끗하지만 가져올 알맹이가 없다
- 2026-09-09 — 위 검토의 대안(모바일 클라이언트 재료): **프로토콜 사양**은 서버 핸들러 52종이 유일한 정본, **암호화**는 `Hades.Server.Base/Security/SecurityProvider.cs`(`Hades.Client` 쪽 237줄과 거의 동일한 중복이므로 서버본을 쓴다), **`.dat` 자료 읽기**는 `Hades.Client.Base`(netstandard2.0 — 모바일 이식 가능, `Archive`·`PaletteCollection`·`Map`·`Tile`; 솔루션 빌드에 포함돼 오류 없이 빌드됨)
- 2026-09-09 — **Hades Phase 0 실행 검증 통과**(`docs/run-procedure.md` 10절 A단계 1~9). 로그인→맵 입장까지 완주: 서버 로그 `wren : Welcome to Lorule`, 게임서버 2615 ESTABLISHED, `aislings/wren.json`(`GameMaster=True`, `CurrentMapId=1`, `4,4`), stderr 0바이트, 10초 자동 저장 동작. 절차서와 달랐던 점 3가지를 문서에 반영: ① 빌드 출력이 `Staging/net5.0/`(SDK 8이 TFM 폴더 추가) ② `game/`에 `Legend.dat`·`cious.dat`가 없어 클라이언트가 `main data file not found`로 종료 — 같은 저장소 `database/archives/`에서 복사해야 함(`LOD_`의 7.41 자료 불필요) ③ 서버가 `Content.Location` 아래 `areas/*.json`을 자기 경로로 덮어쓰므로 `database/server`를 `tmp/hades-run/`로 복사해 가리켜야 submodule이 clean 유지. 클라이언트 비밀번호 칸은 합성 키 입력을 거부해 사람이 직접 입력해야 함(SendInput·WM_CHAR 모두 무시)
- 2026-09-09 — 원본 실행으로 확인한 데이터 공백(PRD 픽스처 근거): 시작 시 로드가 Item 3 · Monster 3 · **Mundane 0** · Spell 0 · Skill 1 · Map 4 · Warp 4 · Popup 3 · Script 135. NPC 상호작용 검증에 필요한 Mundane 템플릿이 0개
- 2026-09-09 — PRD 미결정 2건 해소: **자동 저장 주기** = `LoruleConfig.json` `ServerConfig.SaveRate: 10.0`(초). **지면 아이템 소멸** = 소멸 코드가 없다(아이템은 안 사라짐). `Area.cs:253`의 3분은 소멸이 아니라 남이 떨군 아이템의 소유권 보호(`Cursed`)가 풀리는 시간이며 설정 키 없이 하드코딩
- 2026-09-09 — 로컬 Hades 자료 확인: `sources/Dark-Ages-Private-Server-master`는 중첩 복사본까지 조사했으나 NPC 템플릿·YAML 메뉴·시작맵 연계 몬스터/드롭 공백이 기존 submodule과 동일. `sources/DarkAges718single.exe`(SHA-256 `1E34B83A81E5F844AA62DE496A689706590334E3F3D01D8E1330B1907CBADAF7`) 확보 확인, 실행하지 않음
- 2026-09-09 — PRD `docs/mobile-test-v1-prd.md` v0.2 마무리(Codex 검토 지적 반영: 맵 입장 실패 AC-013, 화면비 AC-014, 확정 드롭 조건, 반복 실행 초기화, 자동 저장 주기 반영, NPC 메뉴 경로 `interactive/Menus` vs `Scripts/Menus` 미확정 기록). Medenia 실험 원복: 패치 99줄 `tmp/medenia-local-run.patch` 보존 후 submodule clean, 로컬 프로세스 종료. DungMunkey 모듈 캐시 `.git/modules` 정리. `sources/`의 로컬 다운로드(7.18 exe, Hades zip)와 `.playwright-mcp/`를 gitignore
- 2026-09-08 — 모바일 전환 기준선 결정: Hades 서버·JSON 데이터·게임 규칙을 유지하고 모바일 클라이언트를 신규 제작한다. Medenia는 실행 대상에서 제외하되 참고 자료와 미커밋 실험은 보존한다. `DungMunkey/Dark-Ages`는 네트워크 없는 오프라인 재현물이어서 submodule에서 제거(원격 저장소는 삭제하지 않음)
- 2026-09-08 — Medenia 후속 조사 기록: 최초 `ServerTableRequest id=0` 실패 뒤 미커밋 `client-crypto.ts` 오프셋 수정으로 `id=1`과 redirect 송신까지 확인. 다만 `ServerTableEntry.port`가 `NaN`이어서 로그인 완주는 미확인. `gateway-listener.ts` 디버그 변경과 실행 프로세스(8081·8082·5173)는 유지하고 이번 커밋에서 제외
- 2026-09-08 — Medenia B단계 실행(Sonnet 에이전트): 서버 8082·자산 8081·클라 5173 기동, SQLite 자동 생성, WS 핸드셰이크 OK. 실패: 서버 `gateway-listener.ts:35` `entry.ip` undefined(ServerTableRequest id=0) → 로그인 폼 미표시. 임시 패치 `tmp/medenia-local-run.patch`(58줄, 미커밋), 보고서 `tmp/medenia-run-report.md`. 레포 밖 변경: 시스템 Python에 setuptools 설치
- 2026-09-08 — 실행 절차·체크리스트 초안 `docs/run-procedure.md` 작성, catch-up 구조 세팅
- 2026-09-08 — 현행 프로젝트 분석서 `docs/current-system-analysis/` 01~09 작성, Graphite 작업 규칙 추가 (dfe0ff2, c74b2db)
